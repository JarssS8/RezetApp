# Same values as app/.env.local's VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY.
# The server also falls back to reading ../app/.env.local, so this file is
# only needed if you want mcp/ to use different credentials.
SUPABASE_URL=
SUPABASE_ANON_KEY=
