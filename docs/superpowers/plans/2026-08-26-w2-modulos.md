# W2 · Módulos — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el esqueleto + contratos de W0/W1 en una app usable: recetas que escalan, alimentos resueltos contra el catálogo, plan semanal con propuestas, despensa con escáner, IA opcional, envío a ShopList, ajustes completos y un MCP mínimo conectable.

**Architecture:** Ocho pistas paralelas en worktrees separados desde `main`. Cada pista añade un servicio (`lib/services/<agregado>.ts`, única capa que toca la DB), sus server actions (`lib/actions/<agregado>.ts`, único puente entre componentes cliente y servicios), sus páginas (Server Components por defecto) y sus componentes. El dominio (`lib/domain`, 100 % cubierto) y los esquemas zod (`lib/validation`) están congelados: se consumen, no se cambian. Todo texto de UI vía `next-intl`; todo color vía tokens; iconos propios.

**Tech Stack:** Next 16.3.3 · React 19.2 · TS 5.9 strict (`exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`) · Tailwind 4 + shadcn (Base UI) · drizzle-orm 0.45.2 + pg 8.23 · zod 4.4.3 · next-intl 4.13 · vitest 4.1 (proyectos `unit`/`ui`/`db`) · Playwright 1.62 · nuevas dependencias (exactas): `sharp 0.35.4`, `cheerio 1.2.0`, `@dnd-kit/core 6.3.1`, `@dnd-kit/sortable 10.0.0`, `@dnd-kit/utilities 3.2.2`, `@zxing/browser 0.2.1`, `ai 7.0.79`, `@ai-sdk/anthropic 4.0.42`, `@ai-sdk/openai 4.0.47`, `@ai-sdk/openai-compatible 3.0.37`, `@modelcontextprotocol/sdk 1.30.0`.

**Spec:** `docs/superpowers/specs/2026-08-26-rezetapp-design.md` (§2 capas, §3 estructura, §7 UI, §8 pantallas, §9 servicios, §10 IA, §12 MCP mínimo, §13 ShopList, §14 SSE, §16 tests, §17 fila W2).

## Global Constraints

- Identificadores en **inglés**; comentarios, docs y textos de usuario en **español** (y su par en inglés en `messages/en/*`). Nada de `any`. `exactOptionalPropertyTypes` y `noUncheckedIndexedAccess` **congelados**: nunca asignar `undefined` a una propiedad opcional; construir objetos condicionalmente.
- **Contratos congelados de W1**: `db/schema/*` (no se añaden columnas ni tablas en W2), `lib/domain/*` y sus firmas, `lib/validation/*` (se pueden AÑADIR esquemas nuevos en ficheros nuevos; no se renombran ni cambian los existentes), nombres de `components/icons` (se añaden iconos nuevos), rutas de `app/(app)/settings/*`, claves i18n existentes (se añaden, no se renombran).
- **Fronteras** (`eslint.config.mjs`, `boundaries/dependencies`): `app` → app/components/services/domain/validation/auth/events/lib/actions · `components` → components/domain/validation/lib/events/actions (**nunca `services` ni `db`**) · `actions` → actions/services/validation/auth/domain/events/lib · `services` → services/domain/validation/db/ai/integrations/auth/events/lib · `ai` → ai/domain/validation/db/lib · `integrations` → integrations/domain/lib · `auth` → auth/db/lib. `lib/domain` no importa nada del repo.
- **Server actions** (`lib/actions/*.ts`, `'use server'` en la primera línea): cada acción llama a `requireHousehold()` (o `requireRole('owner')`), valida con el esquema zod correspondiente, llama al servicio, `revalidatePath` de lo afectado y devuelve `ActionResult<T> = { ok: true; data: T } | { ok: false; code: string; message: string }`. Nunca lanzan hacia el cliente.
- **Servicios**: firma `fn(ctx: Ctx, input)`; `ctx.db` para todo; filtran SIEMPRE por `ctx.householdId`; `ServiceError` (`lib/services/ctx.ts`) con `code` `not_found|forbidden|conflict|validation`; emiten eventos con `emitHouseholdEvent(ctx.householdId, …)` (`lib/events/bus.ts`) tras escribir. Cada servicio tiene un test de **aislamiento entre hogares** (hogar B no ve ni toca datos de A).
- **Rutas REST** (`app/api/v1/**`): `requireApiToken(request, scopes)`; body con `readJson(request)` → 400 `validation` si `null` o si `safeParse` falla; errores `{ error: { code, message } }`; `ServiceError` → 404/403/409/400 según `code`; el resto vía `apiErrorResponse(e)`.
- **UI**: Server Components por defecto; `'use client'` solo con interacción. Zero literales JSX (`react/jsx-no-literals`), claves en `messages/<locale>/<ns>.json` con árboles idénticos en `es`/`en` (`pnpm i18n:check`). Colores solo por utilidades de tokens (`text-warn`, `bg-warn-soft`, `text-text-2`, `bg-card`, `bg-surface-2`, `text-acc-ink`, `rounded-sm|md|lg|pill`, `shadow-card`). Iconos: `components/icons` (añadir con `Icon` base: `currentColor`, trazo 1.85). Táctil ≥ 44 px (`min-h-11`). Números comparables con `tabular` (clase `.tabular` = mono + tabular-nums).
- **Tests**: `pnpm check` = typecheck + lint (0 avisos) + i18n + vitest. Tests de servicio bajo el proyecto `db` (`lib/services/**/*.test.ts`, `lib/actions/**/*.test.ts`) con `getTestDb()`/`truncateAll()` de `db/test/setup.ts` contra `DATABASE_URL_TEST` (cada worktree apunta a su propia base `rezetapp_test_<pista>`; ver README de planes). Componentes con estado en el proyecto `ui` (`*.test.tsx`). E2E con Playwright (`export LD_LIBRARY_PATH=$HOME/.local/chromium-deps/usr/lib/x86_64-linux-gnu`, ver `e2e/README.md`), sesión obtenida registrando un usuario con `enableVirtualAuthenticator` (`e2e/helpers/webauthn.ts`).
- **Commits**: español, imperativo, cortos, **sin trailers** ni menciones a herramientas de IA en ficheros o mensajes. Antes de cada commit: `git grep -ilE 'c[l]aude'` vacío. Autor ya configurado (`JarssS8`).
- **Versiones exactas** en `package.json` (`.npmrc` con `save-exact`).

---

## Orden de ejecución y merge

```
main (365ce2c + merge de w1-a-schema con el seed de alimentos)
  ├─ w2-b-foods        (Tareas 1–5)   ── merge 1º: (a) y (d) lo consumen
  ├─ w2-a-recipes      (Tareas 6–12)  ── arranca tras merge de (b)
  ├─ w2-d-pantry       (Tareas 13–17) ── arranca tras merge de (b)
  ├─ w2-c-plan         (Tareas 18–23) ── independiente de (b); merge tras (a)
  ├─ w2-e-ai           (Tareas 24–28) ── independiente; merge tras (b)
  ├─ w2-f-shoplist     (Tareas 29–31) ── independiente; merge tras (c)
  ├─ w2-g-settings     (Tareas 32–37) ── independiente
  └─ w2-h-mcp          (Tareas 38–40) ── arranca tras merge de (a) (usa recipes.search/get)
```

Orden de merge a `main`: **(b) → (a) → (d) → (c) → (e) → (f) → (g) → (h)**. Cada merge: `pnpm check` verde en `main`, `pnpm e2e` verde si la pista tocó UI. Conflictos previsibles y su resolución: `package.json`/`pnpm-lock.yaml` (unión de dependencias; `pnpm install`), `messages/*/*.json` (unión de claves; `pnpm i18n:check`), `components/icons/index.ts` (unión de exports), `lib/validation/index.ts` (unión de `export *`), `app/(app)/settings/*/page.tsx` (cada pista toca páginas distintas; (g) toca varias).

Preparación de cada worktree (lo hace el orquestador): `git worktree add -b w2-<letra>-<slug> .worktrees/w2-<letra>-<slug> main`, copiar `.env` con `DATABASE_URL_TEST=…/rezetapp_test_<letra>` (crear la base si no existe: `docker exec -i <contenedor-db> psql -U rezetapp -d rezetapp -c 'CREATE DATABASE rezetapp_test_<letra>'`), `pnpm install --frozen-lockfile --offline`.

## Mapa de ficheros (todas las pistas)

| Pista | Crea | Modifica |
|---|---|---|
| (b) | `lib/integrations/open-food-facts.ts`, `lib/services/foods.ts`, `lib/actions/foods.ts`, `components/foods/food-picker.tsx`, `components/foods/food-correction-dialog.tsx`, tests | `messages/*/recipes.json` (namespace `foods` va dentro de `recipes.json` bajo la clave `food`), `lib/validation/foods.ts` (añade `FoodCorrectionSchema`) |
| (a) | `lib/services/recipes.ts`, `lib/services/recipe-import.ts`, `lib/actions/recipes.ts`, `lib/uploads/store.ts`, `app/api/v1/uploads/route.ts`, `app/api/uploads/[...path]/route.ts`, `app/(app)/recipes/{[id],[id]/edit,new,import}/page.tsx`, `components/recipes/*`, tests, e2e | `app/(app)/recipes/page.tsx`, `messages/*/recipes.json`, `components/icons/*` (+`upload`, `scale`, `bookmark`) |
| (d) | `lib/services/pantry.ts`, `lib/actions/pantry.ts`, `app/(app)/pantry/{add,scan}/page.tsx`, `components/pantry/*`, tests, e2e | `app/(app)/pantry/page.tsx`, `messages/*/pantry.json` |
| (c) | `lib/services/plan.ts`, `lib/actions/plan.ts`, `app/(app)/plan/{month,proposals,shopping}/page.tsx`, `components/plan/*`, tests, e2e | `app/(app)/plan/page.tsx`, `messages/*/plan.json`, `components/icons/*` (+`calendar-month`, `skip`, `sparkles`) |
| (e) | `lib/ai/{provider,budget,models,structured}.ts`, `lib/ai/tasks/{parse-ingredients,import-recipe,estimate-nutrition,propose-plan}.ts`, `lib/services/ai-settings.ts`, `lib/actions/ai.ts`, `components/settings/ai-settings-form.tsx`, tests | `app/(app)/settings/ai/page.tsx`, `messages/*/settings.json`, `lib/validation/household.ts` (añade `AiSettingsSchema`) |
| (f) | `lib/integrations/shoplist.ts`, `lib/services/shopping.ts`, `lib/actions/shopping.ts`, `lib/services/shoplist-settings.ts`, `components/settings/shoplist-settings-form.tsx`, `components/plan/shopping-summary.tsx`, tests | `app/(app)/settings/shoplist/page.tsx`, `app/(app)/plan/shopping/page.tsx` (creada por (c); (f) la completa), `messages/*/settings.json`, `messages/*/plan.json`, `lib/validation/household.ts` (añade `ShoplistSettingsSchema`) |
| (g) | `lib/services/api-tokens.ts`, `lib/services/members.ts`, `lib/services/user-prefs.ts`, `lib/services/passkeys.ts`, `lib/actions/settings.ts`, `components/settings/*`, `app/api/auth/passkeys/{options,verify}/route.ts`, tests, e2e | `app/(app)/settings/{tokens,appearance,members,household,passkeys}/page.tsx`, `app/(app)/settings/layout.tsx` (botón salir), `messages/*/settings.json`, `messages/*/common.json` |
| (h) | `lib/mcp/{server,auth,tools/household,tools/recipes}.ts`, `app/mcp/route.ts`, tests | `docs/05-MCP.md` |

---

## Contrato de la pista (b) que consumen (a) y (d)

`lib/services/foods.ts` exporta exactamente estas firmas (se implementan en las Tareas 2–4; (a) y (d) las importan tal cual):

```ts
export interface FoodSummary {
  id: string
  householdId: string | null          // null = global (seed)
  name: string                        // nameEs o nameEn según ctx.locale
  nameEs: string
  nameEn: string
  defaultUnit: BaseUnit
  kcal100g: number | null
  isEstimated: boolean
  source: 'off' | 'usda' | 'manual' | 'ai'
  allergens: string[]
}
export type FoodWithNutrition = FoodSummary & FoodNutrition  // FoodNutrition de lib/domain/types

export type ResolveMethod = 'exact' | 'alias' | 'trigram'
export interface ResolvedFood { foodId: string; method: ResolveMethod; score: number; name: string }

export function searchFoods(ctx: Ctx, input: { q: string; locale?: Locale; limit?: number; offset?: number }): Promise<FoodSummary[]>
export function resolveFoodName(ctx: Ctx, foodName: string, locale: Locale): Promise<ResolvedFood | null>   // cascada §9.4 (a)(b); ≥ 0.6
export function resolveMany(ctx: Ctx, names: string[], locale: Locale): Promise<(ResolvedFood | null)[]>
export function getFoodsNutrition(ctx: Ctx, foodIds: string[]): Promise<Map<string, FoodWithNutrition>>
export function getFood(ctx: Ctx, foodId: string): Promise<FoodWithNutrition | null>
export function lookupBarcode(ctx: Ctx, barcode: string): Promise<FoodWithNutrition | null>            // local → Open Food Facts → crea el alimento en el hogar (no global), source 'off'
export function createFood(ctx: Ctx, input: FoodInput, source?: Food['source']): Promise<FoodWithNutrition>   // siempre household-scoped; 'off' lo usa internamente lookupBarcode
export function correctFood(ctx: Ctx, foodId: string, patch: FoodCorrection): Promise<FoodWithNutrition>
   // si el alimento es global: crea copia del hogar (source 'manual', is_estimated false) y devuelve la copia (id NUEVO);
   // si ya es del hogar: actualiza in situ. Los llamadores sustituyen el foodId por el devuelto.
```

Visibilidad: toda consulta devuelve alimentos con `household_id IS NULL OR household_id = ctx.householdId`, excluyendo `merged_into_id IS NOT NULL`, y prefiere el del hogar cuando hay empate de nombre.

---
# Pista (b) · Alimentos — Tareas 1–5

Worktree `w2-b-foods`. Base de test `rezetapp_test_b`.

### Task 1: Cliente de Open Food Facts

**Files:**
- Create: `lib/integrations/open-food-facts.ts`, `lib/integrations/open-food-facts.test.ts`

**Interfaces:**
- Produces: `fetchOffProduct(barcode: string, fetchImpl?: typeof fetch): Promise<OffProduct | null>`, `offToFoodInput(p: OffProduct): FoodInput`, tipo `OffProduct`.

- [ ] **Step 1: Test que falla**

```ts
// lib/integrations/open-food-facts.test.ts
import { describe, expect, it } from 'vitest'
import { fetchOffProduct, offToFoodInput } from './open-food-facts'

const sample = {
  status: 1,
  product: {
    code: '8410000810004',
    product_name: 'Galletas María',
    product_name_es: 'Galletas María',
    product_name_en: 'Maria biscuits',
    nutriments: { 'energy-kcal_100g': 436, proteins_100g: 7.5, carbohydrates_100g: 74, fat_100g: 12, fiber_100g: 2.5 },
    allergens_tags: ['en:gluten', 'en:milk'],
    quantity: '800 g',
  },
}

function fakeFetch(body: unknown, status = 200): typeof fetch {
  return (async () => new Response(JSON.stringify(body), { status })) as unknown as typeof fetch
}

describe('open-food-facts', () => {
  it('devuelve el producto y lo convierte a FoodInput', async () => {
    const p = await fetchOffProduct('8410000810004', fakeFetch(sample))
    expect(p?.code).toBe('8410000810004')
    const input = offToFoodInput(p!)
    expect(input).toMatchObject({ nameEs: 'Galletas María', nameEn: 'Maria biscuits', kcal100g: 436, protein100g: 7.5, carbs100g: 74, fat100g: 12, fiber100g: 2.5, barcode: '8410000810004' })
    expect(input.allergens).toEqual(['gluten', 'lactose'])
  })
  it('devuelve null si OFF no conoce el código o falla', async () => {
    expect(await fetchOffProduct('0000', fakeFetch({ status: 0 }))).toBeNull()
    expect(await fetchOffProduct('0000', fakeFetch({}, 500))).toBeNull()
  })
  it('sin nombre en inglés usa el genérico; sin kcal deja null', async () => {
    const p = await fetchOffProduct('1', fakeFetch({ status: 1, product: { code: '1', product_name: 'Cosa', nutriments: {} } }))
    const input = offToFoodInput(p!)
    expect(input.nameEn).toBe('Cosa')
    expect(input.kcal100g).toBeNull()
  })
})
```

- [ ] **Step 2: Ejecutar** — Run: `pnpm exec vitest run lib/integrations/open-food-facts.test.ts` — Expected: FAIL `Cannot find module './open-food-facts'`.

- [ ] **Step 3: Implementar**

```ts
// lib/integrations/open-food-facts.ts
// Cliente mínimo de Open Food Facts (ODbL). Solo lectura por código de barras.
import type { FoodInput } from '@/lib/validation/foods'

export interface OffProduct {
  code: string
  name: string
  nameEs: string | null
  nameEn: string | null
  kcal100g: number | null
  protein100g: number | null
  carbs100g: number | null
  fat100g: number | null
  fiber100g: number | null
  allergens: string[]
}

const OFF_URL = 'https://world.openfoodfacts.org/api/v2/product'
const USER_AGENT = 'RezetApp/0.1 (self-hosted recipe manager)'

// Mapa de etiquetas OFF → alérgenos de lib/validation/foods.ts::ALLERGENS
const ALLERGEN_MAP: Record<string, string> = {
  'en:gluten': 'gluten', 'en:milk': 'lactose', 'en:eggs': 'egg', 'en:fish': 'fish', 'en:crustaceans': 'shellfish',
  'en:nuts': 'nuts', 'en:peanuts': 'peanut', 'en:soybeans': 'soy', 'en:sesame-seeds': 'sesame', 'en:celery': 'celery',
  'en:mustard': 'mustard', 'en:sulphur-dioxide-and-sulphites': 'sulphites', 'en:lupin': 'lupin', 'en:molluscs': 'mollusc',
}

type OffJson = {
  status?: number
  product?: {
    code?: string
    product_name?: string
    product_name_es?: string
    product_name_en?: string
    nutriments?: Record<string, number | undefined>
    allergens_tags?: string[]
  }
}

function num(v: number | undefined): number | null {
  return typeof v === 'number' && Number.isFinite(v) ? v : null
}

export async function fetchOffProduct(barcode: string, fetchImpl: typeof fetch = fetch): Promise<OffProduct | null> {
  try {
    const res = await fetchImpl(`${OFF_URL}/${encodeURIComponent(barcode)}.json?fields=code,product_name,product_name_es,product_name_en,nutriments,allergens_tags`, {
      headers: { 'user-agent': USER_AGENT },
    })
    if (!res.ok) return null
    const json = (await res.json()) as OffJson
    if (json.status !== 1 || !json.product) return null
    const p = json.product
    const n = p.nutriments ?? {}
    const name = p.product_name?.trim() || p.product_name_es?.trim() || p.product_name_en?.trim()
    if (!name) return null
    return {
      code: p.code ?? barcode,
      name,
      nameEs: p.product_name_es?.trim() || null,
      nameEn: p.product_name_en?.trim() || null,
      kcal100g: num(n['energy-kcal_100g']),
      protein100g: num(n['proteins_100g']),
      carbs100g: num(n['carbohydrates_100g']),
      fat100g: num(n['fat_100g']),
      fiber100g: num(n['fiber_100g']),
      allergens: (p.allergens_tags ?? []).map((t) => ALLERGEN_MAP[t]).filter((a): a is string => Boolean(a)),
    }
  } catch {
    return null
  }
}

export function offToFoodInput(p: OffProduct): FoodInput {
  return {
    nameEs: p.nameEs ?? p.name,
    nameEn: p.nameEn ?? p.name,
    aliases: [],
    defaultUnit: 'g',
    kcal100g: p.kcal100g,
    protein100g: p.protein100g,
    carbs100g: p.carbs100g,
    fat100g: p.fat100g,
    fiber100g: p.fiber100g,
    barcode: p.code,
    allergens: p.allergens as FoodInput['allergens'],
    seasonalMonths: [],
  }
}
```

`FoodInput` no existe aún como tipo exportado: añadir en `lib/validation/foods.ts` la línea `export type FoodInput = z.infer<typeof FoodInputSchema>` (añadir, no cambiar).

- [ ] **Step 4: Ejecutar** — Run: `pnpm exec vitest run lib/integrations/open-food-facts.test.ts` — Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add lib/integrations/open-food-facts.ts lib/integrations/open-food-facts.test.ts lib/validation/foods.ts
git commit -m "Añade cliente de Open Food Facts"
```

---

### Task 2: Servicio de alimentos — búsqueda y resolución en cascada

**Files:**
- Create: `lib/services/foods.ts`, `lib/services/foods.test.ts`
- Modify: `lib/validation/foods.ts` (añade `FoodCorrectionSchema`)

**Interfaces:**
- Consumes: `foods` (schema), `normalizeSearchName` (domain), `Ctx`/`ServiceError`.
- Produces: el contrato de la sección "Contrato de la pista (b)": `searchFoods`, `resolveFoodName`, `resolveMany`, `getFoodsNutrition`, `getFood` (las de escritura llegan en Task 3).

- [ ] **Step 1: Test que falla** (proyecto `db`; requiere el seed de alimentos en la base de test — el helper lo siembra una vez)

```ts
// lib/services/foods.test.ts
import { afterAll, beforeAll, beforeEach, describe, expect, it } from 'vitest'
import { closeTestDb, getTestDb, truncateAll, type TestDb } from '@/db/test/setup'
import * as schema from '@/db/schema'
import type { Ctx } from '@/lib/services/ctx'
import { getFood, getFoodsNutrition, resolveFoodName, resolveMany, searchFoods } from './foods'

let db: TestDb
let ctxA: Ctx
let ctxB: Ctx

async function makeHousehold(name: string): Promise<Ctx> {
  const [h] = await db.insert(schema.households).values({ name }).returning()
  const [u] = await db.insert(schema.users).values({ displayName: name }).returning()
  if (!h || !u) throw new Error('setup')
  await db.insert(schema.householdMembers).values({ householdId: h.id, userId: u.id, role: 'owner' })
  return { db, householdId: h.id, userId: u.id, apiTokenId: null, role: 'owner', locale: 'es' }
}

async function seedGlobal(nameEs: string, nameEn: string, extra: Partial<typeof schema.foods.$inferInsert> = {}) {
  const [f] = await db
    .insert(schema.foods)
    .values({ nameEs, nameEn, searchNameEs: nameEs.toLowerCase(), searchNameEn: nameEn.toLowerCase(), source: 'usda', kcal100g: 40, ...extra })
    .returning()
  return f!
}

beforeAll(async () => {
  db = await getTestDb()
})
afterAll(closeTestDb)
beforeEach(async () => {
  await truncateAll(db)
  ctxA = await makeHousehold('A')
  ctxB = await makeHousehold('B')
  await seedGlobal('cebolla', 'onion', { aliases: ['cebolla blanca'] })
  await seedGlobal('cebolla morada', 'red onion')
  await seedGlobal('pimiento rojo', 'red pepper')
})

describe('searchFoods', () => {
  it('busca por trigram en el idioma del contexto y devuelve el nombre en ese idioma', async () => {
    const r = await searchFoods(ctxA, { q: 'cebol' })
    expect(r.map((f) => f.name)).toEqual(['cebolla', 'cebolla morada'])
    const en = await searchFoods({ ...ctxA, locale: 'en' }, { q: 'onio' })
    expect(en[0]?.name).toBe('onion')
  })
  it('prefiere el alimento del hogar cuando hay uno con el mismo nombre y no muestra los de otros hogares', async () => {
    await db.insert(schema.foods).values({ householdId: ctxA.householdId, nameEs: 'cebolla', nameEn: 'onion', searchNameEs: 'cebolla', searchNameEn: 'onion', source: 'manual', kcal100g: 38 })
    const a = await searchFoods(ctxA, { q: 'cebolla' })
    expect(a[0]?.householdId).toBe(ctxA.householdId)
    const b = await searchFoods(ctxB, { q: 'cebolla' })
    expect(b.every((f) => f.householdId === null)).toBe(true)
  })
})

describe('resolveFoodName', () => {
  it('exacto por nombre normalizado', async () => {
    const r = await resolveFoodName(ctxA, 'Cebolla', 'es')
    expect(r?.method).toBe('exact')
  })
  it('por alias', async () => {
    const r = await resolveFoodName(ctxA, 'cebolla blanca', 'es')
    expect(r?.method).toBe('alias')
    expect(r?.name).toBe('cebolla')
  })
  it('por trigram ≥ 0.6, y null por debajo', async () => {
    expect((await resolveFoodName(ctxA, 'pimientos rojos', 'es'))?.method).toBe('trigram')
    expect(await resolveFoodName(ctxA, 'zzzz', 'es')).toBeNull()
  })
  it('resolveMany conserva el orden y los nulls', async () => {
    const r = await resolveMany(ctxA, ['cebolla', 'nada', 'pimiento rojo'], 'es')
    expect(r.map((x) => x?.name ?? null)).toEqual(['cebolla', null, 'pimiento rojo'])
  })
})

describe('getFoodsNutrition / getFood', () => {
  it('devuelve un mapa por id con conversión y nutrición', async () => {
    const [f] = await searchFoods(ctxA, { q: 'cebolla morada' })
    const m = await getFoodsNutrition(ctxA, [f!.id])
    expect(m.get(f!.id)?.kcal100g).toBe(40)
    expect(await getFood(ctxB, f!.id)).not.toBeNull() // global: visible para todos
  })
  it('un alimento privado de A no es visible para B', async () => {
    const [p] = await db.insert(schema.foods).values({ householdId: ctxA.householdId, nameEs: 'secreto', nameEn: 'secret', searchNameEs: 'secreto', searchNameEn: 'secret' }).returning()
    expect(await getFood(ctxB, p!.id)).toBeNull()
    expect((await getFoodsNutrition(ctxB, [p!.id])).size).toBe(0)
  })
})
```

- [ ] **Step 2: Ejecutar** — Run: `pnpm exec vitest run --project db lib/services/foods.test.ts` — Expected: FAIL (módulo no encontrado).

- [ ] **Step 3: Añadir `FoodCorrectionSchema`** al final de `lib/validation/foods.ts`:

```ts
// Corrección manual de un alimento (gana a cualquier fuente). Todo opcional: se actualiza lo enviado.
export const FoodCorrectionSchema = FoodInputSchema.partial().strict()
export type FoodCorrection = z.infer<typeof FoodCorrectionSchema>
```

- [ ] **Step 4: Implementar `lib/services/foods.ts`** (lectura)

```ts
import { and, desc, eq, inArray, isNull, or, sql } from 'drizzle-orm'
import * as schema from '@/db/schema'
import type { BaseUnit, FoodNutrition, Locale } from '@/lib/domain/types'
import { normalizeSearchName } from '@/lib/domain/quantities'
import type { FoodCorrection, FoodInput } from '@/lib/validation/foods'
import { type Ctx, ServiceError } from './ctx'

export interface FoodSummary {
  id: string
  householdId: string | null
  name: string
  nameEs: string
  nameEn: string
  defaultUnit: BaseUnit
  kcal100g: number | null
  isEstimated: boolean
  source: 'off' | 'usda' | 'manual' | 'ai'
  allergens: string[]
}
export type FoodWithNutrition = FoodSummary & FoodNutrition
export type ResolveMethod = 'exact' | 'alias' | 'trigram'
export interface ResolvedFood { foodId: string; method: ResolveMethod; score: number; name: string }

const TRIGRAM_RESOLVE = 0.6
const TRIGRAM_SEARCH = 0.25

// Visible para el hogar: global o propio, y no fusionado
function visible(ctx: Ctx) {
  return and(or(isNull(schema.foods.householdId), eq(schema.foods.householdId, ctx.householdId)), isNull(schema.foods.mergedIntoId))
}

function toSummary(f: schema.Food, locale: Locale): FoodWithNutrition {
  return {
    id: f.id, householdId: f.householdId, name: locale === 'en' ? f.nameEn : f.nameEs, nameEs: f.nameEs, nameEn: f.nameEn,
    defaultUnit: f.defaultUnit, kcal100g: f.kcal100g, isEstimated: f.isEstimated, source: f.source, allergens: f.allergens,
    protein100g: f.protein100g, carbs100g: f.carbs100g, fat100g: f.fat100g, fiber100g: f.fiber100g,
    gramsPerCup: f.gramsPerCup, gramsPerTbsp: f.gramsPerTbsp, gramsPerUnit: f.gramsPerUnit, densityGPerMl: f.densityGPerMl,
  }
}

function searchColumn(locale: Locale) {
  return locale === 'en' ? schema.foods.searchNameEn : schema.foods.searchNameEs
}

export async function searchFoods(ctx: Ctx, input: { q: string; locale?: Locale; limit?: number; offset?: number }): Promise<FoodSummary[]> {
  const locale = input.locale ?? ctx.locale
  const q = normalizeSearchName(input.q)
  if (!q) return []
  const col = searchColumn(locale)
  const sim = sql<number>`similarity(${col}, ${q})`
  const rows = await ctx.db
    .select({ f: schema.foods, sim })
    .from(schema.foods)
    .where(and(visible(ctx), or(sql`${col} % ${q}`, sql`${col} LIKE ${q + '%'}`)))
    // propio antes que global; luego similitud; luego nombre
    .orderBy(sql`(${schema.foods.householdId} IS NULL)`, desc(sim), col)
    .limit(input.limit ?? 20)
    .offset(input.offset ?? 0)
  return rows.filter((r) => r.sim >= TRIGRAM_SEARCH || true).map((r) => toSummary(r.f, locale))
}

export async function resolveFoodName(ctx: Ctx, foodName: string, locale: Locale): Promise<ResolvedFood | null> {
  const q = normalizeSearchName(foodName)
  if (!q) return null
  const col = searchColumn(locale)
  const nameCol = locale === 'en' ? schema.foods.nameEn : schema.foods.nameEs
  // (a) exacto por nombre normalizado, propio antes que global
  const exact = await ctx.db.select({ id: schema.foods.id, name: nameCol }).from(schema.foods)
    .where(and(visible(ctx), eq(col, q))).orderBy(sql`(${schema.foods.householdId} IS NULL)`).limit(1)
  if (exact[0]) return { foodId: exact[0].id, method: 'exact', score: 1, name: exact[0].name }
  // (a') alias exacto (aliases se guardan tal cual; comparamos normalizado en SQL)
  const alias = await ctx.db.select({ id: schema.foods.id, name: nameCol }).from(schema.foods)
    .where(and(visible(ctx), sql`EXISTS (SELECT 1 FROM unnest(${schema.foods.aliases}) a WHERE lower(unaccent(a)) = ${q})`))
    .orderBy(sql`(${schema.foods.householdId} IS NULL)`).limit(1)
  if (alias[0]) return { foodId: alias[0].id, method: 'alias', score: 0.95, name: alias[0].name }
  // (b) trigram ≥ 0.6
  const sim = sql<number>`similarity(${col}, ${q})`
  const tri = await ctx.db.select({ id: schema.foods.id, name: nameCol, sim }).from(schema.foods)
    .where(and(visible(ctx), sql`${sim} >= ${TRIGRAM_RESOLVE}`)).orderBy(desc(sim), sql`(${schema.foods.householdId} IS NULL)`).limit(1)
  if (tri[0]) return { foodId: tri[0].id, method: 'trigram', score: tri[0].sim, name: tri[0].name }
  return null
}

export async function resolveMany(ctx: Ctx, names: string[], locale: Locale): Promise<(ResolvedFood | null)[]> {
  const out: (ResolvedFood | null)[] = []
  for (const n of names) out.push(await resolveFoodName(ctx, n, locale))
  return out
}

export async function getFoodsNutrition(ctx: Ctx, foodIds: string[]): Promise<Map<string, FoodWithNutrition>> {
  const map = new Map<string, FoodWithNutrition>()
  if (foodIds.length === 0) return map
  const rows = await ctx.db.select().from(schema.foods).where(and(visible(ctx), inArray(schema.foods.id, foodIds)))
  for (const f of rows) map.set(f.id, toSummary(f, ctx.locale))
  return map
}

export async function getFood(ctx: Ctx, foodId: string): Promise<FoodWithNutrition | null> {
  const [f] = await ctx.db.select().from(schema.foods).where(and(visible(ctx), eq(schema.foods.id, foodId))).limit(1)
  return f ? toSummary(f, ctx.locale) : null
}
```

`unaccent` no está instalado en la migración inicial: el alias se compara con `lower(a)` y con la normalización hecha en JS al guardar (Task 3 normaliza `aliases` con `normalizeSearchName` antes de insertar, así que la comparación SQL es `lower(a) = q`). Sustituir `lower(unaccent(a))` por `lower(a)` y documentar en un comentario que los alias se guardan ya normalizados. Eliminar también el filtro `|| true` del `searchFoods` (la condición `%`/`LIKE` ya filtra en SQL): `return rows.map((r) => toSummary(r.f, locale))`. El umbral `TRIGRAM_SEARCH` se aplica con `SET pg_trgm.similarity_threshold`? No: usar la condición explícita `sql\`similarity(${col}, ${q}) >= ${TRIGRAM_SEARCH}\`` en vez de `%` para no depender del GUC.

- [ ] **Step 5: Ejecutar** — Run: `pnpm exec vitest run --project db lib/services/foods.test.ts` — Expected: 8 passed. Si `similarity` da error "function does not exist": la extensión `pg_trgm` está en la migración inicial; comprobar que `getTestDb()` aplicó las migraciones (`runMigrations`).

- [ ] **Step 6: Commit**

```bash
git add lib/services/foods.ts lib/services/foods.test.ts lib/validation/foods.ts
git commit -m "Añade servicio de alimentos con búsqueda y resolución en cascada"
```

---

### Task 3: Servicio de alimentos — escritura, corrección manual y código de barras

**Files:**
- Modify: `lib/services/foods.ts`, `lib/services/foods.test.ts`

**Interfaces:**
- Produces: `createFood`, `correctFood`, `lookupBarcode` (ver contrato).

- [ ] **Step 1: Tests que fallan** (añadir al fichero de test)

```ts
import { correctFood, createFood, lookupBarcode } from './foods'

describe('createFood / correctFood', () => {
  it('createFood crea un alimento del hogar con nombres normalizados y aliases normalizados', async () => {
    const f = await createFood(ctxA, { nameEs: 'Tomate Pera', nameEn: 'Plum Tomato', aliases: ['Tomate de pera'], defaultUnit: 'g', allergens: [], seasonalMonths: [7, 8] })
    expect(f.householdId).toBe(ctxA.householdId)
    expect(f.source).toBe('manual')
    expect((await resolveFoodName(ctxA, 'tomate de pera', 'es'))?.foodId).toBe(f.id)
    expect(await resolveFoodName(ctxB, 'tomate pera', 'es')).toBeNull()
  })
  it('correctFood sobre un alimento global crea una copia del hogar y la devuelve', async () => {
    const [g] = await searchFoods(ctxA, { q: 'pimiento rojo' })
    const c = await correctFood(ctxA, g!.id, { kcal100g: 31, isEstimated: undefined } as never)
    expect(c.id).not.toBe(g!.id)
    expect(c.householdId).toBe(ctxA.householdId)
    expect(c.kcal100g).toBe(31)
    expect(c.source).toBe('manual')
    // el global sigue intacto y el hogar A ahora resuelve a su copia
    expect((await getFood(ctxB, g!.id))?.kcal100g).toBe(40)
    expect((await resolveFoodName(ctxA, 'pimiento rojo', 'es'))?.foodId).toBe(c.id)
  })
  it('correctFood sobre un alimento propio actualiza in situ; sobre uno ajeno → not_found', async () => {
    const f = await createFood(ctxA, { nameEs: 'x', nameEn: 'x', aliases: [], defaultUnit: 'g', allergens: [], seasonalMonths: [] })
    const c = await correctFood(ctxA, f.id, { kcal100g: 5 })
    expect(c.id).toBe(f.id)
    await expect(correctFood(ctxB, f.id, { kcal100g: 5 })).rejects.toMatchObject({ code: 'not_found' })
  })
})

describe('lookupBarcode', () => {
  it('devuelve el alimento local si ya existe con ese código', async () => {
    await seedGlobal('galleta', 'biscuit', { barcode: '123' })
    const f = await lookupBarcode(ctxA, '123', async () => null)
    expect(f?.nameEs).toBe('galleta')
  })
  it('consulta OFF si no existe y crea un alimento global source off', async () => {
    const fetcher = async () => ({ code: '999', name: 'Yogur', nameEs: 'Yogur natural', nameEn: 'Plain yogurt', kcal100g: 60, protein100g: 3.5, carbs100g: 4.5, fat100g: 3, fiber100g: 0, allergens: ['lactose'] })
    const f = await lookupBarcode(ctxA, '999', fetcher)
    expect(f?.source).toBe('off')
    expect(f?.householdId).toBeNull()
    expect((await lookupBarcode(ctxB, '999', async () => null))?.id).toBe(f?.id)
  })
  it('devuelve null si OFF no lo conoce', async () => {
    expect(await lookupBarcode(ctxA, '000', async () => null)).toBeNull()
  })
})
```

Quitar el `{ kcal100g: 31, isEstimated: undefined } as never` del test y dejar `{ kcal100g: 31 }` — el cast era un desliz; `FoodCorrection` no tiene `isEstimated`.

- [ ] **Step 2: Ejecutar** — Expected: los nuevos tests fallan (funciones no exportadas).

- [ ] **Step 3: Implementar** (añadir a `lib/services/foods.ts`)

```ts
import { fetchOffProduct, offToFoodInput, type OffProduct } from '@/lib/integrations/open-food-facts'

function toInsert(input: FoodInput, householdId: string | null, source: schema.Food['source'], isEstimated: boolean): typeof schema.foods.$inferInsert {
  return {
    householdId, nameEs: input.nameEs, nameEn: input.nameEn,
    searchNameEs: normalizeSearchName(input.nameEs), searchNameEn: normalizeSearchName(input.nameEn),
    aliases: input.aliases.map(normalizeSearchName).filter(Boolean),
    defaultUnit: input.defaultUnit,
    kcal100g: input.kcal100g ?? null, protein100g: input.protein100g ?? null, carbs100g: input.carbs100g ?? null,
    fat100g: input.fat100g ?? null, fiber100g: input.fiber100g ?? null,
    barcode: input.barcode ?? null, allergens: input.allergens,
    gramsPerCup: input.gramsPerCup ?? null, gramsPerTbsp: input.gramsPerTbsp ?? null, gramsPerUnit: input.gramsPerUnit ?? null,
    densityGPerMl: input.densityGPerMl ?? null, seasonalMonths: input.seasonalMonths, source, isEstimated,
  }
}

export async function createFood(ctx: Ctx, input: FoodInput, source: 'manual' | 'ai' = 'manual'): Promise<FoodWithNutrition> {
  const [f] = await ctx.db.insert(schema.foods).values(toInsert(input, ctx.householdId, source, source === 'ai')).returning()
  if (!f) throw new ServiceError('conflict', 'No se pudo crear el alimento')
  return toSummary(f, ctx.locale)
}

// La corrección manual gana a todo (docs/03 §4): si el alimento es global se copia al hogar.
export async function correctFood(ctx: Ctx, foodId: string, patch: FoodCorrection): Promise<FoodWithNutrition> {
  const [f] = await ctx.db.select().from(schema.foods).where(and(visible(ctx), eq(schema.foods.id, foodId))).limit(1)
  if (!f) throw new ServiceError('not_found', 'Alimento no encontrado')
  const merged: FoodInput = {
    nameEs: patch.nameEs ?? f.nameEs, nameEn: patch.nameEn ?? f.nameEn, aliases: patch.aliases ?? f.aliases, defaultUnit: patch.defaultUnit ?? f.defaultUnit,
    kcal100g: patch.kcal100g !== undefined ? patch.kcal100g : f.kcal100g, protein100g: patch.protein100g !== undefined ? patch.protein100g : f.protein100g,
    carbs100g: patch.carbs100g !== undefined ? patch.carbs100g : f.carbs100g, fat100g: patch.fat100g !== undefined ? patch.fat100g : f.fat100g,
    fiber100g: patch.fiber100g !== undefined ? patch.fiber100g : f.fiber100g, barcode: patch.barcode !== undefined ? patch.barcode : f.barcode,
    allergens: (patch.allergens ?? f.allergens) as FoodInput['allergens'],
    gramsPerCup: patch.gramsPerCup !== undefined ? patch.gramsPerCup : f.gramsPerCup, gramsPerTbsp: patch.gramsPerTbsp !== undefined ? patch.gramsPerTbsp : f.gramsPerTbsp,
    gramsPerUnit: patch.gramsPerUnit !== undefined ? patch.gramsPerUnit : f.gramsPerUnit, densityGPerMl: patch.densityGPerMl !== undefined ? patch.densityGPerMl : f.densityGPerMl,
    seasonalMonths: patch.seasonalMonths ?? f.seasonalMonths,
  }
  if (f.householdId === null) {
    const [copy] = await ctx.db.insert(schema.foods).values({ ...toInsert(merged, ctx.householdId, 'manual', false), sourceRef: null }).returning()
    if (!copy) throw new ServiceError('conflict', 'No se pudo copiar el alimento')
    return toSummary(copy, ctx.locale)
  }
  const [updated] = await ctx.db.update(schema.foods)
    .set({ ...toInsert(merged, f.householdId, 'manual', false), updatedAt: new Date() })
    .where(and(eq(schema.foods.id, f.id), eq(schema.foods.householdId, ctx.householdId))).returning()
  if (!updated) throw new ServiceError('not_found', 'Alimento no encontrado')
  return toSummary(updated, ctx.locale)
}

export type BarcodeFetcher = (barcode: string) => Promise<OffProduct | null>

export async function lookupBarcode(ctx: Ctx, barcode: string, fetcher: BarcodeFetcher = fetchOffProduct): Promise<FoodWithNutrition | null> {
  const [local] = await ctx.db.select().from(schema.foods).where(and(visible(ctx), eq(schema.foods.barcode, barcode)))
    .orderBy(sql`(${schema.foods.householdId} IS NULL)`).limit(1)
  if (local) return toSummary(local, ctx.locale)
  const off = await fetcher(barcode)
  if (!off) return null
  // Global (household_id NULL): el producto envasado es el mismo para todos los hogares
  const [created] = await ctx.db.insert(schema.foods)
    .values({ ...toInsert(offToFoodInput(off), null, 'off', false), sourceRef: off.code })
    .onConflictDoNothing()
    .returning()
  if (created) return toSummary(created, ctx.locale)
  const [again] = await ctx.db.select().from(schema.foods).where(eq(schema.foods.barcode, barcode)).limit(1)
  return again ? toSummary(again, ctx.locale) : null
}
```

- [ ] **Step 4: Ejecutar** — Run: `pnpm exec vitest run --project db lib/services/foods.test.ts` — Expected: 14 passed.

- [ ] **Step 5: Commit**

```bash
git add lib/services/foods.ts lib/services/foods.test.ts
git commit -m "Añade creación, corrección manual y código de barras de alimentos"
```

---

### Task 4: Server actions y selector de alimento reutilizable

**Files:**
- Create: `lib/actions/result.ts`, `lib/actions/foods.ts`, `components/foods/food-picker.tsx`, `components/foods/food-picker.test.tsx`
- Modify: `messages/es/recipes.json`, `messages/en/recipes.json`

**Interfaces:**
- Produces: `ActionResult<T>`, `ok(data)`, `fail(code, message)`, `fromServiceError(e)`; acciones `searchFoodsAction(q)`, `resolveFoodAction(name)`, `lookupBarcodeAction(code)`, `createFoodAction(input)`, `correctFoodAction(foodId, patch)`; componente `<FoodPicker value onChange locale />` (cliente) que (a) y (d) reutilizan.

- [ ] **Step 1: `lib/actions/result.ts`** (contrato de todas las acciones de W2)

```ts
import { ServiceError } from '@/lib/services/ctx'

export type ActionResult<T> = { ok: true; data: T } | { ok: false; code: string; message: string }

export function ok<T>(data: T): ActionResult<T> {
  return { ok: true, data }
}
export function fail<T = never>(code: string, message: string): ActionResult<T> {
  return { ok: false, code, message }
}
// Traduce errores de servicio a resultado; cualquier otro error se registra y sale como 'internal'
export function fromError<T = never>(e: unknown): ActionResult<T> {
  if (e instanceof ServiceError) return fail(e.code, e.message)
  console.error('[action]', e)
  return fail('internal', 'Error interno')
}
```

- [ ] **Step 2: Test del picker (proyecto `ui`)**

```tsx
// components/foods/food-picker.test.tsx
import { fireEvent, render, screen, waitFor } from '@testing-library/react'
import { NextIntlClientProvider } from 'next-intl'
import { describe, expect, it, vi } from 'vitest'
import recipes from '@/messages/es/recipes.json'
import { FoodPicker } from './food-picker'

const search = vi.fn(async (q: string) => ({ ok: true as const, data: q.startsWith('ce') ? [{ id: 'f1', name: 'cebolla', householdId: null, isEstimated: false }] : [] }))

describe('FoodPicker', () => {
  it('busca al escribir y selecciona un alimento', async () => {
    const onChange = vi.fn()
    render(
      <NextIntlClientProvider locale="es" messages={{ recipes }}>
        <FoodPicker value={null} onChange={onChange} search={search} />
      </NextIntlClientProvider>,
    )
    fireEvent.change(screen.getByRole('combobox'), { target: { value: 'ceb' } })
    await waitFor(() => expect(screen.getByRole('option', { name: /cebolla/ })).toBeInTheDocument())
    fireEvent.click(screen.getByRole('option', { name: /cebolla/ }))
    expect(onChange).toHaveBeenCalledWith({ id: 'f1', name: 'cebolla' })
  })
})
```

- [ ] **Step 3: Ejecutar** — Run: `pnpm exec vitest run --project ui components/foods` — Expected: FAIL.

- [ ] **Step 4: Acciones y componente**

```ts
// lib/actions/foods.ts
'use server'
import { revalidatePath } from 'next/cache'
import { requireHousehold } from '@/lib/auth/guards'
import { correctFood, createFood, lookupBarcode, resolveFoodName, searchFoods, type FoodSummary, type FoodWithNutrition, type ResolvedFood } from '@/lib/services/foods'
import { BarcodeSchema, FoodCorrectionSchema, FoodInputSchema, FoodSearchSchema } from '@/lib/validation/foods'
import { type ActionResult, fail, fromError, ok } from './result'

export async function searchFoodsAction(q: string): Promise<ActionResult<FoodSummary[]>> {
  try {
    const ctx = await requireHousehold()
    const parsed = FoodSearchSchema.safeParse({ q, locale: ctx.locale })
    if (!parsed.success) return ok([])
    return ok(await searchFoods(ctx, parsed.data))
  } catch (e) {
    return fromError(e)
  }
}

export async function resolveFoodAction(name: string): Promise<ActionResult<ResolvedFood | null>> {
  try {
    const ctx = await requireHousehold()
    return ok(await resolveFoodName(ctx, name, ctx.locale))
  } catch (e) {
    return fromError(e)
  }
}

export async function lookupBarcodeAction(code: string): Promise<ActionResult<FoodWithNutrition | null>> {
  try {
    const ctx = await requireHousehold()
    const parsed = BarcodeSchema.safeParse(code)
    if (!parsed.success) return fail('validation', 'Código de barras inválido')
    return ok(await lookupBarcode(ctx, parsed.data))
  } catch (e) {
    return fromError(e)
  }
}

export async function createFoodAction(input: unknown): Promise<ActionResult<FoodWithNutrition>> {
  try {
    const ctx = await requireHousehold()
    const parsed = FoodInputSchema.safeParse(input)
    if (!parsed.success) return fail('validation', parsed.error.issues[0]?.message ?? 'Datos inválidos')
    const f = await createFood(ctx, parsed.data)
    revalidatePath('/recipes')
    revalidatePath('/pantry')
    return ok(f)
  } catch (e) {
    return fromError(e)
  }
}

export async function correctFoodAction(foodId: string, patch: unknown): Promise<ActionResult<FoodWithNutrition>> {
  try {
    const ctx = await requireHousehold()
    const parsed = FoodCorrectionSchema.safeParse(patch)
    if (!parsed.success) return fail('validation', parsed.error.issues[0]?.message ?? 'Datos inválidos')
    const f = await correctFood(ctx, foodId, parsed.data)
    revalidatePath('/recipes')
    revalidatePath('/pantry')
    return ok(f)
  } catch (e) {
    return fromError(e)
  }
}
```

```tsx
// components/foods/food-picker.tsx
'use client'
import { useTranslations } from 'next-intl'
import { useEffect, useId, useRef, useState } from 'react'
import { EstimatedIcon } from '@/components/icons'
import { Input } from '@/components/ui/input'
import type { ActionResult } from '@/lib/actions/result'
import { cn } from '@/lib/utils'

export type PickedFood = { id: string; name: string }
type Candidate = { id: string; name: string; householdId: string | null; isEstimated: boolean }
type SearchFn = (q: string) => Promise<ActionResult<Candidate[]>>

// Combobox accesible (role=combobox + listbox). `search` se inyecta: en la app es searchFoodsAction,
// en tests un mock. Debounce de 200 ms; mínimo 2 caracteres.
export function FoodPicker({ value, onChange, search, className }: { value: PickedFood | null; onChange: (f: PickedFood | null) => void; search: SearchFn; className?: string }) {
  const t = useTranslations('recipes')
  const id = useId()
  const [q, setQ] = useState(value?.name ?? '')
  const [items, setItems] = useState<Candidate[]>([])
  const [open, setOpen] = useState(false)
  const timer = useRef<ReturnType<typeof setTimeout> | null>(null)

  useEffect(() => {
    if (timer.current) clearTimeout(timer.current)
    if (q.trim().length < 2) {
      setItems([])
      return
    }
    timer.current = setTimeout(async () => {
      const r = await search(q.trim())
      setItems(r.ok ? r.data : [])
      setOpen(true)
    }, 200)
    return () => {
      if (timer.current) clearTimeout(timer.current)
    }
  }, [q, search])

  return (
    <div className={cn('relative', className)}>
      <Input
        role="combobox"
        aria-expanded={open}
        aria-controls={`${id}-list`}
        aria-autocomplete="list"
        value={q}
        placeholder={t('food.searchPlaceholder')}
        onChange={(e) => {
          setQ(e.target.value)
          if (value) onChange(null)
        }}
        onFocus={() => items.length > 0 && setOpen(true)}
        onBlur={() => setTimeout(() => setOpen(false), 120)}
      />
      {open && items.length > 0 && (
        <ul id={`${id}-list`} role="listbox" className="absolute z-20 mt-1 max-h-64 w-full overflow-auto rounded-md border bg-card shadow-card">
          {items.map((f) => (
            <li
              key={f.id}
              role="option"
              aria-selected={value?.id === f.id}
              className="flex min-h-11 cursor-pointer items-center justify-between px-3 text-sm hover:bg-surface-2"
              onMouseDown={(e) => e.preventDefault()}
              onClick={() => {
                onChange({ id: f.id, name: f.name })
                setQ(f.name)
                setOpen(false)
              }}
            >
              <span>{f.name}</span>
              <span className="flex items-center gap-1 text-xs text-text-2">
                {f.householdId ? t('food.mine') : null}
                {f.isEstimated ? <EstimatedIcon size={14} title={t('food.estimated')} /> : null}
              </span>
            </li>
          ))}
        </ul>
      )}
    </div>
  )
}
```

`messages/es/recipes.json` (añadir al objeto existente; `en` con las mismas claves):

```json
{
  "title": "Recetas",
  "food": {
    "searchPlaceholder": "Buscar alimento…",
    "mine": "propio",
    "estimated": "Nutrición estimada",
    "correct": "Corregir alimento",
    "unresolved": "Sin alimento asignado",
    "create": "Crear alimento nuevo"
  }
}
```

`en`: `"searchPlaceholder": "Search food…", "mine": "mine", "estimated": "Estimated nutrition", "correct": "Correct food", "unresolved": "No food assigned", "create": "Create new food"`.

- [ ] **Step 5: Ejecutar** — Run: `pnpm exec vitest run --project ui components/foods && pnpm check` — Expected: verde.

- [ ] **Step 6: Commit**

```bash
git add lib/actions/result.ts lib/actions/foods.ts components/foods/food-picker.tsx components/foods/food-picker.test.tsx messages/es/recipes.json messages/en/recipes.json
git commit -m "Añade acciones y selector de alimentos"
```

---

### Task 5: Diálogo de corrección de alimento

**Files:**
- Create: `components/foods/food-correction-dialog.tsx`, `components/foods/food-correction-dialog.test.tsx`
- Modify: `messages/*/recipes.json`

**Interfaces:**
- Produces: `<FoodCorrectionDialog food onSaved correct />` — formulario con nombre es/en, kcal y macros por 100 g, gramos por taza/cucharada/unidad, densidad, alérgenos; llama a `correctFoodAction` (inyectable) y devuelve el alimento resultante (posiblemente con id nuevo).

- [ ] **Step 1: Test (ui)**

```tsx
import { fireEvent, render, screen, waitFor } from '@testing-library/react'
import { NextIntlClientProvider } from 'next-intl'
import { describe, expect, it, vi } from 'vitest'
import recipes from '@/messages/es/recipes.json'
import common from '@/messages/es/common.json'
import { FoodCorrectionDialog } from './food-correction-dialog'

const food = { id: 'g1', householdId: null, name: 'pimiento rojo', nameEs: 'pimiento rojo', nameEn: 'red pepper', defaultUnit: 'g' as const, kcal100g: 40, isEstimated: false, source: 'usda' as const, allergens: [], protein100g: 1, carbs100g: 6, fat100g: 0.3, fiber100g: 2, gramsPerCup: null, gramsPerTbsp: null, gramsPerUnit: 150, densityGPerMl: null }

describe('FoodCorrectionDialog', () => {
  it('envía solo los campos cambiados y notifica el alimento devuelto', async () => {
    const correct = vi.fn(async (_id: string, patch: unknown) => ({ ok: true as const, data: { ...food, id: 'h1', householdId: 'A', kcal100g: 31, ...(patch as object) } }))
    const onSaved = vi.fn()
    render(
      <NextIntlClientProvider locale="es" messages={{ recipes, common }}>
        <FoodCorrectionDialog food={food} open onOpenChange={() => {}} correct={correct} onSaved={onSaved} />
      </NextIntlClientProvider>,
    )
    fireEvent.change(screen.getByLabelText(/kcal/i), { target: { value: '31' } })
    fireEvent.click(screen.getByRole('button', { name: /guardar/i }))
    await waitFor(() => expect(correct).toHaveBeenCalledWith('g1', { kcal100g: 31 }))
    expect(onSaved).toHaveBeenCalledWith(expect.objectContaining({ id: 'h1', kcal100g: 31 }))
  })
})
```

- [ ] **Step 2: Implementar** — `'use client'`; usa `Dialog`, `DialogContent`, `DialogTitle` de `components/ui/dialog`, `Input`/`Label`, `Button`. Estado local con los valores actuales; al guardar calcula el `patch` comparando con `food` (solo claves cambiadas; números con `Number(value)`, vacío → `null`); alérgenos como checkboxes con etiquetas de `recipes.food.allergens.<key>` (añadir las 14 claves de `ALLERGENS` en es/en: gluten "Gluten", lactose "Lactosa", egg "Huevo", fish "Pescado", shellfish "Crustáceos", nuts "Frutos secos", peanut "Cacahuete", soy "Soja", sesame "Sésamo", celery "Apio", mustard "Mostaza", sulphites "Sulfitos", lupin "Altramuces", mollusc "Moluscos"); etiquetas de campos bajo `recipes.food.fields.*` (`nameEs` "Nombre (es)", `nameEn` "Nombre (en)", `kcal100g` "kcal por 100 g", `protein100g` "Proteína / 100 g", `carbs100g` "Hidratos / 100 g", `fat100g` "Grasa / 100 g", `fiber100g` "Fibra / 100 g", `gramsPerCup` "g por taza", `gramsPerTbsp` "g por cucharada", `gramsPerUnit` "g por unidad", `densityGPerMl` "Densidad (g/ml)") y nota `recipes.food.copyNote` "Al corregir un alimento del catálogo se crea tu propia versión; tus recetas la usarán a partir de ahora." Botones `common.actions.save` / `common.actions.cancel`; `aria-busy` mientras guarda; error con `role="alert"`.

- [ ] **Step 3: Ejecutar** — `pnpm exec vitest run --project ui components/foods && pnpm check` — verde.

- [ ] **Step 4: Commit**

```bash
git add components/foods/food-correction-dialog.tsx components/foods/food-correction-dialog.test.tsx messages/es/recipes.json messages/en/recipes.json
git commit -m "Añade diálogo de corrección de alimentos"
```

**Pista (b) lista cuando:** `pnpm check` verde; `foods.test.ts` 14/14 bajo `db`; merge a `main` antes de arrancar (a) y (d).

---
# Pista (a) · Recetas — Tareas 6–12

Worktree `w2-a-recipes` desde `main` **con (b) mergeada**. Base de test `rezetapp_test_a`. Dependencias nuevas: `pnpm add sharp@0.35.4 cheerio@1.2.0`.

### Task 6: Servicio de recetas — crear, leer, escalar, actualizar, borrar

**Files:**
- Create: `lib/services/recipes.ts`, `lib/services/recipes.test.ts`, `lib/services/recipe-mapper.ts`

**Interfaces:**
- Consumes: `recipes`, `recipeIngredients`, `recipeSteps`, `tags`, `recipeTags` (schema); `parseIngredientLine`, `toBaseUnit`, `isNonLinearByDefault`, `recipeNutrition`, `scaleRecipe`, `detectTimers` (domain); `resolveMany`, `getFoodsNutrition` (foods); `RecipeInput`, `RecipeGetQuerySchema` (validation).
- Produces:
  ```ts
  export interface RecipeDetail { recipe: Recipe; ingredients: IngredientWithFood[]; steps: RecipeStep[]; tags: { id: string; name: string; slug: string }[]; nutrition: Nutrition | null; scaled: ScaledRecipe | null }
  export function createRecipe(ctx, input: RecipeInput): Promise<RecipeDetail>
  export function updateRecipe(ctx, id, input: RecipeInput): Promise<RecipeDetail>
  export function getRecipe(ctx, id, opts?: { servings?: number }): Promise<RecipeDetail | null>   // scaled != null cuando servings ≠ servingsBase
  export function softDeleteRecipe(ctx, id): Promise<void>
  export function prepareIngredients(ctx, inputs: RecipeIngredientInput[], locale): Promise<PreparedIngredient[]>  // parsea rawText si faltan quantity/unit, resuelve foodId si falta, calcula scalesLinearly por defecto
  ```
  `lib/services/recipe-mapper.ts`: `toIngredientWithFood(row, food)`, `toRecipeForScaling(detail)` (puros sobre filas; sin DB).

- [ ] **Step 1: Test que falla** (proyecto `db`)

```ts
// lib/services/recipes.test.ts
import { afterAll, beforeAll, beforeEach, describe, expect, it } from 'vitest'
import { closeTestDb, getTestDb, truncateAll, type TestDb } from '@/db/test/setup'
import * as schema from '@/db/schema'
import type { Ctx } from '@/lib/services/ctx'
import { createRecipe, getRecipe, softDeleteRecipe, updateRecipe } from './recipes'

let db: TestDb
let ctxA: Ctx
let ctxB: Ctx
let onionId: string
let saltId: string

async function makeHousehold(name: string): Promise<Ctx> {
  const [h] = await db.insert(schema.households).values({ name }).returning()
  const [u] = await db.insert(schema.users).values({ displayName: name }).returning()
  await db.insert(schema.householdMembers).values({ householdId: h!.id, userId: u!.id, role: 'owner' })
  return { db, householdId: h!.id, userId: u!.id, apiTokenId: null, role: 'owner', locale: 'es' }
}

beforeAll(async () => { db = await getTestDb() })
afterAll(closeTestDb)
beforeEach(async () => {
  await truncateAll(db)
  ctxA = await makeHousehold('A')
  ctxB = await makeHousehold('B')
  const [onion] = await db.insert(schema.foods).values({ nameEs: 'cebolla', nameEn: 'onion', searchNameEs: 'cebolla', searchNameEn: 'onion', source: 'usda', kcal100g: 40, protein100g: 1.1, carbs100g: 9.3, fat100g: 0.1, fiber100g: 1.7, gramsPerUnit: 150 }).returning()
  const [salt] = await db.insert(schema.foods).values({ nameEs: 'sal', nameEn: 'salt', searchNameEs: 'sal', searchNameEn: 'salt', source: 'usda', kcal100g: 0, protein100g: 0, carbs100g: 0, fat100g: 0, fiber100g: 0 }).returning()
  onionId = onion!.id
  saltId = salt!.id
})

const input = {
  title: 'Cebolla caramelizada', servingsBase: 4, ingredients: [
    { rawText: '2 cebollas grandes', scalesLinearly: true },
    { rawText: '1 cdta de sal', scalesLinearly: true },
  ], steps: [{ text: 'Pocha la cebolla 25 minutos a fuego suave' }], tags: ['guarnición', 'básico'], imageUrls: [],
}

describe('createRecipe', () => {
  it('parsea ingredientes, resuelve alimentos, marca la sal como no lineal y calcula nutrición por ración', async () => {
    const d = await createRecipe(ctxA, input)
    expect(d.ingredients.map((i) => i.foodId)).toEqual([onionId, saltId])
    expect(d.ingredients[0]).toMatchObject({ quantity: 300, unit: 'g', displayQuantity: 2, displayUnit: 'ud', scalesLinearly: true })
    expect(d.ingredients[1]).toMatchObject({ quantity: 5, unit: 'ml', displayUnit: 'tsp', scalesLinearly: false })
    expect(d.nutrition?.perServing.kcal).toBeCloseTo(30, 0) // 300 g × 40 kcal/100 g / 4
    expect(d.recipe.kcalPerServing).toBeCloseTo(30, 0)
    expect(d.steps[0]?.timerSeconds).toBe(1500)
    expect(d.tags.map((t) => t.slug).sort()).toEqual(['basico', 'guarnicion'])
  })
  it('respeta quantity/unit/foodId explícitos y no los reparsea', async () => {
    const d = await createRecipe(ctxA, { ...input, ingredients: [{ rawText: 'cebolla', foodId: onionId, quantity: 100, unit: 'g', displayQuantity: 100, displayUnit: 'g', scalesLinearly: true }] })
    expect(d.ingredients[0]).toMatchObject({ quantity: 100, unit: 'g', foodId: onionId })
  })
})

describe('getRecipe', () => {
  it('escala a N raciones sin cambiar kcal por ración y marca no lineales', async () => {
    const d = await createRecipe(ctxA, input)
    const g = await getRecipe(ctxA, d.recipe.id, { servings: 8 })
    expect(g?.scaled?.ratio).toBe(2)
    expect(g?.scaled?.ingredients[0]?.quantity).toBe(600)
    expect(g?.scaled?.ingredients[1]?.quantity).toBeCloseTo(5 * Math.pow(2, 0.65), 3)
    expect(g?.scaled?.nonLinearIds).toEqual([d.ingredients[1]?.id])
    expect(g?.nutrition?.perServing.kcal).toBeCloseTo(30, 0)
  })
  it('no devuelve recetas de otro hogar ni borradas', async () => {
    const d = await createRecipe(ctxA, input)
    expect(await getRecipe(ctxB, d.recipe.id)).toBeNull()
    await softDeleteRecipe(ctxA, d.recipe.id)
    expect(await getRecipe(ctxA, d.recipe.id)).toBeNull()
    await expect(softDeleteRecipe(ctxB, d.recipe.id)).rejects.toMatchObject({ code: 'not_found' })
  })
})

describe('updateRecipe', () => {
  it('reemplaza ingredientes, pasos y etiquetas y recalcula la nutrición', async () => {
    const d = await createRecipe(ctxA, input)
    const u = await updateRecipe(ctxA, d.recipe.id, { ...input, servingsBase: 2, ingredients: [{ rawText: '1 cebolla', scalesLinearly: true }], steps: [{ text: 'Sofríe' }], tags: [] })
    expect(u.ingredients).toHaveLength(1)
    expect(u.recipe.kcalPerServing).toBeCloseTo(30, 0) // 150 g × 0.4 / 2
    expect(u.tags).toEqual([])
    await expect(updateRecipe(ctxB, d.recipe.id, input)).rejects.toMatchObject({ code: 'not_found' })
  })
})
```

- [ ] **Step 2: Ejecutar** — `pnpm exec vitest run --project db lib/services/recipes.test.ts` — FAIL.

- [ ] **Step 3: `lib/services/recipe-mapper.ts`**

```ts
import type { Ingredient, IngredientWithFood, FoodNutrition, RecipeForScaling } from '@/lib/domain/types'
import type { RecipeIngredient } from '@/db/schema'

export function toIngredient(r: RecipeIngredient): Ingredient {
  return {
    id: r.id, foodId: r.foodId, rawText: r.rawText, quantity: r.quantity, unit: r.unit, displayQuantity: r.displayQuantity,
    displayUnit: r.displayUnit, preparation: r.preparation, groupLabel: r.groupLabel, stepIndex: r.stepIndex,
    scalesLinearly: r.scalesLinearly, sortOrder: r.sortOrder,
  }
}
export function toIngredientWithFood(r: RecipeIngredient, food: FoodNutrition | null): IngredientWithFood {
  return { ...toIngredient(r), food }
}
export function toRecipeForScaling(servingsBase: number, ingredients: Ingredient[]): RecipeForScaling {
  return { servingsBase, ingredients }
}
```

- [ ] **Step 4: `lib/services/recipes.ts`**

```ts
import { and, eq, inArray, isNull, sql } from 'drizzle-orm'
import * as schema from '@/db/schema'
import { detectTimers, isNonLinearByDefault, normalizeSearchName, parseIngredientLine, recipeNutrition, scaleRecipe, toBaseUnit } from '@/lib/domain'
import type { IngredientWithFood, Locale, Nutrition, ScaledRecipe } from '@/lib/domain/types'
import { emitHouseholdEvent } from '@/lib/events/bus'
import type { RecipeInput } from '@/lib/validation/recipes'
import { type Ctx, ServiceError } from './ctx'
import { getFoodsNutrition, resolveMany, type FoodWithNutrition } from './foods'
import { toIngredient, toIngredientWithFood } from './recipe-mapper'

export interface RecipeDetail {
  recipe: schema.Recipe
  ingredients: IngredientWithFood[]
  steps: schema.RecipeStep[]
  tags: { id: string; name: string; slug: string }[]
  nutrition: Nutrition | null
  scaled: ScaledRecipe | null
}

type IngredientInput = RecipeInput['ingredients'][number]
export interface PreparedIngredient {
  rawText: string; foodId: string | null; quantity: number | null; unit: 'g' | 'ml' | 'ud' | null
  displayQuantity: number | null; displayUnit: string | null; preparation: string | null; groupLabel: string | null
  stepIndex: number | null; scalesLinearly: boolean; sortOrder: number; needsReview: boolean
}

export function slugify(s: string): string {
  return normalizeSearchName(s).replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')
}

// Completa cada línea: parsea si faltan datos, resuelve el alimento si falta, convierte a base y decide scalesLinearly.
export async function prepareIngredients(ctx: Ctx, inputs: IngredientInput[], locale: Locale): Promise<PreparedIngredient[]> {
  const parsed = inputs.map((i) => parseIngredientLine(i.rawText, locale))
  const toResolve = inputs.map((i, k) => (i.foodId ? null : parsed[k]!.foodName))
  const resolved = await resolveMany(ctx, toResolve.map((n) => n ?? ''), locale)
  const foodIds = inputs.map((i, k) => i.foodId ?? (toResolve[k] ? resolved[k]?.foodId ?? null : null))
  const foods = await getFoodsNutrition(ctx, foodIds.filter((x): x is string => x !== null))
  return inputs.map((i, k): PreparedIngredient => {
    const p = parsed[k]!
    const food = foodIds[k] ? foods.get(foodIds[k]!) ?? null : null
    const displayQuantity = i.displayQuantity !== undefined ? i.displayQuantity : p.quantity
    const displayUnit = i.displayUnit !== undefined ? i.displayUnit : p.unit
    let quantity = i.quantity ?? null
    let unit = i.unit ?? null
    if (i.quantity === undefined && displayQuantity !== null && displayUnit !== null) {
      const base = toBaseUnit(displayQuantity, displayUnit, locale, food ?? undefined)
      quantity = base?.qty ?? null
      unit = base?.unit ?? null
    }
    const explicitLinear = 'scalesLinearly' in i && i.scalesLinearly !== undefined
    const scalesLinearly = explicitLinear ? i.scalesLinearly : !isNonLinearByDefault(food?.name ?? p.foodName, locale)
    return {
      rawText: i.rawText, foodId: foodIds[k] ?? null, quantity, unit, displayQuantity, displayUnit,
      preparation: i.preparation !== undefined ? i.preparation : p.preparation, groupLabel: i.groupLabel ?? null,
      stepIndex: i.stepIndex ?? null, scalesLinearly, sortOrder: k, needsReview: foodIds[k] === null || p.needsReview,
    }
  })
}

async function upsertTags(ctx: Ctx, names: string[]): Promise<string[]> {
  const ids: string[] = []
  for (const name of names) {
    const slug = slugify(name)
    if (!slug) continue
    const [existing] = await ctx.db.select().from(schema.tags)
      .where(and(eq(schema.tags.slug, slug), sql`(${schema.tags.householdId} = ${ctx.householdId} OR ${schema.tags.householdId} IS NULL)`))
      .orderBy(sql`(${schema.tags.householdId} IS NULL)`).limit(1)
    if (existing) { ids.push(existing.id); continue }
    const [created] = await ctx.db.insert(schema.tags).values({ householdId: ctx.householdId, name, slug }).returning()
    if (created) ids.push(created.id)
  }
  return ids
}

function nutritionColumns(n: Nutrition | null) {
  return {
    kcalPerServing: n?.perServing.kcal ?? null, proteinPerServing: n?.perServing.protein ?? null, carbsPerServing: n?.perServing.carbs ?? null,
    fatPerServing: n?.perServing.fat ?? null, fiberPerServing: n?.perServing.fiber ?? null, kcal100g: n?.per100g?.kcal ?? null,
    nutritionIsEstimated: n?.isEstimated ?? false,
  }
}

async function writeChildren(ctx: Ctx, tx: Ctx['db'], recipeId: string, input: RecipeInput, prepared: PreparedIngredient[]): Promise<void> {
  await tx.delete(schema.recipeIngredients).where(eq(schema.recipeIngredients.recipeId, recipeId))
  await tx.delete(schema.recipeSteps).where(eq(schema.recipeSteps.recipeId, recipeId))
  await tx.delete(schema.recipeTags).where(eq(schema.recipeTags.recipeId, recipeId))
  if (prepared.length) await tx.insert(schema.recipeIngredients).values(prepared.map(({ needsReview: _n, ...p }) => ({ ...p, recipeId })))
  if (input.steps.length) {
    await tx.insert(schema.recipeSteps).values(input.steps.map((s, index) => ({
      recipeId, index, text: s.text, imageUrl: s.imageUrl ?? null,
      timerSeconds: s.timerSeconds ?? detectTimers(s.text, ctx.locale)[0]?.seconds ?? null,
    })))
  }
  const tagIds = await upsertTags({ ...ctx, db: tx }, input.tags)
  if (tagIds.length) await tx.insert(schema.recipeTags).values(tagIds.map((tagId) => ({ recipeId, tagId })))
}

async function computeNutrition(ctx: Ctx, prepared: PreparedIngredient[], servings: number, yieldGrams: number | null): Promise<Nutrition | null> {
  const ids = prepared.map((p) => p.foodId).filter((x): x is string => x !== null)
  if (ids.length === 0) return null
  const foods = await getFoodsNutrition(ctx, ids)
  const withFood: IngredientWithFood[] = prepared.map((p, k) => ({
    id: String(k), foodId: p.foodId, rawText: p.rawText, quantity: p.quantity, unit: p.unit, displayQuantity: p.displayQuantity,
    displayUnit: p.displayUnit, preparation: p.preparation, groupLabel: p.groupLabel, stepIndex: p.stepIndex,
    scalesLinearly: p.scalesLinearly, sortOrder: p.sortOrder, food: p.foodId ? foods.get(p.foodId) ?? null : null,
  }))
  return recipeNutrition(withFood, servings, yieldGrams)
}

function baseColumns(input: RecipeInput) {
  return {
    title: input.title, description: input.description ?? null, servingsBase: input.servingsBase, prepMinutes: input.prepMinutes ?? null,
    cookMinutes: input.cookMinutes ?? null, difficulty: input.difficulty ?? null, sourceUrl: input.sourceUrl ?? null, imageUrls: input.imageUrls,
    notes: input.notes ?? null, yieldGrams: input.yieldGrams ?? null,
  }
}

export async function createRecipe(ctx: Ctx, input: RecipeInput): Promise<RecipeDetail> {
  const prepared = await prepareIngredients(ctx, input.ingredients, ctx.locale)
  const nutrition = await computeNutrition(ctx, prepared, input.servingsBase, input.yieldGrams ?? null)
  const id = await ctx.db.transaction(async (tx) => {
    const [r] = await tx.insert(schema.recipes).values({ householdId: ctx.householdId, ...baseColumns(input), ...nutritionColumns(nutrition) }).returning({ id: schema.recipes.id })
    if (!r) throw new ServiceError('conflict', 'No se pudo crear la receta')
    await writeChildren(ctx, tx, r.id, input, prepared)
    return r.id
  })
  emitHouseholdEvent(ctx.householdId, { type: 'recipe.changed', payload: { recipeId: id } })
  const d = await getRecipe(ctx, id)
  if (!d) throw new ServiceError('not_found', 'Receta no encontrada')
  return d
}

export async function updateRecipe(ctx: Ctx, id: string, input: RecipeInput): Promise<RecipeDetail> {
  const prepared = await prepareIngredients(ctx, input.ingredients, ctx.locale)
  const nutrition = await computeNutrition(ctx, prepared, input.servingsBase, input.yieldGrams ?? null)
  await ctx.db.transaction(async (tx) => {
    const [r] = await tx.update(schema.recipes).set({ ...baseColumns(input), ...nutritionColumns(nutrition), updatedAt: new Date() })
      .where(and(eq(schema.recipes.id, id), eq(schema.recipes.householdId, ctx.householdId), isNull(schema.recipes.deletedAt))).returning({ id: schema.recipes.id })
    if (!r) throw new ServiceError('not_found', 'Receta no encontrada')
    await writeChildren(ctx, tx, id, input, prepared)
  })
  emitHouseholdEvent(ctx.householdId, { type: 'recipe.changed', payload: { recipeId: id } })
  const d = await getRecipe(ctx, id)
  if (!d) throw new ServiceError('not_found', 'Receta no encontrada')
  return d
}

export async function getRecipe(ctx: Ctx, id: string, opts: { servings?: number } = {}): Promise<RecipeDetail | null> {
  const [recipe] = await ctx.db.select().from(schema.recipes)
    .where(and(eq(schema.recipes.id, id), eq(schema.recipes.householdId, ctx.householdId), isNull(schema.recipes.deletedAt))).limit(1)
  if (!recipe) return null
  const rows = await ctx.db.select().from(schema.recipeIngredients).where(eq(schema.recipeIngredients.recipeId, id)).orderBy(schema.recipeIngredients.sortOrder)
  const foods = await getFoodsNutrition(ctx, rows.map((r) => r.foodId).filter((x): x is string => x !== null))
  const ingredients = rows.map((r) => toIngredientWithFood(r, r.foodId ? foods.get(r.foodId) ?? null : null))
  const steps = await ctx.db.select().from(schema.recipeSteps).where(eq(schema.recipeSteps.recipeId, id)).orderBy(schema.recipeSteps.index)
  const tags = await ctx.db.select({ id: schema.tags.id, name: schema.tags.name, slug: schema.tags.slug }).from(schema.recipeTags)
    .innerJoin(schema.tags, eq(schema.tags.id, schema.recipeTags.tagId)).where(eq(schema.recipeTags.recipeId, id))
  const nutrition = ingredients.some((i) => i.food) ? recipeNutrition(ingredients, recipe.servingsBase, recipe.yieldGrams) : null
  const scaled = opts.servings && opts.servings !== recipe.servingsBase ? scaleRecipe({ servingsBase: recipe.servingsBase, ingredients: rows.map(toIngredient) }, opts.servings) : null
  return { recipe, ingredients, steps, tags, nutrition, scaled }
}

export async function softDeleteRecipe(ctx: Ctx, id: string): Promise<void> {
  const [r] = await ctx.db.update(schema.recipes).set({ deletedAt: new Date() })
    .where(and(eq(schema.recipes.id, id), eq(schema.recipes.householdId, ctx.householdId), isNull(schema.recipes.deletedAt))).returning({ id: schema.recipes.id })
  if (!r) throw new ServiceError('not_found', 'Receta no encontrada')
  emitHouseholdEvent(ctx.householdId, { type: 'recipe.changed', payload: { recipeId: id } })
}

export type { FoodWithNutrition }
```

Nota sobre `'2 cebollas grandes'`: el parser devuelve `unit: null`, `foodName: 'cebollas grandes'` (o `cebolla` según fixtures). Si `resolveFoodName` no encuentra "cebollas grandes" por trigram ≥ 0.6 el test fallará: en ese caso el servicio hace un **segundo intento** con la primera palabra del nombre (`p.foodName.split(' ')[0]`) antes de rendirse — añadir ese fallback en `prepareIngredients` (documentado en comentario) y en `toBaseUnit` de piezas: cuando `displayUnit` es null y `displayQuantity` no, tratar la línea como piezas (`unit 'ud'`, `displayUnit 'ud'`) y convertir con `gramsPerUnit` si existe (`toBaseUnit(q, 'ud', locale, food)`).

- [ ] **Step 5: Ejecutar** — `pnpm exec vitest run --project db lib/services/recipes.test.ts` — 6 passed. Ajustar valores esperados solo si el parser real difiere en `foodName` (documentar).

- [ ] **Step 6: Commit**

```bash
git add lib/services/recipes.ts lib/services/recipe-mapper.ts lib/services/recipes.test.ts
git commit -m "Añade servicio de recetas con escalado y nutrición"
```

---

### Task 7: Búsqueda full-text con filtros, exportación e importación desde URL/texto

**Files:**
- Create: `lib/services/recipe-import.ts`, `lib/services/recipe-import.test.ts`, `lib/services/__fixtures__/recipe-jsonld.html`, `lib/services/__fixtures__/recipe-microdata.html`
- Modify: `lib/services/recipes.ts`, `lib/services/recipes.test.ts`

**Interfaces:**
- Produces: `searchRecipes(ctx, input: RecipeSearch): Promise<{ items: RecipeSummary[]; total: number }>` con `RecipeSummary = { id, title, imageUrl: string|null, totalMinutes: number|null, difficulty, kcalPerServing, servingsBase, timesCooked, tags: string[], nutritionIsEstimated }`; `exportAll(ctx): Promise<RecipeExport>` (JSON: recetas con ingredientes/pasos/etiquetas, sin ids de hogar); `importRecipeFromUrl(url, fetchImpl?): Promise<RecipeDraft>`; `importRecipeFromText(text, locale): Promise<RecipeDraft>`; `RecipeDraft = RecipeInput & { warnings: string[] }` (no persiste: la UI abre el editor con el borrador).

- [ ] **Step 1: Tests que fallan**

```ts
// añadir a lib/services/recipes.test.ts
import { exportAll, searchRecipes } from './recipes'

describe('searchRecipes', () => {
  it('full-text con websearch, filtros y orden; solo del hogar', async () => {
    await createRecipe(ctxA, input)
    await createRecipe(ctxA, { ...input, title: 'Tortilla de patatas', tags: ['clásico'], prepMinutes: 10, cookMinutes: 20, difficulty: 'easy' })
    await createRecipe(ctxB, { ...input, title: 'Cebolla ajena' })
    expect((await searchRecipes(ctxA, { q: 'cebolla', limit: 20, offset: 0, sort: 'relevance' })).items.map((r) => r.title)).toEqual(['Cebolla caramelizada'])
    expect((await searchRecipes(ctxA, { tags: ['clasico'], limit: 20, offset: 0, sort: 'title' })).items.map((r) => r.title)).toEqual(['Tortilla de patatas'])
    expect((await searchRecipes(ctxA, { maxMinutes: 25, limit: 20, offset: 0, sort: 'title' })).items).toHaveLength(0)
    expect((await searchRecipes(ctxA, { maxMinutes: 30, difficulty: 'easy', limit: 20, offset: 0, sort: 'title' })).items).toHaveLength(1)
    expect((await searchRecipes(ctxA, { hasIngredients: [saltId], limit: 20, offset: 0, sort: 'title' })).total).toBe(2)
  })
  it('onlyWithPantry: recetas cuyos alimentos con food_id están todos en la despensa', async () => {
    await createRecipe(ctxA, input)
    expect((await searchRecipes(ctxA, { onlyWithPantry: true, limit: 20, offset: 0, sort: 'title' })).items).toHaveLength(0)
    await db.insert(schema.pantryItems).values([{ householdId: ctxA.householdId, foodId: onionId, quantity: 500, unit: 'g' }, { householdId: ctxA.householdId, foodId: saltId, quantity: 100, unit: 'g' }])
    expect((await searchRecipes(ctxA, { onlyWithPantry: true, limit: 20, offset: 0, sort: 'title' })).items).toHaveLength(1)
  })
})

describe('exportAll', () => {
  it('exporta recetas con ingredientes, pasos y etiquetas, sin ids internos', async () => {
    await createRecipe(ctxA, input)
    const e = await exportAll(ctxA)
    expect(e.version).toBe(1)
    expect(e.recipes[0]).toMatchObject({ title: 'Cebolla caramelizada', servingsBase: 4, tags: expect.arrayContaining(['guarnición']) })
    expect(JSON.stringify(e)).not.toContain(ctxA.householdId)
  })
})
```

```ts
// lib/services/recipe-import.test.ts (proyecto unit: sin DB)
import { readFileSync } from 'node:fs'
import { describe, expect, it } from 'vitest'
import { importRecipeFromText, importRecipeFromUrl } from './recipe-import'

const html = (f: string) => readFileSync(new URL(`./__fixtures__/${f}`, import.meta.url), 'utf8')
const fetchHtml = (body: string): typeof fetch => (async () => new Response(body, { status: 200, headers: { 'content-type': 'text/html' } })) as unknown as typeof fetch

describe('importRecipeFromUrl', () => {
  it('lee schema.org/Recipe en JSON-LD (incl. @graph) y normaliza ingredientes, pasos, raciones, tiempos e imagen', async () => {
    const d = await importRecipeFromUrl('https://ejemplo.test/lentejas', fetchHtml(html('recipe-jsonld.html')))
    expect(d.title).toBe('Lentejas con chorizo')
    expect(d.servingsBase).toBe(4)
    expect(d.prepMinutes).toBe(15)
    expect(d.cookMinutes).toBe(45)
    expect(d.ingredients.map((i) => i.rawText)).toEqual(['400 g de lentejas pardinas', '1 chorizo', 'sal al gusto'])
    expect(d.steps.map((s) => s.text)).toEqual(['Lava las lentejas.', 'Cuece 45 minutos.'])
    expect(d.imageUrls).toEqual(['https://ejemplo.test/lentejas.jpg'])
    expect(d.sourceUrl).toBe('https://ejemplo.test/lentejas')
    expect(d.warnings).toEqual([])
  })
  it('cae a microdata itemprop cuando no hay JSON-LD', async () => {
    const d = await importRecipeFromUrl('https://ejemplo.test/x', fetchHtml(html('recipe-microdata.html')))
    expect(d.title).toBe('Gazpacho')
    expect(d.ingredients.length).toBe(4)
  })
  it('sin receta reconocible devuelve borrador vacío con aviso', async () => {
    const d = await importRecipeFromUrl('https://ejemplo.test/nada', fetchHtml('<html><body>hola</body></html>'))
    expect(d.title).toBe('')
    expect(d.warnings).toContain('no_recipe_found')
  })
})

describe('importRecipeFromText', () => {
  it('separa título, ingredientes (líneas con cantidad) y pasos (numerados o tras "Preparación")', () => {
    const d = importRecipeFromText(`Tortilla de patatas\n\nIngredientes\n4 huevos\n500 g de patatas\nsal\n\nPreparación\n1. Pela las patatas.\n2. Bate los huevos.`, 'es')
    expect(d.title).toBe('Tortilla de patatas')
    expect(d.ingredients.map((i) => i.rawText)).toEqual(['4 huevos', '500 g de patatas', 'sal'])
    expect(d.steps.map((s) => s.text)).toEqual(['Pela las patatas.', 'Bate los huevos.'])
  })
})
```

Fixtures: `recipe-jsonld.html` con `<script type="application/ld+json">{"@context":"https://schema.org","@graph":[{"@type":"WebPage"},{"@type":"Recipe","name":"Lentejas con chorizo","recipeYield":"4 raciones","prepTime":"PT15M","cookTime":"PT45M","image":["https://ejemplo.test/lentejas.jpg"],"recipeIngredient":["400 g de lentejas pardinas","1 chorizo","sal al gusto"],"recipeInstructions":[{"@type":"HowToStep","text":"Lava las lentejas."},{"@type":"HowToStep","text":"Cuece 45 minutos."}]}]}</script>`; `recipe-microdata.html` con `<div itemscope itemtype="https://schema.org/Recipe"><h1 itemprop="name">Gazpacho</h1><li itemprop="recipeIngredient">1 kg de tomates</li>…(4)…<div itemprop="recipeInstructions">Tritura todo.</div></div>`.

- [ ] **Step 2: Ejecutar** — ambos ficheros fallan.

- [ ] **Step 3: `searchRecipes` y `exportAll`** (añadir a `recipes.ts`)

```ts
import type { RecipeSearch } from '@/lib/validation/recipes'   // añadir `export type RecipeSearch = z.infer<typeof RecipeSearchSchema>` en lib/validation/recipes.ts

export interface RecipeSummary {
  id: string; title: string; imageUrl: string | null; totalMinutes: number | null; difficulty: 'easy' | 'medium' | 'hard' | null
  kcalPerServing: number | null; servingsBase: number; timesCooked: number; tags: string[]; nutritionIsEstimated: boolean
}

export async function searchRecipes(ctx: Ctx, input: RecipeSearch): Promise<{ items: RecipeSummary[]; total: number }> {
  const r = schema.recipes
  const conds = [eq(r.householdId, ctx.householdId), isNull(r.deletedAt)]
  const q = input.q?.trim()
  if (q) conds.push(sql`${r.searchVector} @@ (websearch_to_tsquery('spanish', ${q}) || websearch_to_tsquery('english', ${q}))`)
  if (input.maxMinutes !== undefined) conds.push(sql`coalesce(${r.prepMinutes}, 0) + coalesce(${r.cookMinutes}, 0) <= ${input.maxMinutes}`)
  if (input.difficulty) conds.push(eq(r.difficulty, input.difficulty))
  if (input.tags?.length) {
    conds.push(sql`EXISTS (SELECT 1 FROM ${schema.recipeTags} rt JOIN ${schema.tags} t ON t.id = rt.tag_id WHERE rt.recipe_id = ${r.id} AND t.slug IN (${sql.join(input.tags.map((t) => sql`${slugify(t)}`), sql`, `)}))`)
  }
  for (const foodId of input.hasIngredients ?? []) {
    conds.push(sql`EXISTS (SELECT 1 FROM ${schema.recipeIngredients} ri WHERE ri.recipe_id = ${r.id} AND ri.food_id = ${foodId})`)
  }
  if (input.onlyWithPantry) {
    conds.push(sql`NOT EXISTS (SELECT 1 FROM ${schema.recipeIngredients} ri WHERE ri.recipe_id = ${r.id} AND ri.food_id IS NOT NULL
      AND NOT EXISTS (SELECT 1 FROM ${schema.pantryItems} p WHERE p.household_id = ${ctx.householdId} AND p.food_id = ri.food_id AND p.quantity > 0))`)
  }
  const where = and(...conds)
  const order =
    input.sort === 'title' ? sql`${r.title} asc` :
    input.sort === 'recent' ? sql`${r.updatedAt} desc` :
    input.sort === 'most_cooked' ? sql`${r.timesCooked} desc, ${r.title} asc` :
    q ? sql`ts_rank(${r.searchVector}, websearch_to_tsquery('spanish', ${q})) desc, ${r.title} asc` : sql`${r.updatedAt} desc`
  const [{ total }] = await ctx.db.select({ total: sql<number>`count(*)::int` }).from(r).where(where)
  const rows = await ctx.db.select().from(r).where(where).orderBy(order).limit(input.limit).offset(input.offset)
  const ids = rows.map((x) => x.id)
  const tagRows = ids.length ? await ctx.db.select({ recipeId: schema.recipeTags.recipeId, name: schema.tags.name }).from(schema.recipeTags)
    .innerJoin(schema.tags, eq(schema.tags.id, schema.recipeTags.tagId)).where(inArray(schema.recipeTags.recipeId, ids)) : []
  const items = rows.map((x) => ({
    id: x.id, title: x.title, imageUrl: x.imageUrls[0] ?? null,
    totalMinutes: x.prepMinutes === null && x.cookMinutes === null ? null : (x.prepMinutes ?? 0) + (x.cookMinutes ?? 0),
    difficulty: x.difficulty, kcalPerServing: x.kcalPerServing, servingsBase: x.servingsBase, timesCooked: x.timesCooked,
    tags: tagRows.filter((t) => t.recipeId === x.id).map((t) => t.name), nutritionIsEstimated: x.nutritionIsEstimated,
  }))
  return { items, total: total ?? 0 }
}

export interface RecipeExport { version: 1; exportedAt: string; recipes: (RecipeInput & { timesCooked: number; createdAt: string })[] }

export async function exportAll(ctx: Ctx): Promise<RecipeExport> {
  const rows = await ctx.db.select().from(schema.recipes).where(and(eq(schema.recipes.householdId, ctx.householdId), isNull(schema.recipes.deletedAt))).orderBy(schema.recipes.createdAt)
  const out: RecipeExport['recipes'] = []
  for (const r of rows) {
    const d = await getRecipe(ctx, r.id)
    if (!d) continue
    out.push({
      title: r.title, description: r.description, servingsBase: r.servingsBase, prepMinutes: r.prepMinutes, cookMinutes: r.cookMinutes,
      difficulty: r.difficulty, sourceUrl: r.sourceUrl, imageUrls: r.imageUrls, notes: r.notes, yieldGrams: r.yieldGrams,
      tags: d.tags.map((t) => t.name),
      ingredients: d.ingredients.map((i) => ({ rawText: i.rawText, foodId: null, quantity: i.quantity, unit: i.unit, displayQuantity: i.displayQuantity, displayUnit: i.displayUnit, preparation: i.preparation, groupLabel: i.groupLabel, stepIndex: i.stepIndex, scalesLinearly: i.scalesLinearly })),
      steps: d.steps.map((s) => ({ text: s.text, timerSeconds: s.timerSeconds, imageUrl: s.imageUrl })),
      timesCooked: r.timesCooked, createdAt: r.createdAt.toISOString(),
    })
  }
  return { version: 1, exportedAt: new Date().toISOString(), recipes: out }
}
```

- [ ] **Step 4: `lib/services/recipe-import.ts`**

```ts
import * as cheerio from 'cheerio'
import type { Locale } from '@/lib/domain/types'
import type { RecipeInput } from '@/lib/validation/recipes'

export type RecipeDraft = RecipeInput & { warnings: string[] }

const USER_AGENT = 'Mozilla/5.0 (compatible; RezetApp/0.1; +https://github.com/)'

function emptyDraft(): RecipeDraft {
  return { title: '', servingsBase: 2, imageUrls: [], tags: [], ingredients: [], steps: [], warnings: [] }
}

// PT1H30M → 90
export function isoDurationToMinutes(s: unknown): number | null {
  if (typeof s !== 'string') return null
  const m = /^P(?:(\d+)D)?T?(?:(\d+)H)?(?:(\d+)M)?/i.exec(s.trim())
  if (!m) return null
  const [, d, h, min] = m
  const total = Number(d ?? 0) * 1440 + Number(h ?? 0) * 60 + Number(min ?? 0)
  return total > 0 ? total : null
}

export function yieldToServings(y: unknown): number | null {
  const s = Array.isArray(y) ? y[0] : y
  const n = typeof s === 'number' ? s : typeof s === 'string' ? Number(/\d+/.exec(s)?.[0]) : NaN
  return Number.isFinite(n) && n > 0 ? Math.min(100, Math.round(n)) : null
}

type Json = Record<string, unknown>

function findRecipeNode(node: unknown): Json | null {
  if (Array.isArray(node)) { for (const n of node) { const r = findRecipeNode(n); if (r) return r } return null }
  if (typeof node !== 'object' || node === null) return null
  const o = node as Json
  const type = o['@type']
  if (type === 'Recipe' || (Array.isArray(type) && type.includes('Recipe'))) return o
  if (o['@graph']) return findRecipeNode(o['@graph'])
  return null
}

function text(v: unknown): string {
  if (typeof v === 'string') return v.replace(/\s+/g, ' ').trim()
  if (typeof v === 'object' && v !== null && 'text' in v) return text((v as Json).text)
  return ''
}

function instructions(v: unknown): string[] {
  if (typeof v === 'string') return v.split(/\n+|(?<=\.)\s+(?=[A-ZÁÉÍÓÚ])/).map((s) => s.trim()).filter(Boolean)
  if (!Array.isArray(v)) return []
  return v.flatMap((x) => {
    if (typeof x === 'object' && x !== null && (x as Json)['@type'] === 'HowToSection') return instructions((x as Json).itemListElement)
    const t = text(x)
    return t ? [t] : []
  })
}

function images(v: unknown): string[] {
  const arr = Array.isArray(v) ? v : [v]
  return arr.map((x) => (typeof x === 'string' ? x : typeof x === 'object' && x !== null ? text((x as Json).url) : '')).filter((s) => /^https?:\/\//.test(s)).slice(0, 10)
}

export function draftFromJsonLd(node: Json, url: string): RecipeDraft {
  const d = emptyDraft()
  d.title = text(node.name)
  d.description = text(node.description) || null
  d.servingsBase = yieldToServings(node.recipeYield) ?? 2
  d.prepMinutes = isoDurationToMinutes(node.prepTime)
  d.cookMinutes = isoDurationToMinutes(node.cookTime) ?? isoDurationToMinutes(node.totalTime)
  d.imageUrls = images(node.image)
  d.sourceUrl = url
  d.ingredients = (Array.isArray(node.recipeIngredient) ? node.recipeIngredient : []).map((s) => ({ rawText: text(s).slice(0, 200), scalesLinearly: true })).filter((i) => i.rawText)
  d.steps = instructions(node.recipeInstructions).map((t) => ({ text: t.slice(0, 2000) }))
  const kw = typeof node.keywords === 'string' ? node.keywords.split(',') : Array.isArray(node.keywords) ? node.keywords : []
  d.tags = kw.map((k) => text(k)).filter(Boolean).slice(0, 20)
  if (!d.ingredients.length) d.warnings.push('no_ingredients')
  if (!d.steps.length) d.warnings.push('no_steps')
  return d
}

export function draftFromMicrodata($: cheerio.CheerioAPI, url: string): RecipeDraft | null {
  const root = $('[itemtype*="schema.org/Recipe"]').first()
  if (!root.length) return null
  const d = emptyDraft()
  const prop = (p: string) => root.find(`[itemprop="${p}"]`)
  d.title = prop('name').first().text().trim()
  d.description = prop('description').first().text().trim() || null
  d.servingsBase = yieldToServings(prop('recipeYield').first().text()) ?? 2
  d.prepMinutes = isoDurationToMinutes(prop('prepTime').attr('datetime') ?? prop('prepTime').attr('content'))
  d.cookMinutes = isoDurationToMinutes(prop('cookTime').attr('datetime') ?? prop('cookTime').attr('content'))
  d.imageUrls = images(prop('image').map((_, el) => $(el).attr('src') ?? $(el).attr('content') ?? '').get())
  d.sourceUrl = url
  d.ingredients = prop('recipeIngredient').map((_, el) => ({ rawText: $(el).text().replace(/\s+/g, ' ').trim().slice(0, 200), scalesLinearly: true })).get().filter((i) => i.rawText)
  d.steps = prop('recipeInstructions').map((_, el) => $(el).text().replace(/\s+/g, ' ').trim()).get().flatMap((t) => instructions(t)).map((t) => ({ text: t.slice(0, 2000) }))
  if (!d.ingredients.length) d.warnings.push('no_ingredients')
  if (!d.steps.length) d.warnings.push('no_steps')
  return d
}

export async function importRecipeFromUrl(url: string, fetchImpl: typeof fetch = fetch): Promise<RecipeDraft> {
  const res = await fetchImpl(url, { headers: { 'user-agent': USER_AGENT, accept: 'text/html' }, redirect: 'follow' })
  if (!res.ok) return { ...emptyDraft(), sourceUrl: url, warnings: ['fetch_failed'] }
  const $ = cheerio.load(await res.text())
  for (const el of $('script[type="application/ld+json"]').toArray()) {
    try {
      const node = findRecipeNode(JSON.parse($(el).text()))
      if (node) return draftFromJsonLd(node, url)
    } catch {
      // JSON-LD roto: probar el siguiente bloque
    }
  }
  const micro = draftFromMicrodata($, url)
  if (micro) return micro
  return { ...emptyDraft(), title: '', sourceUrl: url, warnings: ['no_recipe_found'] }
}

const INGREDIENT_HEADERS: Record<Locale, RegExp> = { es: /^ingredientes?\b/i, en: /^ingredients?\b/i }
const STEP_HEADERS: Record<Locale, RegExp> = { es: /^(preparaci[oó]n|elaboraci[oó]n|pasos)\b/i, en: /^(preparation|instructions|method|steps|directions)\b/i }
const QTY_LINE = /^(\d+([.,]\d+)?|[¼½¾⅓⅔]|un|una|a|an)\b/i

// Heurística: título = primera línea no vacía; ingredientes = bloque tras "Ingredientes" o líneas que empiezan por cantidad;
// pasos = bloque tras "Preparación" o líneas numeradas.
export function importRecipeFromText(raw: string, locale: Locale): RecipeDraft {
  const lines = raw.split(/\r?\n/).map((l) => l.trim()).filter(Boolean)
  const d = emptyDraft()
  if (!lines.length) return { ...d, warnings: ['empty'] }
  d.title = lines[0]!.slice(0, 160)
  let mode: 'none' | 'ing' | 'steps' = 'none'
  for (const line of lines.slice(1)) {
    if (INGREDIENT_HEADERS[locale].test(line)) { mode = 'ing'; continue }
    if (STEP_HEADERS[locale].test(line)) { mode = 'steps'; continue }
    const numbered = /^\d+[.)]\s*/.test(line)
    if (mode === 'steps' || (mode === 'none' && numbered)) { d.steps.push({ text: line.replace(/^\d+[.)]\s*/, '').slice(0, 2000) }); mode = mode === 'none' ? 'steps' : mode; continue }
    if (mode === 'ing' || QTY_LINE.test(line)) { d.ingredients.push({ rawText: line.replace(/^[-•*]\s*/, '').slice(0, 200), scalesLinearly: true }); continue }
    if (mode === 'none' && !d.description) d.description = line.slice(0, 2000)
  }
  if (!d.ingredients.length) d.warnings.push('no_ingredients')
  if (!d.steps.length) d.warnings.push('no_steps')
  return d
}
```

- [ ] **Step 5: Ejecutar** — `pnpm exec vitest run lib/services/recipe-import.test.ts && pnpm exec vitest run --project db lib/services/recipes.test.ts` — verde. Si `'sal'` no pasa `QTY_LINE` en el test de texto, el bloque `mode === 'ing'` lo captura porque viene tras la cabecera "Ingredientes" (es el caso del test).

- [ ] **Step 6: Commit**

```bash
git add lib/services/recipes.ts lib/services/recipes.test.ts lib/services/recipe-import.ts lib/services/recipe-import.test.ts lib/services/__fixtures__ lib/validation/recipes.ts
git commit -m "Añade búsqueda, exportación e importación de recetas"
```

---

### Task 8: Subida de imágenes (`sharp` → webp) y servicio de ficheros

**Files:**
- Create: `lib/uploads/store.ts`, `lib/uploads/store.test.ts`, `app/api/v1/uploads/route.ts`, `app/api/uploads/[...path]/route.ts`
- Modify: `eslint.config.mjs` (elemento `uploads`), `.env.example` (`UPLOADS_DIR`)

**Interfaces:**
- Produces: `saveImage(householdId, bytes: Uint8Array): Promise<{ path: string; url: string }>` (guarda `<UPLOADS_DIR>/<householdId>/<uuid>.webp`, url `/api/uploads/<householdId>/<uuid>.webp`), `readImage(householdId, file): Promise<Buffer | null>`, `MAX_UPLOAD_BYTES = 8 MB`, `UPLOADS_DIR`.

- [ ] **Step 1: Boundaries** — en `eslint.config.mjs` añadir el elemento `{ type: 'uploads', pattern: 'lib/uploads/**' }` (antes de los `lib/*`) y la política `{ from: { element: { type: 'uploads' } }, allow: { to: { element: { types: { anyOf: ['uploads', 'lib'] } } } } }`; permitir `uploads` desde `services`, `actions` y `app` (añadir `'uploads'` a sus `anyOf`). Añadir un caso al test `lib/eslint-boundaries.test.ts`: `lib/uploads/x.ts` importando `@/db` → error.

- [ ] **Step 2: Test que falla** (unit; usa un directorio temporal)

```ts
// lib/uploads/store.test.ts
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import sharp from 'sharp'
import { afterAll, beforeAll, describe, expect, it } from 'vitest'
import { readImage, saveImage } from './store'

let dir: string
beforeAll(() => { dir = mkdtempSync(join(tmpdir(), 'rz-up-')); process.env.UPLOADS_DIR = dir })
afterAll(() => rmSync(dir, { recursive: true, force: true }))

describe('uploads', () => {
  it('convierte a webp ≤ 1600 px y sirve solo dentro del hogar', async () => {
    const png = await sharp({ create: { width: 2400, height: 1200, channels: 3, background: '#2F9E6B' } }).png().toBuffer()
    const saved = await saveImage('h1', new Uint8Array(png))
    expect(saved.url).toMatch(/^\/api\/uploads\/h1\/[0-9a-f-]{36}\.webp$/)
    const buf = await readImage('h1', saved.path.split('/').pop()!)
    const meta = await sharp(buf!).metadata()
    expect(meta.format).toBe('webp')
    expect(meta.width).toBe(1600)
    expect(await readImage('h2', saved.path.split('/').pop()!)).toBeNull()
    expect(await readImage('h1', '../../etc/passwd')).toBeNull()
  })
  it('rechaza bytes que no son imagen', async () => {
    await expect(saveImage('h1', new TextEncoder().encode('hola'))).rejects.toThrow()
  })
})
```

- [ ] **Step 3: Implementar**

```ts
// lib/uploads/store.ts
import { randomUUID } from 'node:crypto'
import { mkdir, readFile, writeFile } from 'node:fs/promises'
import { join, resolve } from 'node:path'
import sharp from 'sharp'

export const MAX_UPLOAD_BYTES = 8 * 1024 * 1024
const MAX_SIDE = 1600
const FILE_RE = /^[0-9a-f-]{36}\.webp$/

export function uploadsDir(): string {
  return resolve(process.env.UPLOADS_DIR ?? './data/uploads')
}

// Guarda la imagen como webp (máx. 1600 px de lado) bajo el hogar. Lanza si los bytes no son una imagen.
export async function saveImage(householdId: string, bytes: Uint8Array): Promise<{ path: string; url: string }> {
  if (bytes.byteLength > MAX_UPLOAD_BYTES) throw new Error('Imagen demasiado grande')
  const webp = await sharp(bytes).rotate().resize({ width: MAX_SIDE, height: MAX_SIDE, fit: 'inside', withoutEnlargement: true }).webp({ quality: 82 }).toBuffer()
  const name = `${randomUUID()}.webp`
  const dir = join(uploadsDir(), householdId)
  await mkdir(dir, { recursive: true })
  const path = join(dir, name)
  await writeFile(path, webp)
  return { path, url: `/api/uploads/${householdId}/${name}` }
}

export async function readImage(householdId: string, file: string): Promise<Buffer | null> {
  if (!FILE_RE.test(file) || !/^[0-9a-f-]{36}$/.test(householdId)) return null
  try {
    return await readFile(join(uploadsDir(), householdId, file))
  } catch {
    return null
  }
}
```

`readImage` con `'h1'` en el test: relajar la comprobación del `householdId` a `/^[A-Za-z0-9-]{1,64}$/` (los ids reales son uuid; el test usa `h1`).

```ts
// app/api/v1/uploads/route.ts
import { apiErrorResponse, requireApiToken } from '@/lib/auth/guards'
import { MAX_UPLOAD_BYTES, saveImage } from '@/lib/uploads/store'

export async function POST(request: Request): Promise<Response> {
  try {
    const ctx = await requireApiToken(request, ['recipes:write'])
    const form = await request.formData().catch(() => null)
    const file = form?.get('file')
    if (!(file instanceof File)) return Response.json({ error: { code: 'validation', message: 'Falta el fichero' } }, { status: 400 })
    if (file.size > MAX_UPLOAD_BYTES) return Response.json({ error: { code: 'validation', message: 'Máximo 8 MB' } }, { status: 413 })
    try {
      const saved = await saveImage(ctx.householdId, new Uint8Array(await file.arrayBuffer()))
      return Response.json({ url: saved.url }, { status: 201 })
    } catch {
      return Response.json({ error: { code: 'validation', message: 'No es una imagen válida' } }, { status: 400 })
    }
  } catch (e) {
    return apiErrorResponse(e)
  }
}
```

```ts
// app/api/uploads/[...path]/route.ts
import { getCurrentSession } from '@/lib/auth/guards'
import { readImage } from '@/lib/uploads/store'

export const dynamic = 'force-dynamic'

export async function GET(_req: Request, ctx: { params: Promise<{ path: string[] }> }): Promise<Response> {
  const session = await getCurrentSession()
  if (!session) return new Response('Unauthorized', { status: 401 })
  const [householdId, file] = (await ctx.params).path
  if (!householdId || !file || householdId !== session.household.id) return new Response('Not found', { status: 404 })
  const buf = await readImage(householdId, file)
  if (!buf) return new Response('Not found', { status: 404 })
  return new Response(buf, { headers: { 'content-type': 'image/webp', 'cache-control': 'private, max-age=31536000, immutable' } })
}
```

`.env.example`: añadir `UPLOADS_DIR=./data/uploads` con comentario. `next.config.ts`: `serverExternalPackages: ['sharp']` (evita que Turbopack empaquete el binario).

- [ ] **Step 4: Ejecutar** — `pnpm exec vitest run lib/uploads lib/eslint-boundaries.test.ts && pnpm check` — verde.

- [ ] **Step 5: Commit**

```bash
git add lib/uploads app/api/v1/uploads app/api/uploads eslint.config.mjs lib/eslint-boundaries.test.ts .env.example next.config.ts package.json pnpm-lock.yaml
git commit -m "Añade subida de imágenes convertidas a webp"
```

---

### Task 9: Acciones de recetas y lista con búsqueda/filtros

**Files:**
- Create: `lib/actions/recipes.ts`, `components/recipes/recipe-card.tsx`, `components/recipes/recipe-filters.tsx`, `components/recipes/recipe-filters.test.tsx`
- Modify: `app/(app)/recipes/page.tsx`, `messages/*/recipes.json`, `components/icons/index.ts` (+ `ScaleIcon`, `UploadIcon`, `BookmarkIcon`)

**Interfaces:**
- Produces: acciones `createRecipeAction(input)`, `updateRecipeAction(id, input)`, `deleteRecipeAction(id)`, `importRecipeAction(input: RecipeImport)` → `RecipeDraft`, `prepareIngredientsAction(inputs)` → `PreparedIngredient[]` (para el editor), `exportRecipesAction()` → `RecipeExport`. Página `/recipes?q&tags&maxMinutes&difficulty&onlyWithPantry&sort&page` (Server Component; lee `searchParams`, valida con `RecipeSearchSchema`, pinta `RecipeCard`s y `RecipeFilters`).

- [ ] **Step 1: Iconos** — `components/icons/scale.tsx` (`ScaleIcon`: `<path d="M4 7h16M12 7v11M6 18h12M7 7l-3 6h6l-3-6ZM17 7l-3 6h6l-3-6Z"/>`), `upload.tsx` (`UploadIcon`: `<path d="M12 16V4M7 9l5-5 5 5M4 20h16"/>`), `bookmark.tsx` (`BookmarkIcon`: `<path d="M6 4h12v16l-6-4-6 4V4Z"/>`); exportar en `index.ts`.

- [ ] **Step 2: Test de filtros (ui)** — `RecipeFilters` es cliente: un `<form method="get">` con buscador, chips de dificultad, campo `maxMinutes`, checkbox `onlyWithPantry`, select `sort`; al enviar navega a `/recipes?…` (usa `useRouter().push` con `URLSearchParams`). Test: render con `initial={{ q: 'lente', sort: 'relevance' }}`; cambiar a `difficulty=easy` y enviar → `push` llamado con `'/recipes?q=lente&difficulty=easy&sort=relevance'` (mock de `next/navigation`).

- [ ] **Step 3: Acciones**

```ts
// lib/actions/recipes.ts
'use server'
import { revalidatePath } from 'next/cache'
import { requireHousehold } from '@/lib/auth/guards'
import { importRecipeFromText, importRecipeFromUrl, type RecipeDraft } from '@/lib/services/recipe-import'
import { createRecipe, exportAll, prepareIngredients, softDeleteRecipe, updateRecipe, type PreparedIngredient, type RecipeDetail, type RecipeExport } from '@/lib/services/recipes'
import { RecipeImportSchema, RecipeInputSchema, RecipeIngredientInputSchema } from '@/lib/validation/recipes'
import { z } from 'zod'
import { type ActionResult, fail, fromError, ok } from './result'

export async function createRecipeAction(input: unknown): Promise<ActionResult<{ id: string }>> {
  try {
    const ctx = await requireHousehold()
    const p = RecipeInputSchema.safeParse(input)
    if (!p.success) return fail('validation', p.error.issues[0]?.message ?? 'Datos inválidos')
    const d = await createRecipe(ctx, p.data)
    revalidatePath('/recipes')
    return ok({ id: d.recipe.id })
  } catch (e) { return fromError(e) }
}

export async function updateRecipeAction(id: string, input: unknown): Promise<ActionResult<{ id: string }>> {
  try {
    const ctx = await requireHousehold()
    const p = RecipeInputSchema.safeParse(input)
    if (!p.success) return fail('validation', p.error.issues[0]?.message ?? 'Datos inválidos')
    const d: RecipeDetail = await updateRecipe(ctx, id, p.data)
    revalidatePath('/recipes'); revalidatePath(`/recipes/${id}`)
    return ok({ id: d.recipe.id })
  } catch (e) { return fromError(e) }
}

export async function deleteRecipeAction(id: string): Promise<ActionResult<null>> {
  try {
    const ctx = await requireHousehold()
    await softDeleteRecipe(ctx, id)
    revalidatePath('/recipes')
    return ok(null)
  } catch (e) { return fromError(e) }
}

export async function importRecipeAction(input: unknown): Promise<ActionResult<RecipeDraft>> {
  try {
    const ctx = await requireHousehold()
    const p = RecipeImportSchema.safeParse(input)
    if (!p.success) return fail('validation', 'Entrada inválida')
    if (p.data.kind === 'url') return ok(await importRecipeFromUrl(p.data.url))
    if (p.data.kind === 'text') return ok(importRecipeFromText(p.data.text, ctx.locale))
    return fail('unsupported', 'Importar desde imagen llega con la IA (W4)')
  } catch (e) { return fromError(e) }
}

export async function prepareIngredientsAction(inputs: unknown): Promise<ActionResult<PreparedIngredient[]>> {
  try {
    const ctx = await requireHousehold()
    const p = z.array(RecipeIngredientInputSchema).max(100).safeParse(inputs)
    if (!p.success) return fail('validation', 'Ingredientes inválidos')
    return ok(await prepareIngredients(ctx, p.data, ctx.locale))
  } catch (e) { return fromError(e) }
}

export async function exportRecipesAction(): Promise<ActionResult<RecipeExport>> {
  try {
    const ctx = await requireHousehold()
    return ok(await exportAll(ctx))
  } catch (e) { return fromError(e) }
}
```

- [ ] **Step 4: Página de lista y tarjeta**

```tsx
// app/(app)/recipes/page.tsx
import Link from 'next/link'
import { getTranslations } from 'next-intl/server'
import { PlusIcon, SettingsIcon, UploadIcon } from '@/components/icons'
import { RecipeCard } from '@/components/recipes/recipe-card'
import { RecipeFilters } from '@/components/recipes/recipe-filters'
import { requireHousehold } from '@/lib/auth/guards'
import { searchRecipes } from '@/lib/services/recipes'
import { RecipeSearchSchema } from '@/lib/validation/recipes'

export default async function RecipesPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) {
  const ctx = await requireHousehold()
  const t = await getTranslations('recipes')
  const c = await getTranslations('common')
  const sp = await searchParams
  const page = Math.max(1, Number(sp.page ?? 1) || 1)
  const parsed = RecipeSearchSchema.safeParse({
    ...(typeof sp.q === 'string' && sp.q ? { q: sp.q } : {}),
    ...(typeof sp.tags === 'string' && sp.tags ? { tags: sp.tags.split(',') } : {}),
    ...(typeof sp.maxMinutes === 'string' && sp.maxMinutes ? { maxMinutes: sp.maxMinutes } : {}),
    ...(typeof sp.difficulty === 'string' && sp.difficulty ? { difficulty: sp.difficulty } : {}),
    ...(sp.onlyWithPantry === '1' ? { onlyWithPantry: true } : {}),
    ...(typeof sp.sort === 'string' && sp.sort ? { sort: sp.sort } : {}),
    limit: 20, offset: (page - 1) * 20,
  })
  const query = parsed.success ? parsed.data : RecipeSearchSchema.parse({})
  const { items, total } = await searchRecipes(ctx, query)
  return (
    <main>
      <div className="flex items-center justify-between">
        <h1 className="text-2xl">{t('title')}</h1>
        <div className="flex gap-1">
          <Link href="/recipes/import" aria-label={t('import.title')} className="inline-flex min-h-11 min-w-11 items-center justify-center"><UploadIcon /></Link>
          <Link href="/recipes/new" aria-label={t('new')} className="inline-flex min-h-11 min-w-11 items-center justify-center"><PlusIcon /></Link>
          <Link href="/settings" aria-label={c('settings')} className="inline-flex min-h-11 min-w-11 items-center justify-center"><SettingsIcon /></Link>
        </div>
      </div>
      <RecipeFilters initial={query} />
      {items.length === 0 ? (
        <p className="mt-6 text-text-2">{total === 0 && !query.q ? t('empty') : t('noResults')}</p>
      ) : (
        <ul className="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-2">
          {items.map((r) => <li key={r.id}><RecipeCard recipe={r} /></li>)}
        </ul>
      )}
      {total > 20 && (
        <nav className="mt-4 flex justify-between" aria-label={t('pagination')}>
          {page > 1 ? <Link href={`/recipes?${new URLSearchParams({ ...Object.fromEntries(Object.entries(sp).filter(([, v]) => typeof v === 'string')) as Record<string, string>, page: String(page - 1) })}`}>{c('actions.back')}</Link> : <span />}
          {page * 20 < total ? <Link href={`/recipes?${new URLSearchParams({ ...Object.fromEntries(Object.entries(sp).filter(([, v]) => typeof v === 'string')) as Record<string, string>, page: String(page + 1) })}`}>{t('more')}</Link> : <span />}
        </nav>
      )}
    </main>
  )
}
```

`RecipeCard` (server, `components/recipes/recipe-card.tsx`): `Card` con imagen (`<img>` de `imageUrl` o un bloque `bg-surface-2` con `RecipesIcon`), título (`font-display`), fila de metadatos `tabular`: `{totalMinutes} min · {kcalPerServing} kcal/{t('perServing')}` con `EstimatedIcon` si `nutritionIsEstimated`, chips de etiquetas (`rounded-pill bg-surface-2`), `Link` a `/recipes/[id]`. Es Server Component: formatea números con `Intl.NumberFormat(locale)` (sin hooks).

`messages/es/recipes.json` añadir: `"new": "Nueva receta"`, `"empty": "Aún no hay recetas. Crea la primera o importa una."`, `"noResults": "Sin resultados con esos filtros."`, `"more": "Siguiente"`, `"pagination": "Paginación"`, `"perServing": "ración"`, `"minutes": "min"`, `"filters": { "search": "Buscar…", "difficulty": "Dificultad", "any": "Cualquiera", "easy": "Fácil", "medium": "Media", "hard": "Difícil", "maxMinutes": "Máx. minutos", "onlyWithPantry": "Tengo los ingredientes", "sort": "Orden", "relevance": "Relevancia", "recent": "Recientes", "most_cooked": "Más cocinadas", "title": "Título", "apply": "Filtrar" }`, `"import": { "title": "Importar receta", … }` (el resto en Task 12). Y sus equivalentes en `en`.

- [ ] **Step 5: Ejecutar** — `pnpm check && pnpm build` — verde; `pnpm dev` + registrarse en `/register` (autenticador virtual no disponible fuera de Playwright: usar `curl` solo para 307) — comprobar visualmente `/recipes` vacío.

- [ ] **Step 6: Commit**

```bash
git add lib/actions/recipes.ts components/recipes/recipe-card.tsx components/recipes/recipe-filters.tsx components/recipes/recipe-filters.test.tsx components/icons "app/(app)/recipes/page.tsx" messages
git commit -m "Añade lista de recetas con filtros y acciones"
```

---

### Task 10: Detalle de receta con stepper de raciones en vivo

**Files:**
- Create: `app/(app)/recipes/[id]/page.tsx`, `components/recipes/servings-stepper.tsx`, `components/recipes/servings-stepper.test.tsx`, `components/recipes/ingredient-list.tsx`, `components/recipes/nutrition-row.tsx`, `components/recipes/recipe-detail.tsx`
- Modify: `messages/*/recipes.json`

**Interfaces:**
- Produces: `<RecipeDetailView detail locale units />` (cliente): mantiene `servings` en estado, recalcula con `scaleRecipe` (dominio, en cliente) y pinta `IngredientList` (cantidades con `toDisplayUnit` + `formatQuantity`; ámbar `text-warn` + `WarningIcon` + `bg-warn-soft` en los no lineales, con nota `recipes.detail.nonLinearNote`), `NutritionRow` (kcal grande por ración, total pequeño, por 100 g o "—", `EstimatedIcon` si estimado), pasos, etiquetas, notas, `timesCooked`, botones "Cocinar" (enlace a `/cook/recipe/[id]?servings=N`, lo implementa W3) y "Añadir al plan" (enlace a `/plan?add=[id]&servings=N`, lo consume (c)), "Editar", "Eliminar" (con `Dialog` de confirmación → `deleteRecipeAction`).

- [ ] **Step 1: Test del stepper (ui)** — `ServingsStepper value min=1 max=100 onChange` con botones `−`/`+` (`MinusIcon`/`PlusIcon`, `min-h-11 min-w-11`), `aria-label`s `recipes.detail.fewer`/`more`, número en `tabular`; test: click `+` → `onChange(5)` desde 4; no baja de 1.

- [ ] **Step 2: Test de la lista de ingredientes (ui)** — render con un `ScaledRecipe` cuyo `nonLinearIds` incluye la sal → la fila de sal tiene clase `text-warn` y un icono con título `recipes.detail.nonLinear`; la nota aparece una vez; cantidades formateadas (`'600 g'`, `'1 ½ cdtas'`).

- [ ] **Step 3: Implementar** `recipe-detail.tsx`:

```tsx
'use client'
import { useMemo, useState } from 'react'
import { useTranslations } from 'next-intl'
import { formatQuantity, recipeNutrition, scaleRecipe, toDisplayUnit } from '@/lib/domain'
import type { IngredientWithFood, Locale, UnitSystem } from '@/lib/domain/types'
// … (Button, Link, iconos, ServingsStepper, IngredientList, NutritionRow)

export function RecipeDetailView({ detail, locale, units }: { detail: SerializableDetail; locale: Locale; units: UnitSystem }) {
  const t = useTranslations('recipes')
  const [servings, setServings] = useState(detail.recipe.servingsBase)
  const scaled = useMemo(() => scaleRecipe({ servingsBase: detail.recipe.servingsBase, ingredients: detail.ingredients }, servings), [detail, servings])
  const nutrition = useMemo(() => (detail.ingredients.some((i) => i.food) ? recipeNutrition(detail.ingredients, detail.recipe.servingsBase, detail.recipe.yieldGrams) : null), [detail])
  const rows = scaled.ingredients.map((i) => {
    const src = detail.ingredients.find((x) => x.id === i.id) as IngredientWithFood
    const display = i.quantity !== null && i.unit !== null
      ? toDisplayUnit(i.quantity, i.unit, src.food, units, i.displayUnit ?? undefined)
      : i.displayQuantity !== null ? { quantity: i.displayQuantity, unit: i.displayUnit ?? '' } : null
    return { id: i.id, name: src.food ? (locale === 'en' ? src.food.nameEn : src.food.nameEs) : src.rawText, text: display ? formatQuantity(display.quantity, display.unit || null, locale) : '', preparation: i.preparation, nonLinear: scaled.nonLinearIds.includes(i.id), unresolved: !src.food }
  })
  // kcal por ración NO cambia al escalar; el total sí (docs/03 §2)
  const totalKcal = nutrition ? nutrition.perServing.kcal * servings : null
  …
}
```

`SerializableDetail` = `RecipeDetail` con `Date` → string (la página lo serializa con `JSON.parse(JSON.stringify(detail))` tipado). `FoodWithNutrition` ya incluye `nameEs`/`nameEn`; el mapper de (a) (`toIngredientWithFood`) recibe `FoodWithNutrition`, así que `src.food.nameEs` existe: tipar `IngredientWithFood['food']` como `FoodWithNutrition | null` en la página (cast estructural: `FoodWithNutrition extends FoodNutrition`).

Página `app/(app)/recipes/[id]/page.tsx`: `requireHousehold()` → `getRecipe(ctx, id)` → `notFound()` si null → `<RecipeDetailView detail={…} locale={ctx.locale} units={ctx.session.user.units} />`.

Claves `recipes.detail.*` (es): `servings` "Raciones", `fewer` "Menos raciones", `more` "Más raciones", `nonLinear` "No escala linealmente", `nonLinearNote` "Los ingredientes en ámbar (sal, especias, levadura…) se ajustan con suavidad al cambiar las raciones: duplicarlos arruinaría el plato.", `kcalPerServing` "kcal por ración", `total` "total", `per100g` "por 100 g", `estimated` "Nutrición estimada: revisa los alimentos marcados", `ingredients` "Ingredientes", `steps` "Preparación", `notes` "Notas", `cooked` "Cocinada {count} veces", `cook` "Cocinar", `addToPlan` "Añadir al plan", `edit` "Editar", `delete` "Eliminar", `deleteConfirm` "¿Eliminar «{title}»? Se puede recuperar exportando antes.", `unresolved` "Sin alimento asignado: no cuenta para las calorías", `timer` "{minutes} min". Y `en`.

- [ ] **Step 4: Ejecutar** — `pnpm exec vitest run --project ui components/recipes && pnpm check && pnpm build` — verde.

- [ ] **Step 5: Commit**

```bash
git add "app/(app)/recipes/[id]" components/recipes messages
git commit -m "Añade detalle de receta con escalado en vivo y nutrición por ración"
```

---

### Task 11: Editor de recetas con parser en vivo y corrección por línea

**Files:**
- Create: `app/(app)/recipes/new/page.tsx`, `app/(app)/recipes/[id]/edit/page.tsx`, `components/recipes/recipe-editor.tsx`, `components/recipes/ingredient-line-editor.tsx`, `components/recipes/ingredient-line-editor.test.tsx`, `components/recipes/image-upload.tsx`
- Modify: `messages/*/recipes.json`

**Interfaces:**
- Produces: `<RecipeEditor initial? recipeId? />` (cliente): título, descripción, raciones base, tiempos, dificultad, imágenes (`ImageUpload` → `POST /api/v1/uploads` con `fetch` + `FormData`, muestra miniaturas), etiquetas (chips con Enter), **ingredientes** como textarea de líneas + lista de `IngredientLineEditor` (una por línea: `rawText`, badge de confianza 🟢/🟡/🔴 según `needsReview`, alimento resuelto con `FoodPicker` para corregir, cantidad/unidad editables, `scalesLinearly` switch, `groupLabel`), pasos (textarea numerado, un paso por línea o párrafo), notas. "Guardar" → `createRecipeAction`/`updateRecipeAction` → `router.push('/recipes/[id]')`.
- El parseo en vivo: al perder el foco del textarea de ingredientes (o con debounce de 400 ms), llama a `prepareIngredientsAction(lines.map(rawText))` y fusiona el resultado respetando las líneas que el usuario ya corrigió a mano (marcadas `touched`).

- [ ] **Step 1: Test de `IngredientLineEditor` (ui)** — props `{ line: PreparedIngredient & { touched: boolean }, onChange, search }`: muestra `rawText`, el nombre del alimento o `recipes.food.unresolved`; al cambiar la cantidad a `'250'` → `onChange` con `displayQuantity: 250, touched: true`; el switch `scalesLinearly` alterna y llama `onChange`.

- [ ] **Step 2: Implementar** el editor. Reglas: `servingsBase` con `ServingsStepper`; validación en cliente mínima (título obligatorio, ≥ 1 ingrediente o paso); errores de la acción en `role="alert"`; `aria-busy` al guardar. Al enviar, construir `RecipeInput`: por cada línea `{ rawText, foodId, quantity, unit, displayQuantity, displayUnit, preparation, groupLabel, stepIndex: null, scalesLinearly }` (solo campos definidos; nunca `undefined` explícito — construir con spreads condicionales), `steps: [{ text }]`, `tags`, `imageUrls`.

Página `new`: `<RecipeEditor />`; con `?draft=1` lee el borrador de `sessionStorage['rz.recipeDraft']` (lo deja Task 12). Página `[id]/edit`: carga `getRecipe`, convierte a `RecipeInput` (misma forma que `exportAll`) y pasa `initial` + `recipeId`.

Claves `recipes.editor.*`: `title` "Título", `description` "Descripción", `servingsBase` "Raciones base", `prepMinutes` "Preparación (min)", `cookMinutes` "Cocción (min)", `difficulty` "Dificultad", `images` "Fotos", `addImage` "Añadir foto", `tags` "Etiquetas", `tagPlaceholder` "Escribe y pulsa Enter", `ingredients` "Ingredientes (uno por línea)", `ingredientsHint` "Ej.: 200 g de lentejas, 1 cebolla, sal al gusto", `reparse` "Volver a analizar", `confidence` { "high": "Reconocido", "mid": "Revisar", "low": "No reconocido" }, `quantity` "Cantidad", `unit` "Unidad", `scalesLinearly` "Escala linealmente", `group` "Grupo", `steps` "Pasos (uno por línea)", `notes` "Notas", `save` "Guardar receta", `saved` "Receta guardada", `titleRequired` "Pon un título", `needsContent` "Añade al menos un ingrediente o un paso", `newTitle` "Nueva receta", `editTitle` "Editar receta". Y `en`.

- [ ] **Step 3: Ejecutar** — `pnpm exec vitest run --project ui components/recipes && pnpm check && pnpm build`.

- [ ] **Step 4: Commit**

```bash
git add "app/(app)/recipes/new" "app/(app)/recipes/[id]/edit" components/recipes messages
git commit -m "Añade editor de recetas con análisis de ingredientes en vivo"
```

---

### Task 12: Importar (URL/texto), exportar y e2e de recetas

**Files:**
- Create: `app/(app)/recipes/import/page.tsx`, `components/recipes/import-form.tsx`, `components/settings/export-button.tsx` (lo usa (g) en `settings/data` — aquí solo el componente), `e2e/recipes.spec.ts`
- Modify: `messages/*/recipes.json`, `docs/07-ROADMAP.md` (marcar fase 1 como hecha en W2)

**Interfaces:**
- Produces: `/recipes/import` con pestañas URL / Texto; al importar guarda el `RecipeDraft` en `sessionStorage['rz.recipeDraft']` y navega a `/recipes/new?draft=1`; avisos (`warnings`) traducidos (`recipes.import.warnings.{no_recipe_found,no_ingredients,no_steps,fetch_failed,empty}`). `ExportButton` descarga `recetas-<fecha>.json` (Blob de `exportRecipesAction`).

- [ ] **Step 1: e2e** (`e2e/recipes.spec.ts`; usa `enableVirtualAuthenticator` + registro como en `e2e/auth.spec.ts`):

```ts
test('crear receta, ver escalado y nutrición, editar y borrar', async ({ page }) => {
  // registro
  await enableVirtualAuthenticator(page); await page.goto('/register'); await page.getByLabel(/nombre|name/i).fill(uniqueName('Ana')); await page.getByRole('button', { name: /crear passkey|create passkey/i }).click(); await expect(page).toHaveURL(/\/today$/)
  await page.goto('/recipes/new')
  await page.getByLabel(/^título|^title/i).fill('Lentejas e2e')
  await page.getByLabel(/ingredientes/i).fill('400 g de lentejas\n1 cebolla\n1 cdta de sal')
  await page.getByLabel(/ingredientes/i).blur()
  await expect(page.getByText(/reconocido/i).first()).toBeVisible()
  await page.getByLabel(/pasos/i).fill('Cuece 45 minutos')
  await page.getByRole('button', { name: /guardar receta/i }).click()
  await expect(page).toHaveURL(/\/recipes\/[0-9a-f-]{36}$/)
  await expect(page.getByRole('heading', { level: 1 })).toHaveText('Lentejas e2e')
  await expect(page.getByText(/kcal por ración/i)).toBeVisible()
  const kcalBefore = await page.getByTestId('kcal-per-serving').textContent()
  await page.getByRole('button', { name: /más raciones/i }).click()
  await expect(page.getByTestId('kcal-per-serving')).toHaveText(kcalBefore ?? '')
  await expect(page.getByText(/no escala linealmente/i).first()).toBeVisible()
  await page.getByRole('link', { name: /editar/i }).click()
  await page.getByLabel(/^título|^title/i).fill('Lentejas e2e v2')
  await page.getByRole('button', { name: /guardar receta/i }).click()
  await expect(page.getByRole('heading', { level: 1 })).toHaveText('Lentejas e2e v2')
  await page.getByRole('button', { name: /eliminar/i }).click()
  await page.getByRole('button', { name: /confirmar/i }).click()
  await expect(page).toHaveURL(/\/recipes$/)
})
test('importar desde texto abre el editor con el borrador', async ({ page }) => { /* registro; /recipes/import; pestaña Texto; pegar; Importar; esperar /recipes/new?draft=1; comprobar título prellenado */ })
```

El detalle debe exponer `data-testid="kcal-per-serving"` en el número grande; el seed de alimentos debe estar en la DB de dev (`pnpm db:seed`) para que "lentejas", "cebolla" y "sal" se resuelvan — documentar en `e2e/README.md`.

- [ ] **Step 2: Implementar** página e `ImportForm` (cliente): dos pestañas (botones con `aria-pressed`), campo URL (`type="url"`) o textarea; "Importar" → `importRecipeAction({kind, url|text})` → si `ok`: `sessionStorage.setItem('rz.recipeDraft', JSON.stringify(data))`, `router.push('/recipes/new?draft=1')`; avisos en lista `role="status"`. `ExportButton`: `onClick` → acción → `Blob` → `URL.createObjectURL` → `<a download>` programático.

Claves `recipes.import.*`: `title` "Importar receta", `fromUrl` "Desde una URL", `fromText` "Desde texto", `url` "Dirección de la receta", `text` "Pega la receta", `submit` "Importar", `warnings.no_recipe_found` "No se encontró una receta en esa página; pega el texto.", `warnings.no_ingredients` "No se detectaron ingredientes.", `warnings.no_steps` "No se detectaron pasos.", `warnings.fetch_failed` "No se pudo descargar la página.", `warnings.empty` "El texto está vacío.", `export` "Exportar todas las recetas (JSON)". Y `en`.

- [ ] **Step 3: Ejecutar** — `pnpm check && pnpm build && LD_LIBRARY_PATH=$HOME/.local/chromium-deps/usr/lib/x86_64-linux-gnu pnpm exec playwright test e2e/recipes.spec.ts` — verde.

- [ ] **Step 4: Commit**

```bash
git add "app/(app)/recipes/import" components/recipes/import-form.tsx components/settings/export-button.tsx e2e/recipes.spec.ts e2e/README.md messages docs/07-ROADMAP.md
git commit -m "Añade importación y exportación de recetas con e2e"
```

**Pista (a) lista cuando:** `pnpm check`, `pnpm build` y `e2e/recipes.spec.ts` verdes; `/recipes` sustituye al método actual de guardar recetas (criterio de `docs/07` fase 1).

---
# Pista (d) · Despensa — Tareas 13–17

Worktree `w2-d-pantry` desde `main` **con (b) mergeada**. Base de test `rezetapp_test_d`. Dependencia nueva: `pnpm add @zxing/browser@0.2.1`.

### Task 13: Servicio de despensa

**Files:**
- Create: `lib/services/pantry.ts`, `lib/services/pantry.test.ts`

**Interfaces:**
- Consumes: `pantryItems`, `foods` (schema); `expiringSoon` (domain); `PantryItemInput`, `PantryAdjust`, `PantryQuery` (validation — añadir `export type PantryItemInput = z.infer<…>`, `PantryQuery`, `PantryAdjust` en `lib/validation/pantry.ts`); `getFoodsNutrition`.
- Produces:
  ```ts
  export interface PantryRow { id: string; foodId: string; name: string; quantity: number; unit: BaseUnit; location: 'fridge'|'freezer'|'pantry'; expiresAt: string | null; openedAt: string | null; addedAt: string; daysToExpiry: number | null; food: FoodWithNutrition }
  export function listPantry(ctx, query: PantryQuery): Promise<PantryRow[]>            // orden: ubicación, caducidad asc nulls last, nombre
  export function upsertPantryItem(ctx, input: PantryItemInput & { id?: string }): Promise<PantryRow>
  export function adjustPantryItem(ctx, input: PantryAdjust): Promise<PantryRow>          // UPDATE … SET quantity = GREATEST(0, quantity + delta) … RETURNING (atómico)
  export function removePantryItem(ctx, id): Promise<void>
  export function expiringPantry(ctx, days: number, today?: Date): Promise<PantryRow[]>
  export function pantryAsDomain(ctx): Promise<PantryItem[]>                              // para consolidateNeeds / allocateDeductions (W3)
  ```

- [ ] **Step 1: Test que falla** (db)

```ts
// lib/services/pantry.test.ts
import { afterAll, beforeAll, beforeEach, describe, expect, it } from 'vitest'
import { closeTestDb, getTestDb, truncateAll, type TestDb } from '@/db/test/setup'
import * as schema from '@/db/schema'
import type { Ctx } from '@/lib/services/ctx'
import { adjustPantryItem, expiringPantry, listPantry, pantryAsDomain, removePantryItem, upsertPantryItem } from './pantry'

let db: TestDb, ctxA: Ctx, ctxB: Ctx, onion: string
async function makeHousehold(name: string): Promise<Ctx> {
  const [h] = await db.insert(schema.households).values({ name }).returning()
  const [u] = await db.insert(schema.users).values({ displayName: name }).returning()
  await db.insert(schema.householdMembers).values({ householdId: h!.id, userId: u!.id, role: 'owner' })
  return { db, householdId: h!.id, userId: u!.id, apiTokenId: null, role: 'owner', locale: 'es' }
}
beforeAll(async () => { db = await getTestDb() })
afterAll(closeTestDb)
beforeEach(async () => {
  await truncateAll(db)
  ctxA = await makeHousehold('A'); ctxB = await makeHousehold('B')
  const [f] = await db.insert(schema.foods).values({ nameEs: 'cebolla', nameEn: 'onion', searchNameEs: 'cebolla', searchNameEn: 'onion', source: 'usda', kcal100g: 40 }).returning()
  onion = f!.id
})

describe('pantry', () => {
  it('upsert crea y actualiza; list ordena por ubicación y caducidad; aislamiento', async () => {
    const a = await upsertPantryItem(ctxA, { foodId: onion, quantity: 500, unit: 'g', location: 'fridge', expiresAt: '2026-09-01' })
    await upsertPantryItem(ctxA, { foodId: onion, quantity: 200, unit: 'g', location: 'pantry' })
    const again = await upsertPantryItem(ctxA, { id: a.id, foodId: onion, quantity: 450, unit: 'g', location: 'fridge', expiresAt: '2026-09-01' })
    expect(again.id).toBe(a.id)
    const rows = await listPantry(ctxA, {})
    expect(rows.map((r) => [r.location, r.quantity])).toEqual([['fridge', 450], ['pantry', 200]])
    expect(await listPantry(ctxB, {})).toEqual([])
    await expect(upsertPantryItem(ctxB, { id: a.id, foodId: onion, quantity: 1, unit: 'g', location: 'fridge' })).rejects.toMatchObject({ code: 'not_found' })
  })
  it('adjust es atómico y nunca baja de 0', async () => {
    const a = await upsertPantryItem(ctxA, { foodId: onion, quantity: 100, unit: 'g', location: 'pantry' })
    expect((await adjustPantryItem(ctxA, { itemId: a.id, delta: -30 })).quantity).toBe(70)
    expect((await adjustPantryItem(ctxA, { itemId: a.id, delta: -500 })).quantity).toBe(0)
    await expect(adjustPantryItem(ctxB, { itemId: a.id, delta: -1 })).rejects.toMatchObject({ code: 'not_found' })
    const results = await Promise.all(Array.from({ length: 10 }, () => adjustPantryItem(ctxA, { itemId: a.id, delta: 5 })))
    expect(Math.max(...results.map((r) => r.quantity))).toBe(50)
  })
  it('expiring devuelve lo que caduca en N días con daysToExpiry, y filtros de list', async () => {
    const today = new Date('2026-08-26T00:00:00Z')
    await upsertPantryItem(ctxA, { foodId: onion, quantity: 1, unit: 'ud', location: 'fridge', expiresAt: '2026-08-28' })
    await upsertPantryItem(ctxA, { foodId: onion, quantity: 1, unit: 'ud', location: 'freezer', expiresAt: '2026-10-01' })
    const exp = await expiringPantry(ctxA, 3, today)
    expect(exp.map((r) => r.daysToExpiry)).toEqual([2])
    expect((await listPantry(ctxA, { location: 'freezer' })).length).toBe(1)
    expect((await listPantry(ctxA, { expiresBefore: '2026-09-01' })).length).toBe(1)
    expect((await listPantry(ctxA, { q: 'cebo' })).length).toBe(2)
  })
  it('remove y pantryAsDomain', async () => {
    const a = await upsertPantryItem(ctxA, { foodId: onion, quantity: 10, unit: 'g', location: 'pantry' })
    expect((await pantryAsDomain(ctxA))[0]).toMatchObject({ id: a.id, foodId: onion, quantity: 10, unit: 'g', expiresAt: null })
    await removePantryItem(ctxA, a.id)
    expect(await listPantry(ctxA, {})).toEqual([])
    await expect(removePantryItem(ctxB, a.id)).rejects.toMatchObject({ code: 'not_found' })
  })
})
```

- [ ] **Step 2: Ejecutar** — FAIL.

- [ ] **Step 3: Implementar** (`lib/services/pantry.ts`): `listPantry` = select con `innerJoin(foods)` filtrando `householdId`, `location`, `expiresAt <= expiresBefore`, `q` con `similarity(search_name_<locale>, q) >= 0.25 OR search_name LIKE q%`; `orderBy(location, sql\`expires_at asc nulls last\`, name)`; `daysToExpiry = expiresAt ? Math.ceil((Date.parse(expiresAt) - startOfDay(today)) / 86_400_000) : null`. `upsertPantryItem`: si `id` → `update … where id and household_id` (returning; sin fila → not_found) else `insert`; el `foodId` debe ser visible (`getFoodsNutrition` size 1, si no → `validation`). `adjustPantryItem`:

```ts
const [row] = await ctx.db.update(schema.pantryItems)
  .set({ quantity: sql`GREATEST(0, ${schema.pantryItems.quantity} + ${input.delta})` })
  .where(and(eq(schema.pantryItems.id, input.itemId), eq(schema.pantryItems.householdId, ctx.householdId))).returning()
if (!row) throw new ServiceError('not_found', 'Artículo no encontrado')
emitHouseholdEvent(ctx.householdId, { type: 'pantry.changed', payload: { foodIds: [row.foodId] } })
```

`expiringPantry` usa `expiringSoon(pantryAsDomain rows, today, days)` y luego mapea a `PantryRow` (una sola consulta + filtro en dominio). Todas las escrituras emiten `pantry.changed`.

- [ ] **Step 4: Ejecutar** — 4 passed. **Step 5: Commit** — `git add lib/services/pantry.ts lib/services/pantry.test.ts lib/validation/pantry.ts && git commit -m "Añade servicio de despensa con ajuste atómico"`.

---

### Task 14: Acciones y página de despensa por ubicación

**Files:**
- Create: `lib/actions/pantry.ts`, `components/pantry/pantry-list.tsx`, `components/pantry/pantry-row.tsx`, `components/pantry/pantry-row.test.tsx`
- Modify: `app/(app)/pantry/page.tsx`, `messages/*/pantry.json`

**Interfaces:**
- Produces: acciones `upsertPantryItemAction(input)`, `adjustPantryItemAction(itemId, delta)`, `removePantryItemAction(id)`, `lookupBarcodeAction` se reutiliza de (b). Página `/pantry?location&q`: secciones por ubicación (`FridgeIcon`/`FreezerIcon`/`CupboardIcon`), cada `PantryRow`: nombre, cantidad en unidad de presentación (`toDisplayUnit`+`formatQuantity`, cliente), stepper `−`/`+` (paso: 1 para `ud`; 10 g/ml para < 1 kg; 100 para ≥ 1 kg) que llama `adjustPantryItemAction` con actualización optimista, días hasta caducar en `text-warn` si < 7, eliminar. `useHouseholdEvents` con `pantry.changed` → `router.refresh()`.

- [ ] **Step 1: Test `PantryRow` (ui)** — render fila con `quantity 500, unit 'g', daysToExpiry 3` → muestra `'500 g'`, texto de caducidad con clase `text-warn`; click `+` → `adjust` mock con `(id, 10)`.
- [ ] **Step 2: Implementar** acciones (mismo patrón que `lib/actions/foods.ts`; `PantryItemInputSchema.extend({ id: IdSchema.optional() })`, `PantryAdjustSchema`), componentes y página (Server Component que llama `listPantry(ctx, query)` y agrupa por `location`; cabecera con enlaces a `/pantry/add` (`PlusIcon`) y `/pantry/scan` (`BarcodeIcon`); buscador `<form method="get">`).
- Claves `pantry.*` (es): `title`, `locations.{fridge,freezer,pantry}` "Nevera/Congelador/Armario", `add` "Añadir", `scan` "Escanear", `search` "Buscar en la despensa…", `empty` "La despensa está vacía. Añade lo que tengas en casa.", `expiresIn` "Caduca en {days} días", `expiresToday` "Caduca hoy", `expired` "Caducado", `noExpiry` "Sin fecha", `remove` "Quitar", `decrease` "Menos", `increase` "Más", `whatToCook` "¿Qué cocino con lo que caduca?", `expiringTitle` "Caduca pronto". Y `en`.
- [ ] **Step 3: Ejecutar** — `pnpm check && pnpm build`. **Step 4: Commit** — `git commit -m "Añade página de despensa por ubicación con ajustes rápidos"`.

---

### Task 15: Añadir artículo (con `FoodPicker`) y crear alimento nuevo

**Files:**
- Create: `app/(app)/pantry/add/page.tsx`, `components/pantry/pantry-item-form.tsx`, `components/pantry/pantry-item-form.test.tsx`
- Modify: `messages/*/pantry.json`

- [ ] **Step 1: Test (ui)** — el formulario con `FoodPicker` (search mock), cantidad, unidad (`g`/`ml`/`ud`, preseleccionada con `defaultUnit` del alimento elegido), ubicación (3 botones `aria-pressed`), caducidad (`type="date"`); enviar → `upsert` mock llamado con `{ foodId:'f1', quantity: 500, unit:'g', location:'fridge', expiresAt:'2026-09-01' }`; sin alimento seleccionado el botón está `disabled` y aparece enlace "Crear alimento nuevo" que abre `FoodCorrectionDialog` en modo creación (prop `mode="create"` — añadir a (b)'s diálogo: cuando `food` es `null` llama `createFoodAction` en vez de `correctFoodAction`; adaptar en esta tarea si (b) no lo dejó).
- [ ] **Step 2: Implementar**; `?foodId=&name=` en la URL prellena (lo usa el escáner). Tras guardar → `router.push('/pantry')`.
- Claves: `pantry.form.{food,quantity,unit,location,expiresAt,save,createFood,pickFood}`.
- [ ] **Step 3: Ejecutar** y **Commit** — `"Añade formulario de despensa con selector de alimento"`.

---

### Task 16: Escáner de código de barras

**Files:**
- Create: `app/(app)/pantry/scan/page.tsx`, `components/pantry/barcode-scanner.tsx`, `lib/barcode.ts`, `lib/barcode.test.ts`
- Modify: `messages/*/pantry.json`

**Interfaces:**
- Produces: `lib/barcode.ts` (puro): `normalizeBarcode(raw): string | null` (quita espacios, valida 8–14 dígitos, EAN-13 checksum si 13 dígitos; UPC-A 12 → prefijo 0). `BarcodeScanner` (cliente): usa `BarcodeDetector` nativo si existe (`'BarcodeDetector' in window`), si no `@zxing/browser` (`BrowserMultiFormatReader.decodeFromVideoDevice`); pide cámara trasera (`facingMode: 'environment'`); al detectar → `onDetected(code)` una vez y para; botón "Introducir a mano" con `inputMode="numeric"`.

- [ ] **Step 1: Test `lib/barcode.test.ts`** (unit): `'8410000810004'` válido; `'8410000810005'` (checksum mal) → null; `' 12345678 '` → `'12345678'`; `'abc'` → null; `'036000291452'` (UPC-A) → `'0036000291452'`.
- [ ] **Step 2: Implementar** `lib/barcode.ts`; página `/pantry/scan`: `<BarcodeScanner onDetected>` → `lookupBarcodeAction(code)` → si alimento: `router.push('/pantry/add?foodId=…&name=…')`; si null: mensaje `pantry.scan.notFound` + botón para crear alimento con el código prellenado (`FoodCorrectionDialog` modo creación con `barcode`). Permiso denegado / sin cámara → `pantry.scan.noCamera` y entrada manual.
- Claves `pantry.scan.*`: `title` "Escanear código", `hint` "Apunta al código de barras", `manual` "Introducir a mano", `lookup` "Buscar", `notFound` "No conocemos este producto. Puedes crearlo.", `noCamera` "No hay cámara disponible o no se dio permiso.", `found` "Encontrado: {name}".
- [ ] **Step 3: Ejecutar** `pnpm check && pnpm build`. **Commit** — `"Añade escáner de código de barras con Open Food Facts"`.

---

### Task 17: "Qué cocinar con lo que caduca" y e2e de despensa

**Files:**
- Create: `components/pantry/expiring-panel.tsx`, `e2e/pantry.spec.ts`
- Modify: `app/(app)/pantry/page.tsx`, `messages/*/pantry.json`

- [ ] **Step 1: Panel** (server): `expiringPantry(ctx, household.expiryAlertDays)`; si hay filas, muestra la lista y un enlace `/recipes?hasIngredients=<foodIds>&sort=most_cooked` con texto `pantry.whatToCook`. (`hasIngredients` en la URL de recetas: (a) lo lee como lista separada por comas — coordinar: en `app/(app)/recipes/page.tsx` añadir `...(typeof sp.hasIngredients === 'string' ? { hasIngredients: sp.hasIngredients.split(',') } : {})` — esta pista lo añade en su merge si (a) no lo hizo.)
- [ ] **Step 2: e2e** — registro; `/pantry/add`: buscar "cebolla" (seed), elegir, 500 g, nevera, caduca en 2 días → guardar; `/pantry` muestra `500 g` y "Caduca en 2 días" en ámbar; `+` → `510 g`; panel "Caduca pronto" visible; quitar → lista vacía.
- [ ] **Step 3: Ejecutar** e2e con `LD_LIBRARY_PATH` y `pnpm check`. **Commit** — `"Añade panel de caducidades y e2e de despensa"`.

**Pista (d) lista cuando:** `pnpm check`, `pnpm build`, `e2e/pantry.spec.ts` verdes.

---

# Pista (c) · Plan — Tareas 18–23

Worktree `w2-c-plan` desde `main` (no depende de (b)). Base de test `rezetapp_test_c`. Dependencias: `pnpm add @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/utilities@3.2.2`.

### Task 18: Servicio del plan — entradas

**Files:**
- Create: `lib/services/plan.ts`, `lib/services/plan.test.ts`
- Modify: `lib/validation/plan.ts` (añade `export type PlanEntryInput`, `PlanEntryMove`, `PlanEntryPatch`)

**Interfaces:**
```ts
export interface PlanEntryView { id: string; date: string; slot: MealSlot; recipeId: string | null; title: string; servings: number; leftoverOfEntryId: string | null; timeBudgetMinutes: number | null; status: EntryStatus; sortOrder: number; kcalPerServing: number | null; totalMinutes: number | null; imageUrl: string | null }
export function listEntries(ctx, range: { from: string; to: string }): Promise<PlanEntryView[]>
export function applyBatch(ctx, batch: PlanBatch): Promise<{ added: PlanEntryView[]; removed: string[] }>   // transacción; emite plan.changed con las fechas tocadas
export function moveEntry(ctx, input: PlanEntryMove): Promise<PlanEntryView>
export function patchEntry(ctx, id, patch: PlanEntryPatch): Promise<PlanEntryView>       // servings, skipped ↔ skippedAt, timeBudgetMinutes
export function createLeftover(ctx, input: { ofEntryId: string; date: string; slot: MealSlot; servings: number }): Promise<PlanEntryView>
export function rangeNutrition(ctx, range): Promise<{ byDate: Record<string, Nutrition>; total: Nutrition }>   // aggregateNutrition sobre entradas planned+cooked (no skipped ni sobras) usando recipes.kcal_per_serving y macros
```

- [ ] **Step 1: Test que falla** (db): fixtures: hogar A/B, receta en A (insert directo en `recipes` con `kcalPerServing: 400, proteinPerServing: 20, carbsPerServing: 40, fatPerServing: 15, fiberPerServing: 5, servingsBase: 2, prepMinutes 10, cookMinutes 20`). Casos: `applyBatch` añade 2 entradas y quita 1 (la de otro hogar se ignora sin error); `listEntries` filtra por rango y hogar y calcula `status`; `moveEntry` cambia fecha/slot/sortOrder y no acepta id ajeno (`not_found`); `patchEntry({skipped:true})` pone `skippedAt` y `status 'skipped'`, `{skipped:false}` lo limpia; `createLeftover` crea una entrada con `leftoverOfEntryId` y `recipeId` heredado; `rangeNutrition` de 2 raciones de 400 kcal = 800 en `byDate[fecha].total.kcal`, excluye saltadas y sobras; `customTitle` sin receta permitido.
- [ ] **Step 2: Implementar**. `title = recipe.title ?? customTitle`. Validar en `applyBatch` que `recipeId` pertenece al hogar (si no → `validation`). `emitHouseholdEvent(…, { type: 'plan.changed', payload: { dates } })`.
- [ ] **Step 3: Ejecutar** 7 passed. **Commit** — `"Añade servicio del plan de comidas"`.

---

### Task 19: Servicio del plan — propuestas

**Files:**
- Modify: `lib/services/plan.ts`, `lib/services/plan.test.ts`

**Interfaces:**
```ts
export interface ProposalView { id: string; source: 'ai'|'rules'|'mcp'; status: 'pending'|'approved'|'rejected'; createdAt: string; payload: ProposalPayload; diff: { add: (PlanEntryInput & { title: string })[]; remove: PlanEntryView[] } }
export function createProposal(ctx, input: { source; payload: ProposalPayload }): Promise<ProposalView>   // createdByUserId o createdByTokenId según ctx; emite proposal.created
export function listProposals(ctx, status?: 'pending'): Promise<ProposalView[]>
export function decideProposal(ctx, id, decision: 'approve'|'reject'): Promise<ProposalView>   // approve = applyBatch en la misma transacción + status approved + resolvedBy; remove ids inexistentes se ignoran
```

- [ ] **Step 1: Tests** — crear propuesta desde `ctx` de usuario (createdByUserId) y desde `ctx` con `apiTokenId` (insertar un `api_tokens` de fixture); `diff.add[].title` resuelve el título de la receta; `approve` aplica y marca; segundo `approve` → `conflict`; `reject` no toca el plan; hogar B no ve propuestas de A.
- [ ] **Step 2: Implementar**; **Step 3:** 5 passed; **Commit** — `"Añade propuestas de plan con aprobación en diff"`.

---

### Task 20: Acciones del plan y calendario semanal con arrastrar y soltar

**Files:**
- Create: `lib/actions/plan.ts`, `components/plan/week-view.tsx`, `components/plan/day-column.tsx`, `components/plan/entry-chip.tsx`, `components/plan/entry-chip.test.tsx`, `components/plan/add-entry-sheet.tsx`, `lib/plan-dates.ts`, `lib/plan-dates.test.ts`
- Modify: `app/(app)/plan/page.tsx`, `messages/*/plan.json`, `components/icons/index.ts` (+ `SkipIcon` `<path d="M5 5l10 7-10 7V5ZM19 5v14"/>`, `CalendarMonthIcon` `<rect x="3.5" y="5" width="17" height="15.5" rx="3"/><path d="M3.5 9.8h17M8 3v3.5M16 3v3.5M7.5 13h2M11 13h2M14.5 13h2M7.5 16.5h2M11 16.5h2"/>`, `SparklesIcon` `<path d="M12 3l1.8 4.7L18.5 9.5l-4.7 1.8L12 16l-1.8-4.7L5.5 9.5l4.7-1.8L12 3ZM5 17l.9 2.1L8 20l-2.1.9L5 23l-.9-2.1L2 20l2.1-.9L5 17Z"/>`)

**Interfaces:**
- `lib/plan-dates.ts` (puro, tipo `lib`): `weekRange(dateIso, weekStartsOn = 1) → { from, to, days: string[] }`, `addDays(iso, n)`, `todayIso(tz = 'Europe/Madrid')`, `monthRange(iso)`.
- Acciones: `applyPlanBatchAction(batch)`, `movePlanEntryAction(input)`, `patchPlanEntryAction(id, patch)`, `createLeftoverAction(input)`, `decideProposalAction(id, decision)`.
- Página `/plan?week=YYYY-MM-DD` (Server): `listEntries(ctx, weekRange)` + `rangeNutrition` + `household.defaultServings` → `<WeekView entries days defaultServings kcalByDate />` (cliente): 7 `DayColumn` × 4 slots; `DndContext` + `useDraggable`/`useDroppable` (touch con `PointerSensor` + `TouchSensor`, `activationConstraint: { delay: 150, tolerance: 5 }`); soltar → `movePlanEntryAction` optimista; `EntryChip`: título, raciones (stepper inline si ≠ default), chips `LeftoversIcon` si sobra, `SkipIcon` toggle, `ClockIcon` + presupuesto de tiempo del día; `+` por hueco abre `AddEntrySheet` (`Sheet`) con búsqueda de recetas (acción de esta pista `searchRecipesForPlanAction(q)` → `{ id, title }[]` en `lib/actions/plan.ts`, respaldada por `lib/services/plan.ts::searchRecipesLite(ctx, q)` — `SELECT id, title FROM recipes WHERE household_id = … AND deleted_at IS NULL AND title ILIKE '%q%' ORDER BY updated_at DESC LIMIT 10`; (a) no es dependencia) y "comida libre" (`customTitle`). `?add=<recipeId>&servings=N` abre el sheet con esa receta preseleccionada (enlace desde el detalle de receta de (a)).

- [ ] **Step 1: Tests** — `plan-dates.test.ts` (lunes como inicio; cruce de mes/año); `entry-chip.test.tsx` (muestra título y raciones; `skipped` con `line-through`; click en `SkipIcon` llama `onSkip`).
- [ ] **Step 2: Implementar**. `useHouseholdEvents(['plan.changed','proposal.created'])` → `router.refresh()`.
- Claves `plan.*` (es): `title`, `week` "Semana", `month` "Mes", `today` "Hoy", `prevWeek`/`nextWeek` "Semana anterior/siguiente", `slots.{breakfast,lunch,dinner,snack}` "Desayuno/Comida/Cena/Picoteo", `addTo` "Añadir a {slot}", `searchRecipe` "Buscar receta…", `freeMeal` "Comida libre", `freeMealTitle` "Título", `servings` "Raciones", `leftover` "Sobras", `skip` "Saltar", `unskip` "Reactivar", `skipped` "Saltada", `cooked` "Cocinada", `timeBudget` "Tiempo disponible (min)", `kcalDay` "{kcal} kcal", `remove` "Quitar del plan", `dragHint` "Mantén pulsado para mover", `proposals` "Propuestas", `shopping` "Generar compra", `empty` "Semana vacía: añade comidas o pide una propuesta.". Y `en`.
- [ ] **Step 3:** `pnpm check && pnpm build`. **Commit** — `"Añade calendario semanal con arrastrar y soltar"`.

---

### Task 21: Vista mensual y sobras

**Files:**
- Create: `app/(app)/plan/month/page.tsx`, `components/plan/month-view.tsx`, `components/plan/leftover-dialog.tsx`
- Modify: `messages/*/plan.json`

- [ ] **Step 1:** `/plan/month?month=YYYY-MM`: rejilla 6×7 con puntos por slot ocupado y kcal del día; click → `/plan?week=`. `LeftoverDialog` desde un `EntryChip`: fecha (por defecto mañana), slot, raciones → `createLeftoverAction`. Test ui del diálogo: valores por defecto y llamada.
- [ ] **Step 2:** `pnpm check && pnpm build`; **Commit** — `"Añade vista mensual y sobras planificables"`.

---

### Task 22: Panel de propuestas con diff

**Files:**
- Create: `app/(app)/plan/proposals/page.tsx`, `components/plan/proposal-card.tsx`, `components/plan/proposal-card.test.tsx`
- Modify: `messages/*/plan.json`

- [ ] **Step 1: Test** — tarjeta con `diff.add` (2) y `diff.remove` (1): pinta `+` en `text-acc-ink` y `−` en `text-warn`, botones "Aprobar"/"Descartar" llaman `decide` con `'approve'`/`'reject'`; `status !== 'pending'` → sin botones y etiqueta.
- [ ] **Step 2:** página lista `listProposals(ctx)` (pendientes primero); badge con el nº de pendientes en el botón "Propuestas" de `/plan`.
- Claves `plan.proposals.*`: `title` "Propuestas", `empty` "No hay propuestas. Llegarán de la IA, del asistente MCP o de tus reglas.", `from.{ai,rules,mcp}` "de la IA / de tus reglas / del asistente", `approve` "Aprobar", `reject` "Descartar", `approved` "Aprobada", `rejected` "Descartada", `adds` "Añade", `removes` "Quita".
- [ ] **Step 3:** verde; **Commit** — `"Añade panel de propuestas de plan"`.

---

### Task 23: Resumen de compra (sin envío) y e2e del plan

**Files:**
- Create: `app/(app)/plan/shopping/page.tsx`, `components/plan/shopping-summary.tsx`, `e2e/plan.spec.ts`
- Modify: `lib/services/plan.ts` (añade `plannedEntriesForShopping(ctx, range): Promise<PlannedEntry[]>` — carga entradas + recetas + ingredientes con `foodName` para `consolidateNeeds`), `messages/*/plan.json`

- [ ] **Step 1:** `/plan/shopping?from&to` (default: semana actual): `consolidateNeeds(await plannedEntriesForShopping(ctx, range), await pantryForShopping(ctx))`. Decisión: esta pista implementa `pantryForShopping(ctx)` en `lib/services/plan.ts` (select de `pantry_items` del hogar mapeado a `PantryItem` de dominio); la pista (f) lo sustituye por `pantryAsDomain` de (d) al mergear. Lista de `ShoppingLine`s agrupada: resueltas, `unresolved` (ámbar), sin cantidad; botón "Enviar a ShopList" **oculto** (lo añade (f)). Test unit de `plannedEntriesForShopping` en `plan.test.ts` (excluye cocinadas/saltadas/sobras).
- [ ] **Step 2: e2e** — registro; crear receta vía `POST /api/v1/…`? No existe aún (W3). Alternativa: insertar receta con la UI de (a) si está mergeada; si no, el e2e crea la entrada como "comida libre": `/plan` → `+` en Cena de hoy → "Comida libre" "Pizza" 2 raciones → chip visible; arrastrar no se prueba (dnd en e2e es frágil); saltar → tachado; `/plan/proposals` vacío con mensaje; `/plan/month` muestra un punto hoy.
- [ ] **Step 3:** verde; **Commit** — `"Añade resumen de compra y e2e del plan"`.

**Pista (c) lista cuando:** `pnpm check`, `pnpm build`, `e2e/plan.spec.ts` verdes.

---
# Pista (e) · IA — Tareas 24–28

Worktree `w2-e-ai` desde `main` (independiente). Base de test `rezetapp_test_e`. Dependencias: `pnpm add ai@7.0.79 @ai-sdk/anthropic@4.0.42 @ai-sdk/openai@4.0.47 @ai-sdk/openai-compatible@3.0.37`. Regla 3 de `AGENTS.md`: todo funciona sin IA — cada tarea devuelve `null`/`'no_provider'` limpiamente cuando el hogar no tiene proveedor.

### Task 24: Catálogo de modelos y proveedor por hogar

**Files:**
- Create: `lib/ai/models.ts`, `lib/ai/provider.ts`, `lib/ai/provider.test.ts`, `lib/services/ai-settings.ts`, `lib/services/ai-settings.test.ts`
- Modify: `lib/validation/household.ts` (añade `AiSettingsSchema`)

**Interfaces:**
```ts
// lib/ai/models.ts — precios en céntimos por millón de tokens (in/out). Los ids de Anthropic/OpenAI se
// verifican contra la documentación oficial del proveedor antes de publicar; si cambian, solo se toca este fichero.
export interface ModelInfo { id: string; provider: 'anthropic' | 'openai' | 'openai_compatible'; label: string; inputCentsPerM: number; outputCentsPerM: number; vision: boolean }
export const MODELS: ModelInfo[]            // anthropic: 2 entradas (uno rápido, uno capaz); openai: 2; openai_compatible: 'qwen3-8b', 'qwen3-4b' (coste 0)
export const DEFAULT_MODEL: Record<'anthropic' | 'openai' | 'openai_compatible', string>
export function modelInfo(provider, id): ModelInfo | null   // desconocido → null (coste 0, sin visión)
export function estimateCostCents(info: ModelInfo | null, tokensIn: number, tokensOut: number): number

// lib/ai/provider.ts
export interface AiConfig { provider: 'anthropic'|'openai'|'openai_compatible'; model: string; apiKey: string | null; baseUrl: string | null; structuredOutput: boolean }
export function resolveAiConfig(h: Pick<Household, 'aiProvider'|'aiModel'|'aiBaseUrl'|'aiApiKeyEnc'|'aiStructuredOutput'>): AiConfig | null   // descifra con decryptSecret(getKeys().secrets); fallback a env AI_ANTHROPIC_API_KEY / AI_OPENAI_API_KEY / AI_LOCAL_BASE_URL+AI_LOCAL_MODEL; provider 'none' → null
export function languageModel(cfg: AiConfig): LanguageModel   // createAnthropic({apiKey})(model) | createOpenAI({apiKey})(model) | createOpenAICompatible({ name:'local', baseURL, apiKey: apiKey ?? 'local' })(model)

// lib/services/ai-settings.ts
export function getAiSettings(ctx): Promise<{ provider; model: string|null; baseUrl: string|null; hasKey: boolean; monthlyCapCents: number; structuredOutput: boolean; spentThisMonthCents: number }>
export function updateAiSettings(ctx, input: AiSettings): Promise<void>     // owner; cifra apiKey si viene (cadena vacía = mantener; null = borrar)
export function testAiConnection(ctx): Promise<{ ok: boolean; message: string; latencyMs: number }>   // generateText 'Responde OK' con maxOutputTokens 5, timeout 15 s; registra en ai_usage_log operation 'test'
```
`AiSettingsSchema = z.strictObject({ provider: z.enum(['none','anthropic','openai','openai_compatible']), model: z.string().max(80).nullable(), baseUrl: z.url().nullable(), apiKey: z.string().max(200).nullable().optional(), monthlyCapCents: z.number().int().min(0).max(100_000), structuredOutput: z.boolean() })`.

- [ ] **Step 1: Tests** — `provider.test.ts` (unit; `process.env.APP_SECRET` de test): `resolveAiConfig` con `aiProvider 'none'` → null; con clave cifrada → descifra; sin clave y con env → usa env; `openai_compatible` sin baseUrl ni env → null. `estimateCostCents` con un modelo conocido y con null. `ai-settings.test.ts` (db): `updateAiSettings` cifra (la columna no contiene el texto plano), `hasKey` true, `apiKey: ''` mantiene, `null` borra; miembro no owner → `forbidden`; `spentThisMonthCents` = suma de `ai_usage_log` del mes en curso (insertar 2 filas, una del mes pasado).
- [ ] **Step 2: Implementar**; **Step 3:** verde; **Commit** — `"Añade catálogo de modelos y configuración de IA por hogar"`.

---

### Task 25: Presupuesto y salida estructurada

**Files:**
- Create: `lib/ai/budget.ts`, `lib/ai/budget.test.ts`, `lib/ai/structured.ts`, `lib/ai/structured.test.ts`

**Interfaces:**
```ts
// lib/ai/budget.ts
export class AiBudgetError extends Error { code = 'ai_budget' }
export async function withBudget<T>(db: Db, householdId: string, cfg: AiConfig, operation: string, fn: (model: LanguageModel) => Promise<{ result: T; usage: { inputTokens: number; outputTokens: number } }>): Promise<T>
   // si cap > 0 y SUM(cost_cents) del mes ≥ cap → AiBudgetError sin llamar; tras fn registra ai_usage_log (provider, model, operation, tokens, cost)
// lib/ai/structured.ts
export async function generateStructured<S extends z.ZodType>(cfg: AiConfig, model: LanguageModel, schema: S, prompt: { system: string; user: string }): Promise<{ result: z.infer<S>; usage }>
   // generateObject({ model, schema, system, prompt, maxRetries: 2 }); con cfg.structuredOutput && provider 'openai_compatible' pasa providerOptions para response_format json_schema; con modelos ≤ 8B: prompts cortos, un objeto de salida
```
- [ ] **Step 1: Tests** — `budget.test.ts` (db): cap 100, gasto 120 → lanza y no llama `fn`; cap 0 → llama y registra fila con `cost_cents` calculado; `structured.test.ts` (unit) con `MockLanguageModelV3` de `ai/test` (comprobar el nombre exportado en `node_modules/ai/dist/index.d.ts` — en `ai` 7 es `MockLanguageModelV3`; si es otro, usar el existente) devolviendo `{"quantity":2}` → parsea con el esquema; salida inválida → reintenta y lanza `AiOutputError`.
- [ ] **Step 2: Implementar**; verde; **Commit** — `"Añade tope de gasto y salida estructurada de IA"`.

---

### Task 26: Tareas de IA — ingredientes, importación, nutrición

**Files:**
- Create: `lib/ai/tasks/parse-ingredients.ts`, `lib/ai/tasks/import-recipe.ts`, `lib/ai/tasks/estimate-nutrition.ts`, `lib/ai/tasks/prompts.ts`, `lib/ai/tasks/tasks.test.ts`

**Interfaces:**
```ts
export const ParsedLinesSchema = z.object({ lines: z.array(z.object({ quantity: z.number().nullable(), unit: z.string().nullable(), foodName: z.string(), preparation: z.string().nullable() })) })
export function parseIngredientsFallback(cfg, model, lines: string[], locale): Promise<ParsedIngredient[]>     // solo para líneas con needsReview; confidence 0.5
export function importRecipeFromTextAi(cfg, model, text: string, locale): Promise<RecipeInput>               // esquema RecipeInputSchema recortado (sin foodId/quantity)
export function importRecipeFromImageAi(cfg, model, image: { bytes: Uint8Array; mime: string }, locale): Promise<RecipeInput>   // solo si modelInfo.vision; si no → AiUnsupportedError
export const NutritionEstimateSchema = z.object({ kcal100g: z.number().min(0), protein100g: z.number().min(0), carbs100g: z.number().min(0), fat100g: z.number().min(0), fiber100g: z.number().min(0), defaultUnit: z.enum(['g','ml','ud']), gramsPerUnit: z.number().positive().nullable() })
export function estimateNutrition(cfg, model, foodName: string, locale): Promise<z.infer<typeof NutritionEstimateSchema>>
```
Prompts en `prompts.ts` (español e inglés según `locale`), cortos, con la regla explícita "no inventes cantidades: null si no aparece".
- [ ] **Step 1: Tests** con modelo mock: cada tarea construye el prompt correcto (snapshot del `system`) y mapea la salida al tipo; `importRecipeFromImageAi` con modelo sin visión → `AiUnsupportedError`.
- [ ] **Step 2: Implementar**; verde; **Commit** — `"Añade tareas de IA para ingredientes, importación y nutrición"`.

---

### Task 27: Propuesta de plan por IA y acciones

**Files:**
- Create: `lib/ai/tasks/propose-plan.ts`, `lib/actions/ai.ts`, `lib/services/ai-tasks.ts`, `lib/services/ai-tasks.test.ts`

**Interfaces:**
```ts
// lib/services/ai-tasks.ts — orquesta: config → withBudget → tarea → efectos en DB. Sin proveedor → { ok: false, code: 'no_provider' }
export function aiParseIngredients(ctx, lines: string[]): Promise<AiResult<ParsedIngredient[]>>
export function aiImportRecipe(ctx, input: { kind: 'text'; text: string } | { kind: 'image'; uploadUrl: string }): Promise<AiResult<RecipeInput>>
export function aiEstimateFood(ctx, foodName: string): Promise<AiResult<FoodWithNutrition>>     // crea el alimento con createFood(ctx, …, 'ai') → is_estimated true
export function aiProposeWeek(ctx, input: { from: string; to: string; notes?: string }): Promise<AiResult<{ proposalId: string }>>
   // contexto: recetas del hogar (título, tiempo, etiquetas, veces cocinada, última vez), despensa que caduca, alérgenos de miembros, plan actual del rango; salida ProposalPayloadSchema; crea plan_proposals source 'ai' (usa createProposal de (c) si está mergeada; si no, insert directo con el mismo shape — sustituir al mergear (c))
export type AiResult<T> = { ok: true; data: T } | { ok: false; code: 'no_provider' | 'ai_budget' | 'ai_unsupported' | 'ai_output' | 'internal'; message: string }
```
Acciones (`lib/actions/ai.ts`): `aiParseIngredientsAction`, `aiImportRecipeAction`, `aiEstimateFoodAction`, `aiProposeWeekAction`, `testAiConnectionAction`, `updateAiSettingsAction`.
- [ ] **Step 1: Tests** (db, con `languageModel` inyectable vía `lib/ai/provider.ts::setModelFactoryForTests` o pasando `model` opcional en `ctx`-menos: usar un parámetro `deps = { model?: LanguageModel }` en cada función de `ai-tasks.ts`): sin proveedor → `no_provider`; `aiEstimateFood` crea alimento `source 'ai'`, `is_estimated true`; `aiProposeWeek` crea propuesta pendiente con payload válido y registra uso.
- [ ] **Step 2: Implementar**; verde; **Commit** — `"Añade propuesta de semana por IA y acciones"`.

---

### Task 28: Pantalla de ajustes de IA y ganchos en la UI

**Files:**
- Create: `components/settings/ai-settings-form.tsx`, `components/settings/ai-settings-form.test.tsx`, `components/ai/ai-button.tsx`
- Modify: `app/(app)/settings/ai/page.tsx`, `messages/*/settings.json`, `messages/*/recipes.json`, `messages/*/plan.json`, `docs/05-MCP.md` (nota de modelos locales), `README.md` (arranque de `llama-server`)

- [ ] **Step 1: Test del formulario (ui)** — proveedor `select` (none/anthropic/openai/openai_compatible); al elegir `openai_compatible` aparece `baseUrl`; `apiKey` con placeholder "•••• (guardada)" si `hasKey`; tope en euros (`monthlyCapCents/100`); gasto del mes; switch salida estructurada; "Probar" llama `test` mock y muestra `ok`/`message`; "Guardar" llama `update` con el payload (apiKey `''` cuando no se ha tocado).
- [ ] **Step 2:** página (owner: `requireRole('owner')`; miembro ve solo lectura), formulario, `AiButton` (`SparklesIcon` + etiqueta; `disabled` con tooltip `common.ai.unavailable` cuando no hay proveedor — la página pasa `aiEnabled`): puntos de enganche documentados para (a) y (c): editor de recetas "Analizar con IA" (`aiParseIngredientsAction` sobre las líneas `needsReview`), importar "Importar con IA" (texto), detalle de alimento sin resolver "Estimar nutrición con IA", plan "Proponer semana" → `aiProposeWeekAction` + redirección a `/plan/proposals`. Si (a)/(c) no están mergeadas al hacer esta tarea, dejar los `AiButton` en `components/ai/` con su prop `onClick` y una nota en el informe; la integración en sus páginas se hace en el merge (2–5 líneas por página).
- Claves `settings.ai.*`: `provider` "Proveedor", `providers.{none,anthropic,openai,openai_compatible}` "Sin IA / Anthropic / OpenAI / Servidor local (compatible OpenAI)", `model` "Modelo", `baseUrl` "URL del servidor local", `apiKey` "Clave de API", `keySaved` "•••• (guardada)", `cap` "Tope mensual (€)", `spent` "Gastado este mes: {amount}", `structured` "Salida estructurada (JSON Schema)", `test` "Probar conexión", `testOk` "Conectado en {ms} ms", `testFail` "No se pudo conectar: {message}", `localHint` "Recomendado: llama-server con qwen3-8b Q4_K_M (8 GB) o qwen3-4b (4 GB).", `ownerOnly` "Solo el propietario puede cambiar la IA."; `common.ai.{unavailable,working}`; `recipes.ai.{analyze,importText,estimate}`; `plan.ai.propose`. Y `en`.
- README: sección "IA opcional" con `llama-server -m qwen3-8b-q4_k_m.gguf -ngl 99 -c 8192 -fa --jinja --port 8080` y la alternativa Ollama (`/v1`); sin nombres de asistentes.
- [ ] **Step 3:** `pnpm check && pnpm build`; **Commit** — `"Añade ajustes de IA y botones de IA en la interfaz"`.

**Pista (e) lista cuando:** verde; con `AI_LOCAL_*` en `.env` y un servidor local levantado, "Probar" devuelve OK (manual; no hay servidor en CI).

---

# Pista (f) · ShopList — Tareas 29–31

Worktree `w2-f-shoplist` desde `main` **con (c) y (d) mergeadas** (usa `plannedEntriesForShopping` y `pantryAsDomain`). Base de test `rezetapp_test_f`.

### Task 29: Cliente de la Edge Function de ShopList

**Files:**
- Create: `lib/integrations/shoplist.ts`, `lib/integrations/shoplist.test.ts`

**Interfaces:**
```ts
export interface ShopListConfig { fnUrl: string; secret: string; listToken: string }
export function pushToShopList(cfg: ShopListConfig, lines: ShoppingLine[], fetchImpl?: typeof fetch): Promise<{ inserted: number; batches: number }>
   // lotes de 100 (docs/06): POST `${fnUrl}/import-items` con { source: 'RezetApp', listToken, items: lines.map(toShopListItem) }, Authorization Bearer secret; error HTTP → ShopListError(status, body)
export function shopListDeepLink(listToken: string): string   // https://shop.jarsss8.es/#/s/<token>
```
- [ ] **Step 1: Tests** — 250 líneas → 3 lotes, suma `inserted`; cabeceras y body correctos (fetch mock captura); 401 → `ShopListError`; `toShopListItem` de dominio: `ud` → quantity entera y nombre limpio, `g` → "Lentejas · 300 g" y `quantity null` (ya testeado en dominio; aquí solo integración).
- [ ] **Step 2: Implementar**; verde; **Commit** — `"Añade cliente de la función de importación de ShopList"`.

---

### Task 30: Servicio de compra y ajustes de ShopList

**Files:**
- Create: `lib/services/shopping.ts`, `lib/services/shopping.test.ts`, `lib/services/shoplist-settings.ts`, `lib/actions/shopping.ts`
- Modify: `lib/validation/household.ts` (añade `ShoplistSettingsSchema = z.strictObject({ fnUrl: z.url().nullable(), secret: z.string().max(200).nullable().optional(), listToken: z.string().max(120).nullable() })`), `lib/services/plan.ts` (sustituye `pantryForShopping` por `pantryAsDomain` de (d))

**Interfaces:**
```ts
export function generateShopping(ctx, range: DateRange): Promise<{ lines: ShoppingLine[]; from: string; to: string }>
export function pushShopping(ctx, lines: ShoppingLine[]): Promise<{ inserted: number; deepLink: string }>   // requiere config (hogar o env); actualiza shoplist_last_pushed_at
export function getShoplistSettings(ctx): Promise<{ fnUrl: string|null; listToken: string|null; hasSecret: boolean; source: 'household'|'env'|'none'; lastPushedAt: string|null }>
export function updateShoplistSettings(ctx, input: ShoplistSettings): Promise<void>   // owner; cifra secret con getKeys().secrets
export function resolveShopListConfig(ctx): Promise<ShopListConfig | null>              // hogar → env (SHOPLIST_FN_URL, SHOPLIST_IMPORT_SECRET, SHOPLIST_LIST_TOKEN) → null
```
- [ ] **Step 1: Tests** (db): `generateShopping` con 1 receta planificada (2 raciones de una receta base 4 con 400 g) y 100 g en despensa → línea de 100 g; entrada cocinada excluida; `pushShopping` sin config → `validation`; con config de hogar y fetch mock → `inserted` y `shoplist_last_pushed_at` actualizado; env fallback; secreto cifrado en DB.
- [ ] **Step 2: Implementar** + acciones `generateShoppingAction(range)`, `pushShoppingAction(lines)`, `updateShoplistSettingsAction(input)`; verde; **Commit** — `"Añade consolidación y envío de la compra a ShopList"`.

---

### Task 31: UI de envío y ajustes de ShopList

**Files:**
- Create: `components/settings/shoplist-settings-form.tsx`, `components/plan/shopping-push-button.tsx`
- Modify: `app/(app)/settings/shoplist/page.tsx`, `app/(app)/plan/shopping/page.tsx`, `messages/*/settings.json`, `messages/*/plan.json`, `e2e/plan.spec.ts` (añade caso: resumen de compra muestra la línea; botón de envío oculto sin config)

- [ ] **Step 1:** ajustes: formulario (owner) con `fnUrl`, `secret` (placeholder si `hasSecret`), `listToken`, estado de la fuente (`settings.shoplist.source.{household,env,none}`), último envío; enlace "Abrir en ShopList" con `shopListDeepLink`. Resumen de compra: `ShoppingPushButton` visible solo si `resolveShopListConfig` ≠ null; al pulsar envía las líneas mostradas (`pushShoppingAction`), muestra `inserted` y el enlace profundo (`SendIcon`, `LinkIcon`).
- Claves `settings.shoplist.*`: `fnUrl` "URL de la función de importación", `secret` "Secreto", `secretSaved` "•••• (guardado)", `listToken` "Token de la lista", `source.{household,env,none}` "Configurado en este hogar / Configurado en el servidor / Sin configurar", `lastPushed` "Último envío: {date}", `open` "Abrir en ShopList", `hint` "RezetApp calcula qué falta y lo envía a ShopList; la lista vive allí."; `plan.shopping.{title,range,send,sent,openList,unresolved,noQuantity,empty,noConfig}`.
- [ ] **Step 2:** e2e + `pnpm check && pnpm build`; **Commit** — `"Añade ajustes de ShopList y botón de envío de la compra"`.

**Pista (f) lista cuando:** verde; con `SHOPLIST_*` en `.env` un envío real inserta en la lista (manual, contra la Edge Function del repo de ShopList).

---
# Pista (g) · Ajustes — Tareas 32–37

Worktree `w2-g-settings` desde `main` (independiente). Base de test `rezetapp_test_g`.

### Task 32: Tokens de API

**Files:**
- Create: `lib/services/api-tokens.ts`, `lib/services/api-tokens.test.ts`, `lib/actions/settings.ts`, `components/settings/api-tokens-panel.tsx`, `components/settings/api-tokens-panel.test.tsx`
- Modify: `app/(app)/settings/tokens/page.tsx`, `messages/*/settings.json`

**Interfaces:**
```ts
export function listApiTokens(ctx): Promise<{ id: string; name: string; scopes: string[]; mcpProfile: 'basic'|'full'; createdAt: string; lastUsedAt: string|null; revokedAt: string|null }[]>
export function createApiToken(ctx, input: ApiTokenCreate): Promise<{ id: string; token: string }>   // owner; token en claro SOLO aquí (generateApiToken + hashToken de lib/auth/api-tokens)
export function revokeApiToken(ctx, id): Promise<void>                                              // owner; revoked_at = now
```
- [ ] **Step 1: Tests** (db): crear devuelve `rz_…`, guarda hash y no el token; listar no expone hash; revocar; miembro → `forbidden`; hogar B no ve/revoca tokens de A. Test ui del panel: tras crear, muestra el token en un bloque `code` con botón copiar (`CopyIcon`) y aviso "solo se muestra una vez"; lista con scopes como chips; botón revocar con confirmación.
- [ ] **Step 2: Implementar** + acciones `createApiTokenAction`, `revokeApiTokenAction`; página con instrucciones de conexión MCP (`settings.tokens.mcpHint`: "Usa este token como `Authorization: Bearer` en `/mcp` o `/api/v1`. Los conectores que exigen OAuth no son compatibles todavía; usa un cliente MCP de escritorio o `mcp-remote --header`.").
- Claves `settings.tokens.*`: `create` "Crear token", `name` "Nombre", `scopes` "Permisos", `scope.<id>` (9), `profile` "Perfil MCP", `profiles.{basic,full}` "Básico (12 herramientas, sin editar ni borrar) / Completo", `created` "Token creado. Cópialo ahora: no se volverá a mostrar.", `copy` "Copiar", `copied` "Copiado", `revoke` "Revocar", `revoked` "Revocado", `lastUsed` "Último uso: {date}", `never` "Nunca", `empty` "Sin tokens. Crea uno para conectar un asistente o un script.", `mcpHint`.
- [ ] **Step 3:** verde; **Commit** — `"Añade gestión de tokens de API"`.

---

### Task 33: Apariencia y preferencias de usuario

**Files:**
- Create: `lib/services/user-prefs.ts`, `lib/services/user-prefs.test.ts`, `components/settings/appearance-form.tsx`, `components/settings/appearance-form.test.tsx`
- Modify: `app/(app)/settings/appearance/page.tsx`, `lib/actions/settings.ts`, `messages/*/settings.json`

- [ ] **Step 1:** `updateUserPrefs(ctx, input: UserPrefs): Promise<User>` (db test: actualiza `users.*` del `ctx.userId`; token sin usuario → `forbidden`). Acción `updateUserPrefsAction`: tras el servicio, `cookies().set(PREFS_COOKIE, prefsCookieValue(user), prefsCookieOptions(APP_URL))` y `revalidatePath('/', 'layout')` para que `<html data-theme data-accent lang>` cambie sin recargar; el idioma se aplica en la siguiente petición (next-intl lee la cookie).
- [ ] **Step 2:** formulario: tema (3 botones `aria-pressed`), acento (8 muestras de color con `style={{ background: 'var(--acc)' }}` NO — literales de color prohibidos: usar `data-accent=<nombre>` en cada muestra y la clase `bg-primary` para que el token pinte cada una), unidades (métrico/imperial), idioma (es/en), nombre visible. Test ui: click en "miel" → `update` con `{ accent: 'miel' }`.
- Claves `settings.appearance.*`: `theme` "Tema", `themes.{system,light,dark}` "Sistema/Claro/Noche suave", `accent` "Acento", `accents.{huerta,miel,tomate,pistacho,higo,berenjena,arandano,canela}`, `units` "Unidades", `unitsMetric` "Métrico", `unitsImperial` "Imperial", `language` "Idioma", `displayName` "Tu nombre", `saved` "Guardado".
- [ ] **Step 3:** verde; **Commit** — `"Añade ajustes de apariencia y preferencias"`.

---

### Task 34: Miembros e invitaciones

**Files:**
- Create: `lib/services/members.ts`, `lib/services/members.test.ts`, `components/settings/members-panel.tsx`, `components/settings/members-panel.test.tsx`
- Modify: `app/(app)/settings/members/page.tsx`, `lib/actions/settings.ts`, `messages/*/settings.json`

- [ ] **Step 1:** servicio: `listMembers(ctx)` (reutiliza `getHouseholdOverview`), `updateMember(ctx, input: MemberUpdate)` (owner o uno mismo; alérgenos de `ALLERGENS`; `dietaryFlags` libres), `removeMember(ctx, userId)` (owner; no a sí mismo; borra sus sesiones del hogar), `createInvite(ctx)` ya existe en `households.ts`. Tests db incl. aislamiento.
- [ ] **Step 2:** panel: lista con rol, alérgenos (chips con claves propias `settings.members.allergen.<id>`; no se reutilizan las de `recipes.food.allergens` porque son namespaces distintos), editar propios (o todos si owner), "Invitar" → `createInviteAction` → muestra URL con copiar + caducidad 24 h; "Expulsar" con confirmación. Test ui: invitar muestra la URL.
- Claves `settings.members.*`: `invite` "Invitar", `inviteUrl` "Enlace de invitación (caduca en 24 h)", `copy`, `role.{owner,member}` "Propietario/Miembro", `allergens` "Alergias", `allergen.<id>` (14), `dietaryFlags` "Preferencias (p. ej. vegetariano)", `dietaryPlaceholder` "Escribe y pulsa Enter", `remove` "Expulsar", `removeConfirm` "¿Expulsar a {name} del hogar?", `you` "tú".
- [ ] **Step 3:** verde; **Commit** — `"Añade gestión de miembros e invitaciones"`.

---

### Task 35: Hogar — nombre, raciones, alerta, salir y borrar

**Files:**
- Create: `components/settings/household-form.tsx`, `components/settings/danger-zone.tsx`, `components/settings/danger-zone.test.tsx`
- Modify: `app/(app)/settings/household/page.tsx`, `lib/services/households.ts` (añade `updateHousehold(ctx, input: HouseholdUpdate)` owner), `lib/services/households.test.ts`, `lib/actions/settings.ts`, `messages/*/settings.json`

- [ ] **Step 1:** `updateHousehold` (db test: owner actualiza; miembro → forbidden). Acciones: `updateHouseholdAction`, `leaveHouseholdAction` (→ `leaveHousehold`, luego `logoutSession` y `redirect('/login')`), `deleteHouseholdAction(confirmName)` (→ `deleteHousehold`, logout, redirect). `DangerZone`: "Salir del hogar" (miembro, o owner si hay otro owner — mostrar el mensaje de error del servicio) y "Borrar hogar" (owner; input que debe coincidir con el nombre; botón `variant="destructive"`). Test ui: el botón borrar está `disabled` hasta que el texto coincide.
- Claves `settings.household.*`: `name` "Nombre del hogar", `defaultServings` "Raciones por defecto", `expiryAlertDays` "Avisar de caducidad con (días)", `save`, `saved`, `leave` "Salir del hogar", `leaveConfirm` "Perderás el acceso a las recetas y al plan de este hogar.", `delete` "Borrar hogar", `deleteHint` "Escribe «{name}» para confirmar. Se borra todo: recetas, plan, despensa, tokens.", `deleteConfirmField` "Nombre del hogar", `switch` "Cambiar de hogar", `households` "Tus hogares".
- Cambio de hogar activo: lista de `listHouseholdsOf` con botón → `switchHouseholdAction(householdId)` (→ `switchHousehold(db, session.id, …)` + `revalidatePath('/', 'layout')`).
- [ ] **Step 2:** verde; **Commit** — `"Añade ajustes del hogar, cambio de hogar, salir y borrar"`.

---

### Task 36: Passkeys y cerrar sesión

**Files:**
- Create: `lib/services/passkeys.ts`, `lib/services/passkeys.test.ts`, `app/api/auth/passkeys/options/route.ts`, `app/api/auth/passkeys/verify/route.ts`, `components/settings/passkeys-panel.tsx`, `components/settings/add-passkey-button.tsx`, `components/settings/logout-button.tsx`
- Modify: `app/(app)/settings/passkeys/page.tsx`, `app/(app)/settings/layout.tsx`, `lib/services/auth.ts` (añade `passkeyOptions(userId, displayName)` → `startRegistration(db, displayName, userId)` y `passkeyVerify(userId, {challengeId, response, name})` → `finishRegistration` + `addCredentialToUser`), `messages/*/settings.json`, `messages/*/common.json`

- [ ] **Step 1:** servicio: `listPasskeys(ctx)` (`credential_id` truncado, `name`, `deviceType`, `backedUp`, `createdAt`, `lastUsedAt`), `renamePasskey(ctx, credentialId, name)`, `removePasskey(ctx, credentialId)` (prohibido si es la última → `conflict`). Tests db. Rutas `POST /api/auth/passkeys/options` (sesión requerida; `excludeCredentials` ya lo pone `startRegistration`) y `verify` (`readJson`, esquema `z.strictObject({ challengeId: IdSchema, name: z.string().max(60).nullable(), response: … })` — añadir `PasskeyVerifyBodySchema` en `lib/validation/household.ts`).
- [ ] **Step 2:** panel: lista, renombrar inline, eliminar con confirmación, `AddPasskeyButton` (cliente, `@simplewebauthn/browser` `startRegistration` como en `passkey-register-form.tsx`). `LogoutButton` (`LogoutIcon`, `POST /api/auth/logout` → `router.push('/login')`) en `settings/layout.tsx` junto al título.
- Claves `settings.passkeys.*`: `add` "Añadir passkey", `name` "Nombre del dispositivo", `unnamed` "Sin nombre", `rename` "Renombrar", `remove` "Eliminar", `lastOne` "No puedes eliminar tu única passkey.", `synced` "Sincronizada", `device` "Solo este dispositivo", `added` "Passkey añadida", `lastUsed` "Último uso: {date}"; `common.logout` "Cerrar sesión".
- [ ] **Step 3:** verde; **Commit** — `"Añade gestión de passkeys y cierre de sesión"`.

---

### Task 37: Datos (exportar) y e2e de ajustes

**Files:**
- Create: `e2e/settings.spec.ts`
- Modify: `app/(app)/settings/data/page.tsx` (usa `ExportButton` de (a) si está mergeada; si no, crea `components/settings/export-button.tsx` con el mismo contrato — al mergear se queda uno), `messages/*/settings.json`

- [ ] **Step 1: e2e** — registro; `/settings/appearance`: cambiar acento a "miel" → `html[data-accent="miel"]`; tema "Noche suave" → `data-theme="dark"`; `/settings/household`: cambiar raciones por defecto a 3 → guardado; `/settings/tokens`: crear token con `recipes:read` → aparece `rz_`; `curl`-equivalente con `page.request.get('/api/v1/household', { headers: { authorization: 'Bearer ' + token } })` → 403 (falta `household:read`) y con un token con `household:read` → 200; `/settings/members`: "Invitar" muestra URL; `/settings/passkeys`: lista 1; "Cerrar sesión" → `/login`.
- [ ] **Step 2:** `settings/data`: exportar recetas (JSON) + nota "Importar/migrar llega en W4". Claves `settings.data.{export,exportHint,importSoon}`.
- [ ] **Step 3:** e2e + `pnpm check && pnpm build`; **Commit** — `"Añade exportación en ajustes y e2e de ajustes"`.

**Pista (g) lista cuando:** verde + `e2e/settings.spec.ts`.

---

# Pista (h) · MCP mínimo — Tareas 38–40

Worktree `w2-h-mcp` desde `main` **con (a) mergeada** (usa `searchRecipes`/`getRecipe`). Base de test `rezetapp_test_h`. Dependencia: `pnpm add @modelcontextprotocol/sdk@1.30.0`. Decisión de transporte (spec §12/§18.5): `WebStandardStreamableHTTPServerTransport` (existe en 1.30.0: `dist/esm/server/webStandardStreamableHttp.js`), **stateless** (`sessionIdGenerator: undefined`), un `McpServer` por petición.

### Task 38: Servidor MCP y autenticación

**Files:**
- Create: `lib/mcp/auth.ts`, `lib/mcp/server.ts`, `lib/mcp/server.test.ts`, `app/mcp/route.ts`
- Modify: `eslint.config.mjs` (elemento `mcp` = `lib/mcp/**`, permitido → mcp/services/validation/auth/domain/lib; `app` → + `mcp`), `lib/eslint-boundaries.test.ts`

**Interfaces:**
```ts
// lib/mcp/auth.ts
export async function authenticateMcp(request: Request): Promise<Ctx & { mcpProfile: 'basic'|'full'; scopes: string[] }>   // authenticateApiToken(db, Authorization, []) — los scopes se comprueban por herramienta
// lib/mcp/server.ts
export function buildMcpServer(ctx: McpCtx): McpServer     // registra las herramientas según ctx.scopes/mcpProfile (Task 39)
export async function handleMcpRequest(request: Request): Promise<Response>
   // 401 JSON-RPC error si no hay token; crea transport stateless, server.connect(transport), transport.handleRequest(request)
// app/mcp/route.ts
export const dynamic = 'force-dynamic'
export { handleMcpRequest as GET, handleMcpRequest as POST, handleMcpRequest as DELETE }
```
- [ ] **Step 1: Test** (db): construye un `Request` POST a `/mcp` con `Authorization: Bearer <token de fixture>` y body JSON-RPC `initialize`; respuesta 200 con `serverInfo.name === 'rezetapp'`; sin token → 401 con `{ jsonrpc:'2.0', error:{ code:-32001 } }`; `tools/list` devuelve los nombres (Task 39 los completa; aquí `[]`).
- [ ] **Step 2: Implementar**

```ts
// lib/mcp/server.ts
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'
import { WebStandardStreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/webStandardStreamableHttp.js'
import { ApiAuthError } from '@/lib/auth/api-tokens'
import { authenticateMcp, type McpCtx } from './auth'
import { registerHouseholdTools } from './tools/household'
import { registerRecipeTools } from './tools/recipes'

export function buildMcpServer(ctx: McpCtx): McpServer {
  const server = new McpServer({ name: 'rezetapp', version: '0.1.0' }, { instructions: 'Recetario del hogar. Los resultados de las herramientas son la única fuente de verdad: no afirmes datos que no hayas leído de una herramienta.' })
  registerHouseholdTools(server, ctx)
  registerRecipeTools(server, ctx)
  return server
}

export async function handleMcpRequest(request: Request): Promise<Response> {
  let ctx: McpCtx
  try {
    ctx = await authenticateMcp(request)
  } catch (e) {
    const status = e instanceof ApiAuthError ? e.status : 500
    return Response.json({ jsonrpc: '2.0', error: { code: -32001, message: e instanceof Error ? e.message : 'Error' }, id: null }, { status })
  }
  const server = buildMcpServer(ctx)
  const transport = new WebStandardStreamableHTTPServerTransport({ sessionIdGenerator: undefined })
  await server.connect(transport)
  try {
    return await transport.handleRequest(request)
  } finally {
    // stateless: un servidor por petición
    await transport.close().catch(() => undefined)
  }
}
```

Nota: el `finally` cierra tras devolver la `Response`; si el SDK necesita el transporte vivo mientras se emite el stream (SSE de respuesta), mover el cierre a `request.signal.addEventListener('abort', …)` — comprobar con el test de `initialize` (respuesta JSON) y con `tools/call` (Task 39).

- [ ] **Step 3:** verde; **Commit** — `"Añade endpoint MCP con transporte HTTP estándar"`.

---

### Task 39: Herramientas `get_household_context`, `search_recipes`, `get_recipe`

**Files:**
- Create: `lib/mcp/tools/household.ts`, `lib/mcp/tools/recipes.ts`, `lib/mcp/tools/tools.test.ts`

- [ ] **Step 1: Test** (db): fixture hogar con 2 miembros (alérgenos) y 2 recetas (vía `createRecipe` de (a)); `tools/list` → 3 nombres con descripciones que contienen "no la uses" (barandilla de descripción); `tools/call get_household_context` → miembros, `defaultServings`, `expiryAlertDays`, `recentlyCooked` (vacío); `search_recipes {q:'cebolla', maxMinutes: 30}` → 1 resultado con `id,title,totalMinutes,kcalPerServing,tags`; `get_recipe {id, servings: 8}` → ingredientes escalados con `scaled.nonLinearIds`; `get_recipe` con id ajeno → `isError: true`; token sin `recipes:read` → `search_recipes` no aparece en `tools/list`; argumentos extra → error de validación (`.strict()`).
- [ ] **Step 2: Implementar** con `server.registerTool(name, { title, description, inputSchema: <zod strictObject> }, async (args) => ({ content: [{ type: 'text', text: JSON.stringify(result) }] }))`; errores → `{ isError: true, content: [{ type:'text', text: mensaje }] }`. Descripciones (en español, cortas, con "Úsala cuando… / No la uses para…"): `get_household_context` — "Miembros, alérgenos, raciones por defecto y lo cocinado recientemente. Llámala una vez al empezar. No la uses para buscar recetas."; `search_recipes` — "Busca recetas del hogar por texto, etiquetas, tiempo máximo o alimentos. Devuelve resúmenes con id. No la uses para leer una receta completa: usa get_recipe."; `get_recipe` — "Receta completa por id, opcionalmente escalada a N raciones (el escalado lo hace el servidor). No inventes cantidades: usa las devueltas." Scopes: `household:read`, `recipes:read`, `recipes:read`. Perfil `full` no añade nada en W2 (documentar).
- [ ] **Step 3:** verde; **Commit** — `"Añade las herramientas MCP básicas de contexto y recetas"`.

---

### Task 40: Documentación de conexión y prueba manual

**Files:**
- Modify: `docs/05-MCP.md`, `README.md`. (Decisión: el e2e de MCP se hace en W3, cuando exista la REST de tokens; en W2 la cobertura del MCP son los tests de integración bajo el proyecto `db` de las Tareas 38–39.)

- [ ] **Step 1:** `docs/05-MCP.md`: sección "Conexión" con el JSON de configuración de un cliente de escritorio vía `npx mcp-remote https://<host>/mcp --header "Authorization: Bearer rz_…"`, y la nota de límite OAuth. README: enlace. `curl` de prueba manual documentado: `curl -s -X POST localhost:3000/mcp -H 'authorization: Bearer rz_…' -H 'content-type: application/json' -H 'accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'` → ejecutarlo contra `pnpm dev` con un token creado en `/settings/tokens` (si (g) está mergeada) o insertado a mano (`hashToken` desde `tsx -e`) y pegar la salida en el informe.
- [ ] **Step 2:** `pnpm check`; **Commit** — `"Documenta la conexión al MCP"`.

**Pista (h) lista cuando:** `tools/list` y las 3 llamadas funcionan por `curl` contra el dev server; tests `db` verdes.

---

## Criterio de «W2 hecha» (todas las pistas mergeadas en `main`)

- `pnpm check`, `pnpm build`, `pnpm e2e` (recipes, pantry, plan, settings + auth/smoke/health) verdes en `main` con el seed de alimentos cargado.
- Flujo manual: registrarse → crear receta con 3 ingredientes → escalar y ver kcal/ración estable → añadir a la despensa por búsqueda y por código → planificar la semana con arrastrar y soltar → generar compra (y enviar si hay ShopList) → crear token → `tools/list` por `curl`.
- `docs/07-ROADMAP.md`: fases 1 (recetas), 3 (plan salvo SSE en pantallas — W3) y 4 (despensa salvo `log_cooked` — W3) marcadas como hechas en W2; fase 2 (MCP) mínima.
- `AGENTS.md` sección "Estado del proyecto": "W2 hecha: …" con la fecha.

## Decisiones tomadas en este plan (no cubiertas por el spec)

1. **Corrección manual de un alimento global = copia del hogar** (`correctFood` devuelve un id NUEVO; los llamadores sustituyen el `foodId`). Evita mutar el catálogo compartido y cumple "la corrección manual gana siempre" por hogar.
2. **Resolución en cascada sin IA en (b)**: el paso (d) "estimación por IA" no está dentro de `resolveFoodName`; es una acción explícita del usuario ("Estimar con IA", pista (e)) que crea un alimento `source 'ai'`, `is_estimated true`. Así (b) mergea antes que (e) y la app funciona sin IA.
3. **`ActionResult<T>`** como contrato único de server actions (`lib/actions/result.ts`).
4. **Subidas**: `UPLOADS_DIR` (default `./data/uploads`), webp 82 %, lado máximo 1600 px, servido por `GET /api/uploads/<household>/<file>` solo a la sesión del mismo hogar; `sharp` en `serverExternalPackages`.
5. **Búsqueda de recetas**: `websearch_to_tsquery` bilingüe sobre el `search_vector` generado; `onlyWithPantry` = no existe ingrediente con `food_id` sin fila en despensa con cantidad > 0.
6. **Importar desde URL**: JSON-LD (incl. `@graph`) antes que microdata; el resultado es un borrador en `sessionStorage` que abre el editor (no se persiste sin revisar).
7. **Plan**: `applyBatch` ignora `remove` de ids inexistentes; `moveEntry` con `sortOrder`; `patchEntry({skipped})` mapea a `skipped_at`; la propuesta reutiliza `PlanBatchSchema`. La despensa para el resumen de compra en (c) usa una consulta local hasta que (f) la sustituye por `pantryAsDomain`.
8. **IA**: catálogo de modelos en `lib/ai/models.ts` con precios; presupuesto por `SUM(cost_cents)` del mes; `AiResult` con códigos `no_provider|ai_budget|ai_unsupported|ai_output`; la propuesta de IA entra por `plan_proposals` como cualquier otra.
9. **ShopList**: configuración por hogar cifrada con la clave `secrets` de HKDF; fallback a env; envío en lotes de 100; enlace profundo fijo `https://shop.jarsss8.es/#/s/<token>`.
10. **Tokens API**: el token en claro solo se devuelve al crearlo; perfil `full` no añade herramientas en W2.
11. **MCP**: stateless, un `McpServer` por petición; scopes por herramienta; descripciones con "no la uses para…"; e2e de MCP pospuesto a W3 (necesita REST de tokens).
12. **Nuevos elementos de boundaries**: `uploads` (`lib/uploads/**`) y `mcp` (`lib/mcp/**`).

## Auto-revisión

- **Cobertura del spec**: §8 Recetas (T9–T12), Despensa (T14–T17), Plan (T20–T23), Ajustes (T28, T31–T37); §9.1 recetas (T6–T7), §9.2 plan (T18–T19), §9.3 despensa (T13), §9.4 cascada (T2–T3 + IA en T27), §9.6 compra (T30), §9.8 IA (T24–T28); §10 (T24–T28); §12 mínimo (T38–T40); §13 (T29–T31); §14 hook usado en T14/T20; §16 tests por pista. Fuera de W2 por diseño: `logCooked`/cocina, Hoy, REST completo, MCP completo (W3); foto/PDF, reglas, PWA (W4).
- **Placeholders**: ninguna tarea dice "TBD/TODO"; donde una pista depende de otra no mergeada, el plan fija la alternativa concreta (consulta local, componente propio) y quién la sustituye en el merge.
- **Nombres reales de `main`**: `requireHousehold`, `requireRole`, `requireApiToken`, `apiErrorResponse`, `readJson`, `getCurrentSession`, `authenticateApiToken`, `generateApiToken`, `hashToken`, `ApiAuthError`, `Ctx`, `ServiceError`, `getHouseholdOverview`, `createInvite`, `listHouseholdsOf`, `leaveHousehold`, `deleteHousehold`, `switchHousehold`, `logoutSession`, `startRegistration`, `finishRegistration`, `addCredentialToUser`, `emitHouseholdEvent`, `useHouseholdEvents`, `getKeys`, `encryptSecret`, `decryptSecret`, `PREFS_COOKIE`, `prefsCookieValue`, `prefsCookieOptions`, esquemas `RecipeInputSchema`, `RecipeSearchSchema`, `RecipeImportSchema`, `RecipeIngredientInputSchema`, `FoodInputSchema`, `FoodSearchSchema`, `BarcodeSchema`, `PantryItemInputSchema`, `PantryAdjustSchema`, `PantryQuerySchema`, `PlanBatchSchema`, `PlanEntryMoveSchema`, `PlanEntryPatchSchema`, `ProposalPayloadSchema`, `ShoppingGenerateSchema`, `ShoppingLineSchema`, `ApiTokenCreateSchema`, `HouseholdUpdateSchema`, `MemberUpdateSchema`, `UserPrefsSchema`, `DeleteHouseholdSchema`, `ALLERGENS`; dominio `parseIngredientLine`, `toBaseUnit`, `toDisplayUnit`, `formatQuantity`, `normalizeSearchName`, `scaleRecipe`, `isNonLinearByDefault`, `recipeNutrition`, `aggregateNutrition`, `detectTimers`, `expiringSoon`, `entryStatus`, `consolidateNeeds`, `toShopListItem`; iconos existentes usados por nombre real. Tipos que el plan pide AÑADIR a validation (`FoodInput`, `FoodCorrection`, `RecipeSearch`, `PantryItemInput`, `PantryQuery`, `PantryAdjust`, `PlanEntryInput`, `PlanEntryMove`, `PlanEntryPatch`, `AiSettingsSchema`, `ShoplistSettingsSchema`, `PasskeyVerifyBodySchema`) son añadidos compatibles.
- **Consistencia**: `FoodWithNutrition` incluye `nameEs`/`nameEn` (T2) y el mapper de (a) lo acepta como `FoodNutrition` (subtipo); `PreparedIngredient` (T6) es el contrato del editor (T11) y de `prepareIngredientsAction` (T9); `RecipeDraft` (T7) es lo que guarda `ImportForm` (T12) y lee `new?draft=1` (T11); `PlanEntryView`/`ProposalView` (T18–T19) alimentan T20–T22; `plannedEntriesForShopping` (T23) lo consume `generateShopping` (T30).
